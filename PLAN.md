# naturalPaint — Implementation Plan

Ordered, executable steps. Requirements are in [PRD.md](PRD.md), mechanism in
[DESIGN-imaging.md](DESIGN-imaging.md), settled decisions in [docs/adr/](docs/adr/),
terminology in [CONTEXT.md](CONTEXT.md).

Every step states **what**, **files**, and **verify**. A step is not done until its
verification passes.

> **Detail tapers on purpose.** Phase 1 is at file-and-function level because the code
> it touches exists and has been read. Phases 2–5 and 19 are step-level. Phases 6–18 are
> outlines — writing them at step level now would be inventing specifics that the
> earlier phases will contradict. Re-expand each phase when it becomes next.

The operation catalogue — every colour control, filter, transform, selection and clipboard
op with its evaluation class — is [docs/operations.md](docs/operations.md).

---

# Phase 1 — Make the simulation obey the new rules

No new user-facing features except one bug fix. Establishes the invariants and the
measurement baseline everything later depends on.

**Order within the phase is load-bearing:** latency first because it is the top
product risk and cheapest to check now; fixed timestep second because it makes
`--diag` reproducible, and the other changes are verified *with* `--diag`.

## 1.1 — Latency baseline

**What.** Measure pen-to-photon end to end, before any brush work exists. PRD risk #1
is that this target is unreachable on wgpu; if it reads 60 ms, the premise of the
whole painting chain is broken and you need to know now, not at phase 10.

- Capture `SDL_PenEvent` arrival time (`SDL_GetTicksNS`) and carry it with the point.
- Record time immediately after `wgpuSurfacePresent` for the frame that first drew
  that point.
- Accumulate p50 / p99 over a stroke; print on stroke end and via a `--latency` flag.

**Files.** `src/main.cpp`, new `src/app/Latency.{hpp,cpp}`

**Verify.** Paint a few strokes; read p50/p99. Record the number in this file's
Findings section below whatever it is. Target < 20 ms (PRD F3).

> If p50 exceeds ~30 ms, stop and investigate present mode and frame pacing before
> continuing. Everything downstream assumes this is fixable.

## 1.2 — Fixed timestep

**What.** Replace frame-derived `dt` with a fixed step plus an accumulator.

- `kFixedDt = 1.0f / 240.0f` — matches the current effective substep rate.
- Per frame: `acc += min(realElapsed, kMaxCatchUp)`, run `floor(acc / kFixedDt)`
  substeps, keep the remainder.
- Cap substeps per frame (8) so a slow frame cannot trigger a death spiral.
- `P.dt` becomes the constant, not a per-frame value.

**Files.** `src/sim/PaintSim.cpp` (step loop), `src/main.cpp` (timing), possibly
`src/app/AppState.hpp`

**Verify.**
```bash
./build/src/naturalPaint --diag 20 > a.txt && ./build/src/naturalPaint --diag 20 > b.txt && diff a.txt b.txt
```
Must be identical. This is PRD H7, and it is what makes 1.3 verifiable at all.

> ⚠️ **`--diag` numbers will shift** relative to the tables recorded in `README.md`,
> because the stepping changes. Re-record them as the new baseline in the same tables
> and note that they are post-fixed-timestep. Do not treat the shift as a regression
> without comparing physics, not numbers.

## 1.3 — Arc-length dab emission

**What.** Deposition becomes per-dab and `dt`-independent (ADR-0003). Closes
`README.md` Known Bug #2.

1. **Build the emitter.** Centripetal Catmull-Rom through the last four sampled
   points; accumulate arc length along it; emit a dab every `spacing × radius`
   pixels; carry the leftover distance across frames so spacing stays continuous.
2. **Feed the solver per dab.** Each emitted dab dispatches the splat pass as a
   *point* (`brushA == brushB`) rather than a per-frame segment. Dab counts per frame
   are small — a fast stroke at 20 px spacing across 200 px is ten dabs — so a
   dispatch per dab is acceptable and avoids a dab buffer for now.
3. **Strip the `dt` scaling** from every deposition term.
4. **Delete `velocityCutoff`** from `oil_transfer.wgsl` — a stationary brush now
   accumulates no arc length, so it emits no dabs, and the hack is redundant.

**Files.** new `src/brush/StrokePath.{hpp,cpp}`; `shaders/splat.wgsl`,
`shaders/oil_splat.wgsl`, `shaders/ink_splat.wgsl`, `shaders/oil_transfer.wgsl`;
`src/sim/PaintSim.cpp`

**Verify.**
- `--diag 20` — pigment mass flat once the brush lifts (PRD F1).
- Hold the brush stationary for 5 s — no mass accumulates. This is the `velocityCutoff`
  behaviour arriving for free.
- **New `--selftest` case:** stroke the same path twice at different speeds; deposited
  mass must match within tolerance. This is the assertion that pins ADR-0003.
- Visual: oil ridges gone under impasto lighting.

## 1.4 — Lazy, per-mode allocation

**What.** ADR-0001. Idle drops from ~294 MB to zero; watercolour from 294 to ~193 MB.

1. **Make the sim optional.** `main.cpp` holds `std::unique_ptr<PaintSim>`, null until
   a paint tool is first used. Every access site becomes construct-on-demand.
2. **Split field allocation by mode.** Currently `PaintSim::init` allocates all of
   `water_ pigC_ pigR_ depC_ depR_ sat_ aux_ paper_ lbmA_ lbmB_ lbmC_ brushVol_
   brushC_ brushR_` unconditionally.
   - watercolour → `water sat aux paper pigC pigR depC depR`
   - ink → `lbmA lbmB lbmC` + the shared water/deposit set
   - oil → `brushVol brushC brushR` + the shared set
3. **Mode switch frees the outgoing set** and allocates the incoming one. The canvas
   already clears on mode switch, so nothing of value is discarded.
4. **Add resident-memory measurement.** On macOS use `task_info` with
   `MACH_TASK_BASIC_INFO` for *current* resident size — `getrusage`'s `ru_maxrss` is
   peak, not current, and will not do.
5. **Add the gate** to `--selftest`: idle RSS < ~~40~~ **80 MB** (revised after
   measuring — see Findings, 2026-08-18: SDL3's own video subsystem costs ~57 MB
   before this project's code runs, and 40 MB was never checked against that), and
   zero sim field textures allocated when no Media content exists.

**Files.** `src/main.cpp`, `src/sim/PaintSim.{hpp,cpp}`, `src/app/SelfTest.cpp`, new
`src/app/Memory.{hpp,cpp}`

**Verify.** `--selftest` passes the two new assertions. Launch and confirm RSS with no
painting. Switch modes and confirm the other modes' fields are not resident.

> ⚠️ **`bindCache_` must be cleared whenever textures are recreated.**
> `PaintSim.hpp:248` caches bind groups keyed by `uint64_t`. Freeing and reallocating
> fields on a mode switch invalidates every cached group, and a stale handle here will
> present as corrupt output rather than as a clean crash.

## 1.5 — Verify the Mixbox seam actually exists

**What.** ADR-0006 relies on `NP_USE_MIXBOX=OFF` as the escape hatch from a CC BY-NC
dependency. That option may not have been exercised in a long time. **An unexercised
build option is not a seam** — and discovering it has rotted at the point you need it
is the worst possible timing.

```bash
cmake -S . -B build-nomix -DNP_USE_MIXBOX=OFF && cmake --build build-nomix -j
./build-nomix/src/naturalPaint --selftest out-nomix.png
```

The selftest asserts blue crossing yellow gives green, so it is a real test of the
`km2` fallback rather than a compile check. Fix whatever has rotted; keep both
configurations building from here on.

**Files.** whatever the build reveals — likely `shaders/include/mixbox.wgsl` guards and
`src/paint/Palette.cpp`

**Verify.** Both configurations build; both pass `--selftest`. Record the visual
difference between the two mixing models — that difference *is* what the Mixbox licence
is being accepted for, and it is worth knowing its size.

> ⚠️ **Deferred, 2026-08-18.** Checking the seam before writing this step's fix found
> there is no `km2` fallback to fix — `NP_USE_MIXBOX` is defined but never consumed
> anywhere in `src/` or `shaders/`. This step is not a verification pass, it's an
> unscoped color-science implementation (see the correction in
> [ADR-0006](docs/adr/0006-mixbox-now-reimplement-from-the-paper-later.md)). Deferred
> out of phase 1 by explicit decision; not blocking 1.1–1.4. Revisit as its own scoped
> task, sized like an implementation phase rather than a plumbing step.

## Phase 1 exit criteria

| | target | status |
|---|---|---|
| `--selftest` idle RSS | < 80 MB | ✅ 63.1 MB (1.4) |
| sim field textures when idle | 0 | ✅ (1.4 — `PaintSim` doesn't exist until first stroke) |
| watercolour resident | ~193 MB | inherited from ADR-0001's original measurement; not re-measured this round, `allocFields()` itself unchanged |
| `--diag` run-to-run | identical | ✅ byte-identical after every phase-1 step (1.2, 1.3, 1.4) |
| pigment mass after brush lift | flat | ✅ (`--diag 20`) |
| equal-mass-at-different-speeds test | passes | ✅ 0.0% diff, 6 vs. 240 samples (1.3) |
| Known Bug #2 | closed | ✅ (1.3 / ADR-0003) |
| latency p50 | recorded | ✅ p50 12.1–12.4 ms, p99 15.7–16.4 ms (1.1) |
| `NP_USE_MIXBOX=OFF` builds and passes `--selftest` | yes | ❌ **deferred** — no `km2` fallback exists to build (see 1.5 above); not blocking |

Phase 1 is otherwise complete. 1.2–1.4 each found and fixed a real bug or false
assumption that a straight read of the plan/ADRs would have missed or perpetuated
(the `dt`-halving in old `kSplat`, oil's genuine need for a segment not a point, and
`setMode()` never freeing the outgoing medium) — recorded in Findings below, not
repeated here.

---

# Phase 2 — See a file

**Goal.** Open a PNG, pan and zoom it, probe pixel values in linear and display
encodings. Proves the tile path and the colour policy before anything depends on them.

1. **`core/Tile.hpp`** — `kTileSize = 128` as `constexpr`, tile coordinate type,
   document↔tile coordinate conversion.
2. **`core/TileStore`** — sparse hash map from tile coordinate to tile. Allocate on
   write, query without allocating, iterate occupied tiles. No COW yet.
3. **`color/Space`** — sRGB and Rec.709 transfer functions both directions, primaries
   as data, `Document`-level working-space policy. Not hard-coded into shaders.
4. **`core/Document` + `core/Layer`** — layer list holding exactly one entry, with the
   `kind` enum from `CONTEXT.md` present but only `RGB` implemented. **Design for N,
   ship 1.**
5. **`Document::createBlank(w, h, space)`** — PRD C7. Nothing else in the plan creates
   a document that did not come from a file.
6. **`io/ImageIO` + `io/StbBackend`** — read PNG/JPEG/TGA/BMP, decode to linear
   `rgba16float`, write into tiles.
7. **Reserve the selection seam.** Deposit and op interfaces take an optional
   selection mask parameter *now*, even though nothing populates it until phase 7.
   PRD E1 — retrofitting this later touches every path.
8. **Tiled viewport draw**, retaining the pan/zoom logic from the outgoing
   `MacPaintUI` — that part is reusable even though the chrome is replaced. New UI
   direction and layout in [docs/ui.md](docs/ui.md).
9. **Mip pyramid** for tiles, so a 25 % zoom evaluates at a matching level.
10. **Pixel probe** reporting both linear and display values, and the **eyedropper**,
    which is the same sampling code writing to the foreground colour instead of a readout
    (PRD Q10). Sample size and sample-all-layers are parameters of the sample, not
    separate tools.
11. **View controls** — fit, 100 %, zoom to selection, cursor-anchored zoom, **mirror view
    L/R and U/D** as independent toggles, **grayscale preview**, rotate view (PRD Q1–Q4).
    All of the last four are *view* state: they never touch the document, and `--selftest`
    should assert that saving with either mirror axis on produces an unmirrored file.
    Build these as one **view matrix** with mirror as sign flips — then both axes, rotation
    and zoom compose in one place, and pen input maps back through its inverse. Doing
    mirror as a special case in the draw path is what makes painting-under-mirror land in
    the wrong spot.
12. **Rulers, guides, grid and snapping** (PRD Q5–Q7). Guides land here rather than later
    because the offset-by-half tiling workflow in phase 9 depends on them and retrofitting
    snapping into an existing viewport is fiddlier than building it in.
13. **Place an image as a layer** into the open document, by menu and by drag-drop
    (PRD I14) — distinct from opening a file, which creates a document.
14. **The base layer is an ordinary layer with alpha** (PRD C16). No locked Background.
15. **`app/Keymap`** — bindings loaded from a data file, not `if (key == ...)` scattered
    through the UI. Actions are named; the file maps keys to names; **conflicts are detected
    at load and reported**, including conflicts that only exist within one layer-kind scope
    (PRD R7, R8). Default keymap and the reasoning behind it:
    [docs/shortcuts.md](docs/shortcuts.md).

> **Build the keymap as data in phase 2, before there are tools to bind.** Every tool added
> from here registers a named action, so the keymap grows by a table row rather than a code
> change — and the conflict detector is what stops the collisions in
> [docs/shortcuts.md §5](docs/shortcuts.md) from reappearing as tools arrive across fifteen
> phases. Retrofitting this after phase 10 means unpicking hardcoded keys from every tool.

> ⚠️ **`⌃⌥`-drag for brush size and hardness is a phase 10 requirement, not a nicety**
> (PRD R5). `[` and `]` are unreachable while holding a pen in the right hand, so the
> on-canvas gesture is the primary path. Reserve the chord here so nothing else claims it.

**Verify.** Open an 8-bit PNG and a 16-bit PNG; probe known values; confirm memory
tracks occupied tiles rather than canvas dimensions (PRD C2). Mirror both axes, save,
reopen — the file is unmirrored. Paint a stroke with a mirror on and confirm it lands under
the cursor.

---

# Phase 3 — Grade it

**Goal.** Live levels, curves, saturation, grayscale with correct linear math and a
curve widget that behaves.

1. **`color/Shaper`** — 1-D log encode/decode. ADR-0004 notes this is a *format-level*
   commitment: saved curve control points are coordinates in this domain.
2. **`ops/PointOps`** — levels, curves, exposure, saturation, RGB→grayscale, channel
   mixer, as plain functions. These are what the LUT baker consumes, so they are
   written once and reused.
3. **Un-premultiply / re-premultiply** wrapped around the point-op path, not left to
   callers (PRD B4).
4. **`color/LutBake`** — bake a maximal run of adjacent point ops onto a 32³ grid via
   one compute dispatch. Rebake on parameter change.
5. **`core/OpStack`** — ordered ops, dirty tracking, run detection for the collapse.
6. **Apply pass** — shaper → 3-D LUT fetch → un-shape.
7. **Histogram** over the visible region.
8. **Op-stack UI** — reorder, toggle, delete, and a curve widget operating in the
   shaper domain.

**Verify.** `--selftest` round trip: decode → encode returns the original within
tolerance (PRD B3). Twelve stacked grade ops cost the same as one at draw time.

---

# Phase 4 — Write it out

**Goal.** A complete open → grade → save loop.

1. **Export path** — encode from working space to a chosen target space and bit depth,
   explicitly, never silently (PRD B6, I5).
2. **`io/OiioBackend` behind `NP_USE_OIIO`** — EXR, TIFF, HDR, DPX, flattened PSD,
   camera raw.
3. **Capability query** — format support is discovered at runtime; the core builds and
   runs without OIIO (PRD I3).

   > ⚠️ **Landed 2026-08-19, with one item of step 2's list undelivered.** EXR, TIFF,
   > HDR, DPX and flattened-PSD read are in; **camera raw is not**, because this
   > project's OpenImageIO is built without LibRaw on purpose (avoiding LibRaw and its
   > dependency weight). That is a build decision, not a bug, and `NP_USE_OIIO` defaults
   > to **OFF** so a plain `cmake -S . -B build` still works everywhere. The capability
   > query reports camera raw unsupported *in the OIIO build too* — it asks the linked
   > library at run time rather than assuming `NP_USE_OIIO` implies a plugin set, and
   > `--selftest` asserts exactly that. PRD I2 (camera raw, P1) stays open. See Findings.
4. **Native `.npaint` save and load** — multi-part tiled EXR via OIIO. No bespoke writer:
   one part per layer, `HALF` channels, latents as named channels, `np:*` typed
   attributes. Spec: [docs/document-format.md](docs/document-format.md). PSD *read*
   arrives free here too; PSD *export* is phase 15.
5. **Wire OIIO's `ImageCache`** as the residency layer for unmodified source tiles.
   This is the main reason the dependency earns its cost.

   > ⚠️ **Landed 2026-08-19, and this step's own wording is backwards.** `io/TileResidency`
   > serves tiles from the cache bit-identically at 73 µs cold / 5.6 µs warm, taking our own
   > resident cost for a 2048² document from 32.00 MiB to 0.12 MiB — but **only for genuinely
   > tiled sources, which means our own `.npaint`, not "source tiles" in the imported sense.**
   > Every format `io/ImageDecode` reads is scanline-stored, and OIIO gives those no partial
   > residency at any useful cost (measured: one 128² request pulls a whole 16.00 MiB PNG, or
   > 1549 µs/tile if forced to sub-tile, against 49 µs tiled), so untiled sources are refused
   > by name and stay eager. Two limits worth knowing before building on this: a **cold**
   > viewport costs 17.6 ms and does not fit phase 1.1's measured 12.1 ms p50 frame (warm does,
   > at 1.34 ms), and OIIO's `max_memory_MB` is **fiction below ~16 MiB** — every budget from
   > 1 to 8 MiB settled at the same 9.88 MiB floor. At 32 MiB of content against the chosen
   > 64 MiB budget the cache is a wash rather than a win; it converts an O(document) cost into
   > an O(budget) one, and that shape — not this document size — is what justifies the
   > dependency. Nothing reads through the residency yet. See Findings.
6. **Lazy OIIO init** — on first file open, not at startup, so PRD A2 holds.

   > ⚠️ **This step, as written, recovers nothing — measured 2026-08-19.** It assumes
   > OpenImageIO does eager initialisation worth deferring. It does not: OIIO's own init
   > is already lazy, and `io/Capabilities`' first probe call moves RSS by 0.02 MB. The
   > entire cost of enabling `NP_USE_OIIO` is paid by **dyld**, before `main()` runs,
   > mapping and relocating the OIIO/OpenEXR/Imath/OCIO/libtiff/libpng/libjpeg-turbo
   > chain. Measured with a standalone program against this project's own OIIO build:
   > **+29.4 MB resident** (1.03 → 30.45 MB) and **+9.4 ms of process startup**
   > (6.37 → 15.72 ms, mean of 200 runs each, file cache warm). That 9.4 ms is ~9 % of
   > PRD A2's entire 100 ms cold-start budget, spent before a line of this project's code
   > executes. No amount of deferring *initialisation* touches either number — only
   > `dlopen()`-ing OpenImageIO on first use would, which is a linkage-architecture
   > change, not the tuning change this step describes. Re-scope this step to
   > "`dlopen` OIIO on first use" or drop it; do not implement it as written. Note the
   > cost is opt-in regardless: `NP_USE_OIIO` defaults **OFF**, and the OFF binary links
   > no OIIO dylib at all (`otool -L` and `nm -u` both zero).
7. **Export As** — format, space, depth **and resize**, with saveable presets (PRD I15).
   Downscale prefilters; see the phase 6 warning.
8. **Document lifecycle** — revert, duplicate document, save a copy, save incremental,
   open recent (PRD I17).

   > ⚠️ **Landed 2026-08-19, and the requirement number here is stale.** The wording above
   > is **PRD I18**'s, not I17's — I17 is now "Export layer comps to files", which is phase
   > 5. The step is right; the citation is not. New `app/DocumentLifecycle`: `OpenDocument`
   > (document + path + `NpaintCarry` + dirty state + residency mode + identity),
   > `DocumentSession` owning an ordered list of them, and the five operations, with
   > `AppState::documents` making the running application genuinely hold documents for the
   > first time — the ownership question steps 5 and 7 both deferred to here. **The bridge
   > from the live painting canvas to a `Document` is still not built**, and this row does
   > not claim it: File > Open... and New Document put real documents in the session and
   > every operation acts on them, but `sim::PaintSim` still owns one dense texture with no
   > layer awareness, so a stroke reaches no `Layer::rgbTiles` and saving a document writes
   > what was opened rather than what is on screen. See Findings.
9. **`core/Journal`** — the recovery journal from
   [ADR-0008](docs/adr/0008-recovery-needs-a-model-journal-not-tile-spill.md). Serialises
   the document *model* on a timer and after every structural edit, using **the same
   writer as native save** aimed at a scratch file. Dirty tiles flush on the same timer
   **for the active document**, not only on deactivate. Unclean scratch directories are
   offered on launch, named and dated (PRD O5–O10).

   > ⚠️ **Landed 2026-08-19 as `app/Journal`, not `core/Journal`, and PRD O5's list is
   > delivered only as far as the *format* reaches.** The module cannot live in `core/`:
   > it serialises an `app::OpenDocument` through `io/NpaintFile`, and `core/` depends on
   > neither (see Deviations). The load-bearing consequence of PRD O7 — the journal reuses
   > `saveNpaint()` and there is no second writer — is that **anything that writer cannot
   > write, the journal cannot recover**, so masks, dabs, selections, paths and, the one
   > that exists today, **op stacks** are all absent from a recovered document. That makes
   > this phase's own Verify line ("recovered document matches the journal, including
   > layer structure and op stacks") **half met**: structure and pixels yes, op stacks no,
   > unblocked by io/NpaintFile gaining `np:docOps`. Worse for a P0 requirement: the
   > journal is OpenImageIO-only, and `NP_USE_OIIO` defaults **OFF**, so **the default
   > build has no crash recovery at all** — it says so by name at startup and creates no
   > scratch directory. And the canvas gap still bounds everything: a stroke reaches no
   > `Layer::rgbTiles`, so the journal cannot recover what is on screen, only what was
   > opened. Interval measured, not inherited: one write of a 2048² 256-tile document
   > costs 0.080–0.085 s, ~1.4 s extrapolated to an eight-layer 4K painting, giving 2.3 %
   > duty cycle at 60 s. See Findings.

> ⚠️ **The design's claim that crash recovery falls out of the `mmap` tile spill is
> wrong**, which is why step 9 exists. The spill fires on *deactivate*, so the document
> being painted has written nothing; it stores tiles with no layer structure; and a
> document whose value is an op stack and a Strokes layer has almost nothing in it. The
> more non-destructively a user works, the less recovery the old scheme gave them.

**Verify.** Round-trip a 32-bit EXR without precision loss. Confirm cold start is
unchanged with `NP_USE_OIIO=ON` but no file opened. `kill -9` mid-session and confirm the
recovered document matches the journal, including layer structure and op stacks — not just
pixels.

---

# Phase 5 — Stack it

**Goal.** The document model, complete — and the save/load/layers/undo the README has
been missing.

1. **Multiple layers** in `Document`, with reorder, visibility, lock, opacity.
2. **`core/Blend`** — the linear-safe set (over, plus, multiply, screen, min, max) and
   `Mix`, the KM latent lerp. Display-referred modes labelled as such (PRD B7).
3. **Pigment layers** — latent × mass tile storage at f16. Per-layer op stack applies
   *after* the latent→RGB projection, so grading never bakes the latents.
4. **Layer masks** — single-channel tile store, the same machinery.
5. **Adjustment layers** — op stack against the composite below.
6. **COW tiles** — copy-on-write with reference-counted history.
7. **`core/History`** — a **linear list with a cursor**, not a stack: undo moves it back,
   **redo** moves it forward, and a new edit at a non-end cursor truncates the tail. Undo
   bounded in *bytes*, compressed, tail spilled to `mmap` scratch (PRD A9, O1).
   **Snapshots** are explicit entries exempt from eviction (O4). See ADR-0005's amendment
   — redo is not an inverse, it is the same keyframe replay with a longer dab stream, which
   only works if history is a cursor from the start.
8. **History panel** listing entries by originating tool or op; clicking one moves the
   cursor there in a single replay, not N (PRD O2, O3).
9. **Clipping masks** — a layer or group clipped by the alpha of the layer below
   (PRD C9). The UI already assumed this: `ADJUSTMENT · CLIPPED` appears in
   [docs/ui.md](docs/ui.md)'s layer rows.
10. **The merge family** — merge down, merge visible, stamp visible, flatten, and
    rasterise a parametric layer (PRD C10, C11). Distinct from `Bake`, which flattens one
    layer's *op stack* and leaves the layer.
11. **Multi-select, align and distribute, colour labels, linking, panel filtering**
    (PRD C12, C13, C15).
12. **Layer comps** — named sets of visibility, position and properties, restorable in one
    click and **persisted in the document** as an `np:comps` blob on part 0 (PRD C14).
    Cheap here because the layer model is fresh in hand; expensive later.
13. **Export comps to files, and layers to files** — one shared loop: set a document state,
    composite, write through phase 4's Export As presets with a name template (PRD I16,
    I18). Both are the same mechanism, so building them apart would mean building it twice.

> **Comps and comp export are not speculative scope.** Export Layer Comps to Files is part
> of the primary user's existing Photoshop workflow, which is the only evidence that settles
> a priority. It is the reason this pair sits at phase 5 rather than in the automation phase
> — and the reason `np:comps` has to be in the format from the first save that carries
> layers, not added later.
14. **Tabs + optional two-tab split**, with the visible-documents GPU rule from
    ADR-0001's amendment.
15. **Native save/load** carrying layers and latents, with the pigment basis stamped and
    a baked RGB composite embedded (PRD C8, I4).
**Verify.** Twenty open tabs cost kilobytes each. Two visible documents hold GPU
textures; hidden ones hold none. Blue on a Pigment layer over yellow gives green under
`Mix` and translucent blue under `Normal`. Undo ten strokes, redo ten, and the result is
pixel-identical to before the undos. Export four comps and confirm four correct files with
the right names.

---

# Phases 6–19 — Outlines

Re-expand to step level when each becomes next. The operation catalogue these phases
build from — every op, its class and its cost — is
[docs/operations.md](docs/operations.md).

## 6 — Filter and transform it
ROI propagation (`roi(rect) → rect` per op, walked backwards) · hash-keyed tile cache
on `(layer, tile, mip, hashOfOpsBelow)` · Gaussian/box blur through the mip pyramid ·
highpass as `src − blur(src)` · unsharp · offset with wrap · then the rest of the filter
set: sharpen, motion blur, median, dust & scratches, add noise, shadows/highlights,
local contrast.

Transform lands here too, because it is the same class-B machinery: a 3×3 matrix per
layer, **composed before resampling** so a stack of three transforms costs one
generation · Lanczos3 / Mitchell / Catmull-Rom / bilinear / nearest · exact paths for
flips and 90° rotations · crop, canvas size, image size.

Then the solved-for-you variants, which are the photo-prep half: **straighten** (draw a
line that should be level) · **perspective correction** (mark what should be a rectangle
and solve the homography) · **lattice warp** of a selection over a subdividable mesh.
Puppet warp is deferred to future work and shares this mesh, so building the lattice first
is not wasted.

And the tool-shaped ops that need the same fill machinery: **gradient** (linear, radial,
angular, with an editor and presets) · **paint bucket** with tolerance and fill-all-similar
· fill and stroke a selection or layer. These were accepted as scope in
[docs/ui.md §4](docs/ui.md) and then never became requirements — D24–D26 closes that gap.

> ⚠️ Two domain traps. **Add noise runs in the shaper domain** — fixed-amplitude noise
> in linear light is invisible in shadow and enormous in highlight. And **downscale must
> prefilter** (area average or descend the mip pyramid); no reconstruction filter fixes
> aliasing after the fact. Both are the most commonly botched operations in this category.

> Motion blur, radial blur and lens blur **cannot use the mip-pyramid shortcut** — they
> are directional or aperture-shaped, so their cost is the honest one. Lens blur is P2
> and is the only filter here likely to need a progress indicator.

## 7 — Select and paste
Selection coverage mask tile store at `r8unorm` · rectangle and ellipse (analytic,
antialiased) · lasso and polygon lasso (polygon rasterisation) · magic wand as a **CPU**
flood fill paging through the tile store · booleans · feather via phase 6's blur ·
**grow/shrink through a distance transform**, not iterated dilation, so the radius is a
real number and the antialiasing survives · colour and luminance range · invert ·
selection ↔ mask conversion · transform selection · marching ants.

**Quick mask** and **channels** land here too: editing the active selection as a paintable
overlay is nearly free once brushes and coverage masks both exist, and it is how precise
selections actually get made · alpha channels stored in the document · load channel as
selection, save selection as channel · the Channels panel's single-channel view and edit
(PRD E12, E13, Q11).

The clipboard ships here, because coverage-weighted cut is what makes it correct and
selections are what make it useful: cut/copy/paste/clear · copy merged · paste in place ·
paste into selection · selection → new layer · duplicate.

> ⚠️ **The clipboard must not hold a flattened buffer.** A 4K full-document copy is 68 MB
> at `rgba16float`, held invisibly — precisely what ADR-0001 exists to prevent, and the
> reason Photoshop ships a "Purge → Clipboard" command. Hold a copy-on-write tile
> reference plus a coverage mask; materialise only when writing the system pasteboard.
> An *internal* copy-paste takes the internal path and never round-trips the pasteboard,
> or pigment latents are silently lost on every copy.

## 8 — Repair it
`Strokes` layer: dab records, spatial index over dab bounds, checkpoint tiles, and the
**samples-only-from-below** rule · clone with aligned/non-aligned · heal via a
gradient-domain solve · diffusion inpaint (Telea).

## 9 — Tile it
Lighting-gradient removal (divide by a heavily blurred copy, re-centre the mean) ·
offset by half · seam heal · **3×3 repeat preview** · PatchMatch as a cached class-D op
with a Recompute button and a deterministic seed.

**Primary user's workflow is complete here.**

## 10 — Paint on it
`brush/Brush`, `brush/Dynamics` (the `Link` matrix — every dynamic uniform, none
special-cased) · deterministic jitter seeded from `(strokeId, dabIndex)` ·
`brush/StrokeBuffer` (coverage scratch giving the opacity ceiling) · procedural round
and stamp tips · rolling and stamp grain · glaze vs blending accumulation, with the
per-pixel-walks-its-tile's-dab-list trick for blending · pressure/tilt/velocity response
curves · the latency path drawing scratch over last composite · Pigment and RGB deposit ·
smudge.

The arc-length emitter already exists from 1.3.

**The eraser is built here, in the same pass as the deposit** — not as a later tool. It is
the brush with a *signed* deposit step, so it inherits the whole `Link` matrix for free,
and the sign is decided per layer kind: alpha on RGB, **Mass** on Pigment with the
**Latent** untouched, deposit on Media, dab-record deletion on Strokes, mask on the
parametric kinds. Spec:
[ADR-0007](docs/adr/0007-erase-is-mass-reduction-not-a-colour.md) (PRD F9–F11).

**Dodge and burn** are also built here, and are not pixel ops: the brush paints coverage
into an exposure adjustment layer's mask, which makes them non-destructive and adjustable
after the fact (PRD D13).

> ⚠️ **Do not implement erase as painting white.** On a Pigment layer white is an opaque
> pigment under Kubelka-Munk, so "erasing" would deposit paint — lightening the pixel *and*
> leaving something that itself needs erasing. Mass reduction is the only correct form, and
> it is lossy on mixed paint by nature: you get less of the mixed colour, never the
> components back. That is what paint does; the UI must not imply otherwise.

## 11 — Media layers
Solver window tracking the wet region in document space, capped at 1024² (ADR-0002) ·
bake-on-dry into the dry extent · 2–4 windows per layer, oldest baked when a new one is
needed · refuse-to-wet past the cap, *visibly* · Media deposit path · solver keyframes
every ~2 s plus dab replay for undo (ADR-0005) · wet mix exposed as dilution / charge /
attack / pull over the existing reservoir.

## 12 — Import brushes
`io/Descriptor` — Action Descriptor reader (`Objc`/`VlLs`/`UntF`/`doub`/`TEXT`/`enum`),
reusable for PSD later · `io/AbrImport` — 8BIM container, `samp` RLE tip bitmaps,
`desc` tree, `bVTy` → `Source` mapping · `io/BrushIR` neutral representation ·
**import report** naming everything dropped · then `io/BrushImport` — Procreate zip,
`bplist00` reader, NSKeyedArchiver graph resolution.

## 13 — Paths
Bézier path data model · anchor editing, corner ↔ smooth conversion · path →
selection via antialiased coverage rasterisation · **stroke path with brush** — a
Bézier feeding the phase-1 dab emitter, so it is nearly free · fill path · Paths panel
and document persistence.

Selection → path (contour extraction plus curve fitting) is a non-goal.

## 14 — Text
`Text` layer kind — string, font reference and layout parameters, rasterised at
evaluation, so it stays editable and parametric like an Adjustment layer · **CoreText**
for shaping, bidi, cluster breaking and font fallback, behind an interface HarfBuzz +
FreeType could replace · point and paragraph text, alignment, leading.

Text on a path, vertical text and rich-text runs are non-goals.

## 15 — PSD export
Not the save path — native save shipped in phase 4. Flattened PSD first (small), then
simply-layered: one PSD layer per naturalPaint layer, blend modes mapped where they
exist, latents dropped with a warning naming what was lost.

> ⚠️ Photoshop's 16-bit range is **0–32768**, not 0–65535, and its 32-bit mode is IEEE
> float. Never emit native `curv`/`levl` blocks — our curves are in the shaper log domain
> and would be silently wrong. Spec:
> [docs/document-format.md](docs/document-format.md).

---

# Phases 16–18 — Flatting

Absorbing autoFlats. Full accounting — inventory, dispositions, hazards and the
scope note — in [docs/autoflats-migration.md](docs/autoflats-migration.md). A third
independent chain; earliest start is after phase 7, because the lasso and marquee repair
tools *are* selection tools.

## 16 — Flat it
**Port `test/run.ts` and `test/harness.ts` first** (478 lines, with fixtures) — they are
the specification for everything after, and porting them last means porting blind ·
`flats/Ink` (extraction, morphology, Zhang-Suen skeletonisation) · `flats/Segment`
(trapped ball, line-centre expansion, region finalisation, declutter, slivers; GPU growth
from autoFlats' existing WGSL chamfer kernel, CPU Dijkstra fallback) · `flats/Fills`
(fill table, anchors, adjacency graph, graph colouring, palette) · the **Flats layer**
kind with RLE label storage and its `.npaint` part · the Fills panel.

Deliverable: open line art → flat → recolour from a palette → save and reload.

> ⚠️ **The port is not "translate `core/`".** About 800 lines of the actual algorithm —
> `replayEdits`, `assignGroups`, `regionAnchor`, the merge tools, `regionAdjacency`, the
> graph colouring — live inside autoFlats' 2,294-line UI file. Step one is extracting a
> *headless flatting library* from both places. Estimating from `core/` alone understates
> this by ~40%.

> ⚠️ **Ink extraction runs in the display domain.** Every threshold was authored against
> 8-bit sRGB; mid-grey is 0.216 in linear, not 0.5, so running them against the working
> space silently changes what "dark" means. Same rule as curves (ADR-0004).

> ⚠️ **Region colour comes from a point inside the region, never from its id.** Ids get
> reshuffled by any parameter change, and an id-derived palette repaints the whole drawing
> whenever a slider moves — which makes it impossible to see what changed. Anchored to a
> place, a 417→381 fill change keeps 91% of colours. This is a regression test, not a
> comment.

## 17 — Fix it
`flats/Gaps` — collision fronts, stroke-width matching, orientation checks against the
stroke orientation field, Kellman-Shipley relatability, Euler-elastica ranking, closure
and simplicity pruning, Hermite bridge curves · the optional stochastic completion field ·
**edit recording and replay** for all six edit types, extracted per §3 of the migration
doc · the repair tools: merge pair, draw-merge, delete, shape fill, group lasso, marquee
select-edits, bridge draw/erase · auto-bridge · ridge and edit overlays.

Gate: replay is idempotent, and a full parameter sweep preserves every recorded edit.

## 18 — Sheet it, and wash it
Membrane sag by **re-parameterising `shaders/jacobi.wgsl`** with Dirichlet boundaries at
ink — the Poisson solver already exists for pressure projection, which is the largest
single saving in the migration · watershed on the sag field · sag / zebra / ridge
visualisations · expand a Flats layer to real layers (per colour, per fill in colour
groups, merged) and the PSD group export those feed · **wash** = stamp the flats into a
Media layer and run the existing solver.

> **Do not port `watercolor.ts`.** Those 409 lines are a 256 px reduced coffee-ring
> simulation that existed because a browser page had nothing better. naturalPaint owns the
> real Curtis '97 solver. Deleting it removes code *and* improves the feature — and stops
> two implementations of one idea from drifting apart.

---

# Phase 19 — Automate it

**Goal.** Record once, run on forty files. For texture preparation this is the largest
productivity feature in the incumbent, and here it is mostly a file format.

1. **`ops/Action`** — serialise a layer or document op stack to a named, human-readable,
   diffable file. **This is not a new subsystem**: an op stack is already an ordered list of
   parameterised operations with a defined evaluation order, so an action is that list
   written down (PRD P1, P5, P6).
2. **Apply an action** to another document, resolving anything document-specific — layer
   references, resolution-dependent radii — explicitly rather than by index.
3. **`app/Batch`** — run an action over a folder or a chosen file set, with an output rule:
   format, space, depth and resize from I15's presets (PRD P2, P3).
4. **Per-file reporting**, and **never a partial overwrite of an input** (PRD P4).
   A batch that fails on file 12 of 40 leaves files 13–40 untouched and says so.
5. **Lens correction** and **pattern define/fill** land here as the remaining P2 image ops
   (PRD D22, D27).

Export-to-files already shipped in phase 5 (PRD I16, I18) — the batch runner reuses that
loop rather than growing a second one.

**Verify.** Grade one plate, save the stack as an action, run it over thirty photographs,
and confirm every output is correct and no input was modified.

> **This phase's dependencies are satisfied at phase 6.** It sits last because nothing
> depends on it, not because it is low value — for the primary user's stated job it is
> arguably the highest-value phase in the second half of the plan, and moving it earlier is
> a defensible reordering at any time.

> ⚠️ **Better than Photoshop's Actions, for a structural reason.** Photoshop records UI
> events, so an action breaks when a dialog changes or a panel moves. Recording a
> *parametric op stack* has no UI in it at all — which is why P6 can say recording is
> optional: any document you have already graded is a finished action.

---

# UI

The MacPaint chrome is superseded. Direction, layout, design tokens and the four
architecture reconciliations are in [docs/ui.md](docs/ui.md). `src/ui/MacPaintUI.*` and
`src/ui/Theme.*` are **replaced**, not extended; the viewport pan/zoom logic is the only
part carried forward.

Interface work is not a phase — it lands inside phases 2–11 alongside the features it
exposes.

---

# Findings

Recorded as phases complete. Empty until phase 1 runs.

| date | finding |
|---|---|
| 2026-08-19 | **Phase 5 step 3 ("Pigment layers -- latent x mass tile storage at f16. Per-layer op stack applies *after* the latent->RGB projection, so grading never bakes the latents"; PRD C1, C3 (P0), C8, F10, L5): PASS, and the three decisions a reviewer should push on are seven channels rather than eight, `Mix` as a *pair* rather than an accumulator, and what opacity does to one.** New `src/core/Pigment.{hpp,cpp}`; `core/TileStore`, `core/Layer`, `core/Blend`, `core/Composite`, `core/Probe`, `core/LayerOps`, `paint/Palette`, `app/LayerPanel`, `io/NpaintFile`, `app/SelfTest`, `main.cpp`, `src/CMakeLists.txt` and `docs/document-format.md` extended; +2071/-308 lines. **The tile: seven f16 channels, 224 KiB, and the eighth is deliberately absent.** DESIGN-imaging.md §3 says both "7 channels (c0 c1 c2 m, res rgb)" in prose and "pigment, 8ch f16 | 256 KiB" in its table; seven is what the *content* is and what the *format names*, eight is what a GPU upload would want (two `rgba16float` planes, the shape `sim::PaintSim`'s `pigC_`/`pigR_` pair already has). Seven won on three grounds: the part's latent channel count then equals the tile's, so io/NpaintFile's pack/unpack is positional with no skip-the-padding step (a padding word a writer forgets to skip writes garbage into a document and is invisible until someone else's tool opens it); the eighth would hold nothing at **32 KiB per occupied tile, 14%**, paid for a GPU path that does not exist because no stroke reaches a `Layer`; and 224 KiB is under the design's own 256 KiB row anyway. Measured 224 KiB vs. core::Tile's 128 KiB, **1.75x**, static_asserted. **The six/seven/eight reconciliation, stated once because three quantities get confused:** `Latent` holds **six** floats (`c0..c2`, `res.R/G/B`); Mixbox's latent has **seven** components, the fourth pigment weight `c3 = 1-(c0+c1+c2)` being derived and never stored (step 2 proved it lerps by the same `t`, so six really is seven); the format names **seven** channels, which are those six **plus mass** -- not part of the latent at all, but the Pigment analogue of alpha, the quantity PRD F10's eraser reduces. `c3` is not one of the seven. **`TileStore` became a template and every existing use site compiles unchanged**, because the template is `TileStoreOf<T>` and `TileStore` is an alias of its `Tile` instantiation -- a class template *named* `TileStore` would have forced `TileStore<Tile>` on io/TileResidency, io/NpaintFile, io/ImageIO, core/Probe, core/Histogram, core/Layer and app/DocumentLifecycle for no reader benefit. Step 4's mask store is the next instantiation. `Layer` gained `optional<PigmentTileStore> pigmentTiles` -- **exactly the extension core/Layer.hpp predicted in advance** ("a second, similarly-optional member ... not a change to this field's type"), not a `std::variant`, for the reason core/OpStack.hpp already gives for `Op`. **Step 2's recorded regret is paid off: `Latent` moved into `core/`, and the `core/` -> `paint/` edge is gone.** It was only possible because of a fact worth stating -- **the latent->RGB projection needs no LUT**: `latentToRgb()` was a `MixboxLut` member that never read `data_`, so it is now a free function in core/Pigment beside the 20-term polynomial (moved verbatim, coefficient for coefficient, because the round trip's exactness depends on `rgbToLatent()` subtracting the *same* polynomial `latentToRgb()` adds back -- measured **0.000e+00**). `paint/Palette` keeps only the inverse and now includes `core/Pigment.hpp`, which is the allowed direction. **The ownership decision Phase 4 refused to guess at: the *layer* owns the op stack.** `Layer::ops` is a `core::OpStack`; `app::AppState::opStack` stays put and keeps meaning the global GPU-previewed grade. The walk projects (latent, mass) to premultiplied RGBA and *then* runs the stack, so a grade never touches `c0..c2` -- asserted by `memcmp` of the tile's raw half words, not argued -- and the reason is DESIGN-imaging.md §3's document invariant (levels/curves/LUTs are in the "must bake to RGB first" column; a curve on a pigment weight is a different pigment, irreversibly). It applies to RGB layers identically, because the design's Layer diagram gives every layer an `ops`. **An empty stack is skipped outright rather than run as an identity**, which is correctness and not tuning: `applyPointOpsPremultiplied()` divides by alpha and multiplies back, so step 1's zero-tolerance byte-identity boundary would break -- the measured `composite vs. flattenDocumentToLinear` residual is **unchanged at 2.148e-04**. **`Mix` is a pair, and chains are refused by name.** PRD L5 defines `Mix` between a layer and the Pigment layer *directly beneath*, so `mixPairing()` pairs greedily from the bottom and core/Composite composites a pair **as one unit** (the lower layer is not composited separately -- the mix replaces both). `t` is the **upper layer's mass**. The rejected alternative was a canvas-sized latent accumulator so a mixed result could itself be mixed: **7 floats/pixel = 117 MiB for a 2048² document**, plus an unanswerable question about what a mid-chain opacity fade (which happens in projected RGB) does to a state that must stay latent. The pair costs **zero extra memory** -- both tiles are read directly. Three Pigment layers with the top two on `mix` therefore pair (0,1) and warn by name about layer 2, the same "composited as `over`, warned, never silently, never refused" contract step 1 established. **Opacity is transparency on a mixed pair, and the near-miss is the interesting part.** The obvious fade -- scale the mixing weight `t` by opacity -- *turns opacity into mass*: it would change the mixture's hue rather than let the backdrop through. So `t` is untouched and the fade is on the projected RGBA, as `P = covLow*covUp*Pmix + covLow*(1-covUp)*Plow + (1-covLow)*covUp*Pup` -- each layer participating or not, weighted by its own coverage. That is not invented for `Mix`: `lerp(dst, over(src,dst), o)` and `over(o*src, dst)` are the **same value** (asserted at exactly 0 residual across five opacities), so it is what opacity already does wherever both forms exist. All three corners asserted: opacity 0 composites **byte-identically to the layer being deleted**; hiding the *lower* layer leaves the mixing one visible and unmixed rather than blanking the pair; both at 1 gives the full KM mix. Stored `pig.m` proven bit-identical across every one, and half the opacity measured **measurably different** from half the mass -- (0.592, 0.754, 0.120) against (0.469, 0.766, 0.142). **PLAN.md's own Phase 5 verify sentence is a first-class assertion with printed values, and the numbers match step 2's to the third decimal.** Blue at mass 0.5 on a Pigment layer over yellow at mass 1.0: **`Mix` -> (0.188, 0.583, 0.239)**, green the largest channel; **`Normal` -> (0.498, 0.527, 0.261)**, which is *exactly* the naive RGB lerp (asserted against it) -- PRD §2's muddy answer, arrived at by the compositor rather than by hand. Both composite to alpha exactly 1.0. Mass 0.5 is not a tuning choice: at mass 1.0 the right answer under `Mix` really is blue, because opaque paint covers, and DESIGN-imaging.md's worked example is "blue-at-50%-over-yellow". The mixed colour is additionally checked against `latentToRgb(mixLatents(low, up, up.mass))`, which is what would catch a wrong mixing weight where the green test would not. **`compositesPixels` did NOT become true for `Mix`, and a second field was added instead.** `blendPixel()` takes two RGBA texels and an RGBA texel has no latent -- that is permanent, not pending -- so `compositesLatents` is the new field and `blendIsImplemented()` is their `||`. The *contextual* question core/Composite actually asks is `blendIsImplementedForLayer()`. **core/Probe was converted too**, because a build whose eyedropper knew nothing about Pigment layers would report a different colour from the one it exports; it calls the same `projectPigmentTexel`/`mixedPairTexel`/`gradedPremultiplied`, only the loop differs. `core/Histogram` is still the one place in `core/` that sums layers and now also ignores Pigment ones -- unchanged divergence, still named in its header, still reached by nothing outside `--selftest`. **The format: Pigment parts are written and read, and `np:basis` stopped being inert.** Eleven `HALF` channels per Pigment part (`R G B A` + the seven), the seven moved as raw half words so the zero-tolerance fidelity claim covers them, `R G B A` a baked projection **ignored on read** exactly as part 0 is, and deliberately *ungraded* (baking a grade the file cannot carry would put a look in the document that reloading could not reproduce). **The reader matches channels by name**, and the measurement behind that is worth recording: a part written in the format's order **reads back in that order** -- which is *not* OpenEXR's storage (`Imf::ChannelList` is a name-sorted map putting `res.B` before `res.R`), so it is OpenImageIO normalising per EXR layer name; a positional read would work today by luck and swap the residual's red and blue the day that changed. A document with Pigment layers whose carried `np:basis` is not this build's is now **refused** (docs/document-format.md §3.3); an RGB-only one still carries a foreign basis through untouched. **`np:ops` is still deferred, and half its blocker is gone**: the ownership question io/NpaintFile.hpp posed is answered, but the *carrier* is not -- array-typed EXR attributes are dropped by this OpenImageIO, so `saveNpaint()` **warns by name** for every layer with a non-empty stack rather than dropping it silently (PRD I11). Unblocked by a base64/hex `string` carrier plus a serialisation for `core::Op`. **New `--selftest pigment layers` section** (`runPigmentLayerTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain after `blendOk`, and confirmed by `[selftest] pigment layers PASS` actually appearing in both runs -- **57 assertions in the OIIO build, 47 in the other**, the difference being the `.npaint` block, where the OFF build asserts io/NpaintFile's own named refusal instead). **Tolerances derived here, not borrowed, and the f16 one is where borrowing would have been wrong:** binary16 has an 11-bit significand, so round-to-nearest gives a **relative** error of at most 2^-11 = **4.883e-04**, with an absolute floor of half a subnormal ulp (2^-25 = 2.980e-08) below 2^-14 -- nearly four orders of magnitude looser than the 1.0e-7 the RGB paths use for a single float division, which would have failed on correct data. Measured worst case for a real Mixbox latent: **1.348e-04**. Dyadic fixtures round-trip **exactly**, so the bound is not the only thing checked. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` -- **35** sections (up from 34), **0 FAIL**, exit 0; `NP_USE_OIIO=OFF` -- **35** sections, **0 FAIL**, exit 0. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** (34 total in each, every one from the SDL3 `_deps/` tree). Idle RSS in **paired alternating runs**, three pairs: ON HEAD 92.1/92.3/92.0 against 92.0/92.2/92.1 (ceiling 112 MB); OFF HEAD 64.1/64.1/63.9 against 63.6/63.7/63.8 (ceiling 80 MB) -- **-0.03 and -0.33 MB**, i.e. nothing, which is the expected answer: `--selftest` returns before `AppState` exists and a document with no Pigment layer allocates not one pigment tile. **The tile memory story changes only where pigment exists, and by a factor: 224 KiB per occupied pigment tile against 128 KiB.** `--diag 20` is byte-identical run-to-run **and byte-identical to HEAD**, and both `--selftest` output PNGs are byte-identical to HEAD's. **An RGB-only document's `.npaint` file was checked rather than assumed, and it is unchanged**: HEAD's binary and this one, each run in its own directory with the round-trip file kept (a temporary local patch to both trees' cleanup, reverted after), produce files differing in **4 bytes** -- and HEAD's own two consecutive runs differ at the **same four offsets**, which are inside OpenImageIO's `DateTime` header string. Masking that one attribute, the two files are **byte-identical, 6216 bytes**. **Five existing output lines changed in each build, each justified individually.** (1) `npaint format`'s "a Pigment layer is refused by index, name and kind" -- cause: a Pigment layer is *saveable* now, so the kind with no representation is Media (it needs per-medium simulation state `core::Layer` has no member for); the fixture moved to a Media layer, the claim it makes is unchanged, and a second assertion was added for the Pigment analogue of the malformed case (`pigmentTiles` absent). (2) `layer stack`'s "a newer build's name, `mix` ... and an empty string are reported unimplemented" -- cause: `mix` left that list by being implemented. (3) `blend modes`' "blendIsImplemented() answers exactly BlendModeInfo::compositesPixels" -- cause: the table has two implementation columns now; the single-source-of-truth property it exists for is unchanged and a new assertion pins that exactly one mode is latent-level and not texel-level. (4) `blend modes`' "`mix` is NOT [implemented]" -- same cause, inverted. (5) `blend modes`' "`Mix` additionally says it does not composite yet" -- cause: the menu marker was a warning about nothing once `Mix` worked in exactly the situation the menu offers it in (the menu filters through the same PRD L5 predicate the pairing uses); the assertion now proves **no** offerable mode carries the marker, and the mechanism survives for `layerRowSubLine()`'s `(!)`, which still marks a newer build's carried blend name. Everything else in both builds is additions only (78 ON / 65 OFF), plus the excluded set: the idle-RSS line, tile residency's `fetch cost:` line, and the journal's four timing/duty-cycle `[measured]` lines. **What was deliberately not built.** The canvas bridge -- `sim::PaintSim` still owns one dense texture and a stroke reaches no `Layer` of any kind, so **a Pigment layer has no painting path**; its content can only come from a `.npaint` or from a test writing texels, and the user chose to proceed knowing this. Chained `Mix` (above). Any UI for the per-layer op stack -- `Layer::ops` is reachable from `core/` and nothing in `ui/` shows it, so a user cannot build one; `ui/MacPaintUI.cpp` was **untouched** this step. `np:ops`/`np:docOps` serialisation (above). A document-level `OpStack` on `core::Document`. Media layers, still refused on save by name. Masks (step 4), adjustment layers (step 5), COW (step 6), history (step 7), groups. `Document::createBlank()` and `makeRgbLayer()` still hand out **RGB** layers, and the reason changed rather than went away: a new Pigment layer would be one a user could add and then not paint on. **What got worse.** `core/Layer.hpp` now includes `core/OpStack.hpp` and `core/Pigment.hpp`, so everything that touches a `Document` -- which is nearly everything -- pulls in `ops/PointOps.hpp` and the tile types; that is a real build-weight increase and it is the price of the ownership decision. `core/Composite.cpp` roughly doubled and its walk now has four branches (consumed, mixed pair, RGB, Pigment) where it had one; not measured, because no `--selftest` timing covers the flattener and inventing one for a step with no performance requirement would be a number nobody would maintain. `mixPairing()` allocates two `vector<bool>` per call and `blendIsImplementedForLayer()` recomputes it, so calling that per layer is O(n²) -- core/Composite avoids it by using the pairing it already has, but the public function is a trap. `io/TileResidency` still assumes 4-channel tiles and `npaintLayerTileSource()` will happily hand it a Pigment layer's 11-channel subimage; nothing reaches that path today, and it is named here rather than guarded, which is a gap. `core/Histogram` ignores Pigment layers on top of already summing rather than compositing. **And what I could not verify:** nothing was exercised through the GUI -- the application was not launched at all, because doing so creates `~/Library/Application Support/naturalPaint/` and this task instructed me not to touch it (confirmed still non-existent). The layers panel compiles and every pure function behind it is asserted headlessly; what is unverified is that a Pigment layer's row and the `Mix` dropdown entry draw correctly on screen. Nor is there any way to *see* a Pigment layer: the canvas renders `sim::PaintSim`'s texture, not a `Document`, so every colour claim in this row is from `--selftest`'s numbers and none of it from a screenshot. |
| 2026-08-19 | **Phase 5 step 2 ("`core/Blend` -- the linear-safe set and `Mix`, the KM latent lerp; display-referred modes labelled as such"; PRD B7, C3, L5): PASS, and the two decisions a reviewer should push on are the enum and what "`Mix` landed" is allowed to mean.** New `src/core/Blend.{hpp,cpp}` (552 lines); `core/Composite`, `core/Probe`, `core/LayerOps`, `app/LayerPanel`, `app/SelfTest`, `ui/MacPaintUI`, `main.cpp` and `src/CMakeLists.txt` extended. **The enum question core/Layer.hpp deferred here is resolved *against* the answer that header predicted, and that is the load-bearing decision.** Layer.hpp expected `blend` to "become that enum plus one name<->enum mapping". It has not: **the enum lives in `core/Blend`, `Layer::blend` stays a `std::string`, and the type safety an enum member would have bought is bought by typing the *setter* instead** (`core::setLayerBlend(doc, i, BlendMode::Multiply)` -- there is deliberately no string-taking overload, which would be the hole the typed setter exists to close). Two reasons, and step 1 supplied the second. (1) PRD I10 at the *value* level is a structural property with a string and machinery with an enum: `np:blend = "linear-burn"` survives load/edit/save byte-for-byte because nothing between io/NpaintFile's reader and its writer parses it into a closed set, whereas an enum needs the loader to detect the unknown name, stash it in a side channel and have the writer prefer the side channel -- three moving parts whose only job is to undo the enum, each a place the guarantee can break. `--selftest` asserts that guarantee, and it must keep being a test of the format rather than of the workaround. (2) The remaining benefit is assignment-site typing, and the only assignment path a UI can take is the setter. The cost is stated rather than hidden: `Layer::blend` can hold a string no mode answers to, and every consumer must decide what to do with one -- there is exactly one answer and core/Composite.hpp still argues it (composited as `over`, reported by name, never silently, never refused). **`compositeOver()` moved verbatim, and that word is load-bearing.** Step 1 wrote it as a standalone two-pixel function specifically so this step could move it; body and signature are unchanged, and `blendPixel(Normal, ...)` short-circuits to it rather than routing through the general form -- because the general separable-blend expression `cb*(1-as) + cs*(1-ab) + ab*cs` is algebraically identical to `cs + cb*(1-as)` and **not** bit-identical, and step 1's regression boundary is asserted at zero tolerance. Asserted against a second transcription of step 1's formula across 441 deliberately non-dyadic alpha pairs. **The premultiplied forms were derived, not transcribed, and the derivation is in the header.** From the standard separable blend `co = cb*(1-as) + cs*(1-ab) + as*ab*B(Cs,Cb)`, `ao = as + ab*(1-as)`, each mode simplifies so that **no division by alpha appears anywhere**: `plus = cs+cb`; `multiply = cs*cb + cs*(1-ab) + cb*(1-as)` (the alphas cancel exactly in the third term); `screen = cs+cb-cs*cb` (the premultiplied form really is the straight one -- a coincidence worth writing down precisely because it is what gets assumed for multiply, where it is false and would lose both layers wherever the other does not cover); `min/max = min-or-max(ab*cs, as*cb) + cs*(1-ab) + cb*(1-as)`, valid because `as*ab >= 0` preserves the order inside min. Two properties fall out and are asserted for **every** mode rather than argued: **alpha is `over`'s under every mode** (the formula's `ao` does not mention `B` at all -- a blend mode changes colour, not coverage; checked across a 5x5 alpha grid at exact equality), and **a fully transparent source is a bit-exact identity on the backdrop**, as is a transparent backdrop on the source, which is what keeps step 1's non-overlap identity true whatever a layer's blend is. The partial-alpha references are hand-computed **twice by different routes** -- the premultiplied algebra, and the three Porter-Duff coverage regions summed independently -- because nothing but the arithmetic tells you the naive transcription of multiply is wrong; the test additionally asserts multiply's answer differs from `cs*cb`, so it could not pass against the wrong implementation. **PRD B7's label is a field, and it labels something real.** `BlendModeInfo::space` is a required member of an aggregate-initialised table, so a mode cannot be added without classifying it, and `app/LayerPanel::blendMenuEntryText()` -- the only function that turns a mode into menu text -- derives the marker from that field on every call, so there is no path from a mode to a menu entry that skips it. The row sub-line carries it too, because a label that only appeared while the dropdown happened to be open would not be "labelled as such". **The classification departs from this file's own wording and a reviewer may disagree**: PLAN.md calls all six "the linear-safe set", and **screen is labelled display-referred**. The criterion is stated and tested rather than asserted -- a mode is scene-linear when it is monotone non-decreasing in each operand over the whole non-negative range a linear space can hold, i.e. has no built-in reference white. `screen(2,2) = 0` and `screen(2,3) = -1` **exactly**: above 1.0 it is not merely inaccurate, it is anti-monotone -- adding light makes it darker -- because it is `1-(1-cs)(1-cb)` and only means anything if 1.0 is white. Every other implemented mode is asserted monotone at the same HDR values, so the classification separates the set instead of labelling everything. The alternative was inventing an overlay/soft-light family this step was never scoped to add, purely so B7's mechanism had a subject; that would have been dead code with a fake exercise. Nothing clamps -- clamping is io/Export's decision at quantization (color/Space.hpp). **What `Mix` means here, stated narrowly, because PRD C3 is P0 and this step did not close it.** `core::Layer` has no latent storage: `rgbTiles` is 4-channel rgba16float and Pigment/Media need the 7-channel tile PLAN.md step 3 owns. So **`Mix`'s pure function landed and its layer wiring did not.** `mixLatents()` is implemented against `np::Latent` -- the representation that already exists and is verified -- and tested against the **real Mixbox LUT and the shipped palette**: yellow mixed with blue at t=0.5 gives KM **(0.189, 0.583, 0.239)** against the naive RGB lerp's **(0.498, 0.527, 0.261)**, i.e. green with the red channel at 38% of the lerp's, which is PLAN.md's own Phase 5 verify sentence and the reason the Mixbox licence is being accepted. Endpoints are exact (`std::lerp`), the **implied** fourth pigment weight is proven to lerp by the same `t` so six floats is *exactly* Mixbox's seven rather than an approximation of it, and the projection round trip measures 0.000e+00 -- exact by construction, since `rgbToLatent()` defines the residual as `rgb - evalPolynomial(c)` and every bit of LUT error lands there. **Synthesising latents from RGB to have something to blend was rejected**: `rgbToLatent()`'s decomposition is "plausible rather than true" (docs/ui.md 3.3), two RGB layers carry no record of what pigments made them, and the result would be confident wrong colour that looked like the feature working -- the shape of thing step 4's deferral list refused. **The unblocking condition is one thing: a tile that stores a latent.** At the layer level `mix` is therefore `compositesPixels = false` and gets its **own** warning sentence, distinct from an unknown name's, because the two facts differ -- an unknown name means this build is behind the *document*, `mix` means it is behind *PLAN.md* and has a dated fix. In practice that warning is unreachable through the UI: PRD L5 restricts `Mix` to a Pigment layer over a Pigment layer, and neither holds a texel, so a `Mix` layer never reaches the arithmetic. It is reachable from a file, and is tested that way. **PRD L5 is one predicate, and it is enforced in the model, not only in the dropdown.** `blendModeAvailableForLayer()` is the single implementation; `blendMenuForLayer()` filters through it and `core::setLayerBlend()` **refuses** through it, so "the dropdown never offers it" is not the only thing making L5 true. Tested from four directions (RGB-over-RGB: six modes, no `Mix`; Pigment-over-Pigment: seven; the **bottom** Pigment layer: no `Mix`, because `layers` is bottom-to-top so "beneath" is `index - 1` and there is nothing under index 0; Pigment-over-RGB: no) plus a reorder that takes a layer out of L5's reach **while it still carries the value** -- the menu stops offering it, the value is preserved not coerced, and `blendMenuSelection()` reports "not in this menu" rather than defaulting to entry 0, which would be a silent lie about what the layer says. **`core/Probe` was converted too**, resolving each layer's mode once per layer before the sample box (a string comparison per texel per layer, in an operation that runs on every pointer move, was the one plausible way to make it slow), so an eyedropper and an export cannot disagree about a `multiply` layer. `core/Histogram` is still the one place in `core/` that sums layers -- unchanged from step 1, still named in its header, still reached by nothing outside `--selftest`. **New `--selftest blend modes` section** (`runBlendTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain after `layerStackOk`, and confirmed by `[selftest] blend modes PASS` actually appearing in both runs -- **68 assertions, identical in both configurations**, since nothing in it touches OIIO). Almost all of it is at **exactly zero tolerance**, and that is a property of the arithmetic rather than a shortcut: no formula in core/Blend contains a division and every fixture value is a short dyadic rational, so each product and sum lands on the float grid exactly. The two derived tolerances are `1.0e-7` for the flattener's un-premultiply (half an ulp at results in [0.25,1) is 2^-25 = 2.98e-8; 3.4x, the same figure and derivation runLayerStackTest() uses) and `5.0e-7` for the latent round trip (`p + (r - p)`, two correctly-rounded operations on magnitudes <= 1, so 2 ulps of 1.0 = 1.19e-7; 4.2x, and the naive 8-bit-quantisation derivation would have bounded the wrong thing and landed three orders of magnitude too loose). Step 1's regression boundary is **re-made for the whole set**: five non-overlapping layers, each with a *different* blend mode, composite byte-identically to a second implementation of the plain sum written inside the test, with one overlapping texel proving the identity is about non-overlap. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` -- **34** sections (up from 33), **0 FAIL**, exit 0; `NP_USE_OIIO=OFF` -- **34** sections, **0 FAIL**, exit 0. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** (34 total in each, every one from the SDL3 `_deps/` tree). Idle RSS in **paired alternating runs**, three pairs: ON HEAD 92.0/92.4/92.3 against 92.2/92.5/92.2 (ceiling 112 MB); OFF HEAD 64.0/64.0/64.0 against 63.8/63.9/63.9 (ceiling 80 MB) -- +0.1 and -0.1 MB, i.e. nothing. `--diag 20` is byte-identical run-to-run **and byte-identical to HEAD** (PRD H7 untouched), and both `--selftest` output PNGs are byte-identical to HEAD's. **Three existing output lines changed, each justified individually, and one fixture value with them.** (1)+(2) In `layer stack`, `blend: "normal" is the one blend this build implements` and `blend: everything else ... is reported unimplemented` are now `blend: "normal" and the rest of core/Blend's linear set are implemented` and `blend: a newer build's name, \`mix\` ... and an empty string are reported unimplemented`. Cause: this step makes the old wording false by design; the fixtures those two lines guard (composited as `over`, reported, never silently) are unchanged, and the full enumeration moved to the new section. (3) In `npaint format`, `save: the **two** layers whose blend this build cannot composite are named` is now `the **one** layer`. Cause: that fixture carried `np:blend = "multiply"` and `"screen"` to prove both the verbatim I10 carry and the save-boundary warning, and implementing both names would have quietly voided the second claim (no warnings, assertion deleted) and weakened the first (a name the build recognises proves nothing about carrying one it does not). **So the fixture's top layer moved from `"screen"` to `"linear-burn"`, and that needs its own justification**: it is the layer with no tiles, hidden, at opacity 0.0, so it has never contributed a pixel to the composite -- confirmed by `[measured] composite vs. flattenDocumentToLinear: max residual` staying at **2.148e-04** and both output PNGs staying byte-identical. Layer 0 keeps `"multiply"` so the round trip still covers a name that *is* in the set. Everything else in both builds is additions only (82 ON / 76 OFF), plus the known-nondeterministic set: the output filename, four journal timing `[measured]` lines, and the stdout/stderr interleaving artefact where libpng's unbuffered stderr lands mid-`printf` -- re-confirmed nondeterministic here by running HEAD's own binary twelve more times and getting runs with **no split at all** as well as runs split at two different characters. **What was deliberately not built.** Any display-referred *mode* (overlay, soft light, hard light, dodge, burn) -- B7 asks for labelling, not for that family, and adding one to give the mechanism a subject would have been inventing scope; `screen` turned out to be a real subject. `Mix` at the layer level (above). Clamping. Per-layer blend on anything but RGB-kind layers, since no other kind holds a texel. `core/Histogram`'s conversion, still. Groups, masks, adjustment layers, COW, history. **What got worse.** **`core/Blend.hpp` includes `paint/Palette.hpp` for `np::Latent`, which is a new `core/` -> `paint/` dependency** and the one structural thing I am least happy about: `Latent` is a domain value type that happens to live beside the Mixbox LUT, and the alternative -- a second definition of the same six floats in `core/` -- is exactly the "two implementations of `over`" problem step 1 refused. The natural fix is for `Latent` to move to `core/` beside the tile that will store it, at step 3; it was not done here because it reaches `sim/`, `ui/` and `main.cpp` for no benefit this step needs. `core/Composite.hpp` now includes `core/Blend.hpp` purely so its existing callers keep compiling, so everything that composites pulls in one more header. `core/LayerOps.hpp` gained the same include, and `app/LayerPanel.hpp` too, so the enum is now visible almost everywhere. `blendPixel()` is a switch on a hot path with a special case before it; not measured, because no `--selftest` timing covers the flattener and inventing one for a step with no performance requirement would be a number nobody would maintain. `ui/MacPaintUI.cpp` grew another ~30 lines in a file PLAN.md says is **replaced, not extended**. **And what I could not verify:** the blend dropdown was **not exercised through the GUI at all** -- unlike step 1 I did not launch the application, because doing so creates `~/Library/Application Support/naturalPaint/` and this task instructed me not to touch it (it is confirmed still non-existent). The dropdown compiles, and every pure function behind it -- which modes are offered, what each entry reads, which entry is selected, what the setter does with the choice -- is asserted headlessly; what is unverified is that ImGui draws it and that clicking an entry reaches `setLayerBlend()`. |
| 2026-08-19 | **Phase 5 step 1 ("Multiple layers in `Document`, with reorder, visibility, lock, opacity"; PRD C4, C16): PASS, and the load-bearing half of it is that the flattener stopped summing.** New `src/core/Composite.{hpp,cpp}` (`over`), `src/core/LayerOps.{hpp,cpp}` (the operations, and where `locked` gets teeth) and `src/app/LayerPanel.{hpp,cpp}` (the panel's pure logic), 848 lines total; `io/Export`, `io/NpaintFile`, `core/Probe`, `core/Layer`, `core/Document`, `core/Histogram`, `app/DocumentLifecycle`, `ui/MacPaintUI`, `app/SelfTest`, `main.cpp` and `src/CMakeLists.txt` extended. **Compositing was not optional scope, it was the step.** `visible` and `opacity` are properties of how a layer meets the layers under it, so they cannot mean anything without a compositor; and `flattenDocumentToLinear()` -- the single function that turns a `Document` into pixels for export, for Export As, and for part 0's composite on every save (PRD I12) -- was a **plain sum**, documented as provably correct only under "at most one layer holds painted content at a point". Multiple layers is exactly the invariant this step destroys. So the sum is gone, replaced by Porter-Duff `over` in linear light on premultiplied values. **Where `over` lives is a decision a reviewer may disagree with: `core/Composite`, not `io/Export`.** Compositing a Document is a domain operation, not a file-format one, and `core/Probe` needs the same arithmetic to answer "what colour is at this pixel"; putting it in `io/` would have meant `core/` depending on `io/`, or a second `over`. `compositeOver(src, dst)` is deliberately a standalone two-pixel function with no Document, no Layer and no loop in it, **so PLAN.md step 2's `core/Blend` can move it verbatim as the `over` entry of its set** rather than re-deriving the arithmetic out of a compositing walk; the header says so in those words, and says the document walk stays behind. `Layer::blend` stays a `std::string` -- step 2 still owns the enumeration, and nothing here parses it. **The unknown-blend ruling: composited as `over`, warned by name, never silently -- and specifically NOT refused.** Refusing looks more principled and is wrong here for a concrete reason: io/NpaintFile deliberately carries an unrecognised `np:blend` through a load/save untouched (PRD I10, which is core/Layer.hpp's entire argument for the member being a string), and the loader already tells the user a newer build's document "was opened anyway". Since part 0 is regenerated on **every** save, a compositor that refused would make that carefully preserved value the thing that renders the document unsaveable. A preserved attribute that bricks the file it was preserved in is worse than an approximate composite that says it is approximate. So every boundary that produces a durable artefact reports it: `NpaintSaveResult::warnings` (whose own doc comment is exactly this case) and a **new** `ExportResult::warnings`, carried on a refusal as well as on success -- fixing a depth refusal must not be how the approximation gets discovered. A `probePixel()` returns a colour and has nowhere to put a warning; that asymmetry is stated in `ProbeParams` rather than left to be found. **Ordering, stated once and tested rather than assumed.** `Document::layers` is bottom-to-top -- fixed by docs/document-format.md ("Part order is layer order, bottom to top, after part 0") and by io/NpaintFile writing `layers[0]` as the first layer part, not chosen here. `core/Composite` walks index 0 first. The panel shows the top layer first, so **exactly one function in the codebase reverses an index** (`app/LayerPanel::layerIndexForPanelRow`, with its inverse beside it and an out-of-range guard, because an unchecked `count - 1 - row` wrapping through unsigned subtraction is precisely how "Up" ends up moving a layer down). `--selftest` reorders a three-layer document, saves it, reloads it, and asserts the order came back **and** that the reloaded document composites byte-identically to the saved one. **Layer operations are free functions in `core/LayerOps`, not methods on `Document`.** `Document` is a plain aggregate with a public `layers` vector, and io/ImageIO and io/NpaintFile already mutate it directly and are right to. A `removeLayer()` *method* that refused a locked layer, sitting next to a vector any caller can `erase()`, would read like an enforced invariant while being none -- the worst of both. The honest claim is narrow: **`locked` is enforced by these operations, not by `Layer`**, and the layers panel goes through them for every change, which is what makes the lock real where a user can reach it. Each returns a `LayerOpResult` carrying the `editLabel` an app-level caller should record, because `core/` may not depend on `app/`; `app::recordLayerEdit(od, addLayer(od.document, ...))` is the single bridge that turns any of them into a **`EditKind::Structural`** edit on an open document -- one function rather than one wrapper per operation, and structural always, because every one of them changes the shape or metadata of the stack, which is what ADR-0008 wants journalled at once rather than a minute later. A refused operation records nothing: no revision bump, no label, no journal write. **What `locked` means, given there is no edit path to a layer at all.** A locked layer's content and its own place in the stack are frozen; only its visibility and the lock itself still change. Refused: remove, move-itself, set-opacity, rename. Allowed and each argued in the header: hide (every editor with a lock keeps the eye icon live), unlock (a lock that cannot be removed is a bug), duplicate (it reads the source and changes nothing -- and the copy **inherits** the lock, so duplication is not a one-step laundering), and moving a *different* layer past it (locking one layer must not freeze the document). This is the whole of what the lock can honestly mean today and the header says so: `sim::PaintSim` owns one dense texture, a stroke reaches no `Layer::rgbTiles`, so "the brush refuses" is not available to implement, and faking a refusal path to demonstrate the lock would be worse than a narrow real one. **`core/Probe` was converted too; `core/Histogram` deliberately was not.** Both carried the same note -- "the moment a second layer can hold content at the same pixel, this loop is exactly where real compositing has to replace the plain sum". Probe now composites **per texel and averages the box afterwards** (the other order is a different and wrong operation, since `over` is not linear in its operands) through the same `compositeOver()`, so an eyedropper and an export can never disagree. Its `sampleAllLayers` honours visible/opacity; its single-layer mode deliberately does **not**, because that mode asks "what is on this layer" -- a hidden layer must stay probeable, which is Photoshop's own split. Histogram still bins each layer's texels independently, so a doubly-covered pixel contributes two samples and neither flag is honoured. Left because its whole shape is "stream each layer's allocated tiles", and compositing means binning a *materialised* region instead -- a different algorithm with a different memory profile -- plus a semantics question this step cannot answer (does a histogram of a stack count uncovered pixels as transparent-black samples?) for a panel that does not exist. Exposure today is nil: nothing outside `--selftest` calls it. **Recorded in the header as a named divergence, and counted below as something that got worse.** **One existing assertion's output changed, and one measurement, both in the ON build only; nothing else did.** (1) `npaint format`'s assertion "save: and reports nothing approximate about the result" is now "save: the two layers whose blend this build cannot composite are named, with their blend, rather than silently composited as over". Cause: that section's fixture deliberately carries `np:blend = "multiply"` and `"screen"` to prove the value round-trips verbatim, so part 0 -- now a real composite -- is genuinely an approximation of those two layers, and the save says so. Asserting the warnings' *content* rather than their absence is what makes "never silently" checkable. (2) `[measured] composite vs. flattenDocumentToLinear: max residual` moved **1.628e-04 -> 2.148e-04** (bound 4.895e-04, unchanged). Cause: that fixture's layer 0 sits at `np:opacity 0.72`, which the flattener now honours, so the composite chain gained two float roundings per channel (the opacity multiply and `over`'s multiply-add) on top of the dominant float-to-half stage; the tolerance derivation comment was updated from `2^-11 + 3*2^-24` to `2^-11 + 5*2^-24` and the number is still 2.3x under the bound. **One fixture was changed and it needs its own justification**: that section had `visible = false` on the *middle* layer, which owns half of the deliberate two-layers-at-one-pixel overlap the composite tolerance exists to bound -- once a hidden layer contributes nothing, keeping the `false` there would have silently voided the measurement, and separately would have left the document with **no** pixel at composited alpha 1.0, making the companion "exactly zero at alpha in {0,1}" assertion vacuous (layer 0's opaque texels now land at alpha 0.72). The `false` moved to the top layer, which has no tiles and already contributes nothing at opacity 0.0, at no cost to either property; the `np:visible` round-trip assertion still covers one false and two trues and its printed text is unchanged. **Everything else in both builds is additions only.** **New `--selftest layer stack` section** (`runLayerStackTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain after `recoveryJournalOk`, and confirmed by its `[selftest] layer stack PASS` line actually appearing in both runs -- **85 assertions in the OIIO build, 76 in the other**, the difference being the `.npaint` round trip, where the OFF build asserts io/NpaintFile's own named refusal instead). Most of it asserts at **exactly zero tolerance**, which is the point rather than a shortcut: every fixture value is a dyadic rational exact in binary16, so the whole premultiplied chain is exact and "contributes exactly nothing" can be a `memcmp`. `over` is checked against a hand-computed reference with the arithmetic written out -- two 50%-alpha layers giving premultiplied (0.25, 0, 0.5, 0.75), straight (1/3, 0, 2/3, 0.75) -- **plus an explicit assertion that it is not the plain sum's opaque purple**, so the fixture could not pass against the code it replaced. Opacity is proven a coverage multiplier and not an alpha replacement in both directions (50% opacity on an opaque layer is bit-identical to alpha 0.5; alpha 0.5 *at* 50% opacity composites to exactly 0.25), with the tile and the `Layer` proven unmutated afterwards. A hidden layer is compared against the same document with that layer **deleted** -- because "hidden" has to mean "as if it were not there" -- over 200x200x4 floats at zero tolerance, with a negative control proving the visible version genuinely differs. The one derived tolerance is 1.0e-7 for the flattener's final un-premultiply, a single correctly-rounded division whose half-ulp bound at results in [0.25,1) is 2^-25 = 2.98e-8; landed 3.4x the bound, matching runExportTest()'s and runNpaintFormatTest()'s "a small multiple of the bound, never of the measurement" discipline. **The regression boundary is asserted in the suite, not just by diffing.** Against a **second, independent implementation of the plain sum written inside the test** (the same discipline the journal section's own second FNV-1a follows), a single-layer document and a three-layer document whose layers never cover the same pixel composite **byte-identically** -- fixtures deliberately awkward: translucent texels, several tiles, off-canvas content, and values not exactly representable in half so a one-ulp difference anywhere would show. One overlapping texel is then asserted to break it, so the identity is a property of non-overlap rather than of the implementation. The reason it holds exactly: `over` with `src.a == 0` multiplies the accumulator by literal `1.0f`, and an all-zero accumulator passes the first contributor through untouched. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` -- **33** sections (up from 32), **0 FAIL**, exit 0, idle RSS **92.4-92.5 MB** across three runs against HEAD's 92.0-92.1 in the same session (ceiling 112 MB); `NP_USE_OIIO=OFF` -- **33** sections, **0 FAIL**, exit 0, idle RSS **64.0-64.7 MB** against HEAD's 63.9-64.3 (ceiling 80 MB). The ~0.4 MB ON increase is binary size, not allocation -- `--selftest` returns before `AppState` exists, and nothing here allocates at rest. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** (34 total in each, every one from the SDL3 `_deps/` tree). Regression-checked against HEAD's own binaries, built into `/tmp/np-head-{on,off}` and left there: excluding only the idle-RSS line and tile residency's `fetch cost:` line, the **OFF** build is 79 added / 2 changed (the output filename and one 0.012 -> 0.013 ms discovery timing) and the **ON** build 93 added / 7 changed -- the filename, the two justified above, three timing lines, and one stdout/stderr interleaving artefact where libpng's unbuffered stderr lands mid-`printf`, which was confirmed nondeterministic by running HEAD's own binary three more times and getting a third split point. Three runs of each new binary are identical to each other under the same exclusions plus `[measured]`. `--diag 20` is byte-identical run-to-run **and byte-identical to HEAD** (PRD H7 untouched), and the `--selftest` output PNG is byte-identical to HEAD's. **The canvas gap is unchanged, and a layers panel is the worst possible place to be vague about it.** `sim::PaintSim` still owns one dense texture with no layer awareness; a stroke writes that texture and touches no `Layer::rgbTiles`. So the panel lists the layers of the **open document** -- what File > New Document and File > Open... put there -- and a document that has been painted on does not show the paint, because the paint never reached it. The panel says this on screen (and in a tooltip) rather than leaving it to be inferred, and **there are no thumbnails**, deliberately: a thumbnail of a document the canvas cannot reach would imply a connection that does not exist. The user chose to proceed with Phase 5 knowing this; no bridge was built. **What was deliberately not built.** Any blend but `over` (step 2). A blend dropdown in the panel -- offering a list today would invent step 2's enumeration in the UI, which is exactly what `std::string blend` refuses to do in the model. Groups: `np:parent` is still carried and still never acted on, because honouring it means compositing members offscreen first, which is step 9's machinery. Masks (step 4), adjustment layers (step 5), COW tiles (step 6), history (step 7), multi-select (step 11), merges (step 10). Layer thumbnails, drag-to-reorder, and keyboard shortcuts for any of it. `core/Histogram`'s conversion (above). No `setLayerBlend()`. **What got worse.** `core/Histogram` is now the one place in `core/` that still sums layers, which is a real inconsistency inside a single directory even though nothing reaches it -- named in its header, not hidden. `ExportResult` gained a field, a shipped struct changing; `exportDocumentToFile()` still takes only an `errorOut` and therefore **discards** the blend warnings, which is a gap I did not close because widening that signature reaches into io/ExportAs' own result plumbing. `ui/MacPaintUI.cpp` grew ~170 lines in a file PLAN.md says is **replaced, not extended** -- deliberate, since a lock nobody can reach is not a lock, but it is more to throw away. `app/DocumentLifecycle.hpp` now includes `core/LayerOps.hpp`, so everything that reaches `AppState` pulls in one more header. Compositing costs more than summing per texel (a subtract, three multiply-adds and a branch instead of four adds); not measured, because no `--selftest` timing covers the flattener and inventing one for a step that has no performance requirement would be a number nobody would maintain. **And one thing I could not verify:** the layers panel was smoke-tested only -- the application launches, draws frames and survives with no document open -- but I did not exercise add/reorder/lock/opacity **through the GUI**, only through the same `core/LayerOps` calls the GUI makes. The `~/Library/Application Support/naturalPaint/` directory that smoke test created was removed afterwards and is confirmed non-existent again, as it was before. |
| 2026-08-19 | **Phase 4 step 9 ("`core/Journal` -- the recovery journal from ADR-0008; serialises the document *model* on a timer and after every structural edit, using the same writer as native save; dirty tiles flush on the same timer for the active document; unclean scratch directories offered on launch, named and dated"; PRD O5-O10): PASS for what the model contains today, and the honest headline is that what the model contains is the limit.** New `src/app/Journal.hpp`/`.cpp` compiled in **both** configurations; `app/DocumentLifecycle`, `app/AppState`, `app/SelfTest`, `ui/MacPaintUI`, `main.cpp` and `src/CMakeLists.txt` extended. **Built as `app/Journal`, not PLAN.md's `core/Journal`** -- it serialises an `app::OpenDocument` through `io/NpaintFile`, and a `core/` module may do neither: core/Layer.hpp says "app/ depends on core/, never the reverse", and both io/NpaintFile.hpp and app/DocumentLifecycle.hpp already refused to put a bag of EXR header attributes on the domain model. The plan's directory is wrong; its content is not (Deviations). **A journal entry is two files.** `doc-0001.npaint`, written by `saveNpaint()` with the document's own carry -- so PRD O7 is true in the only sense that matters, that exactly one piece of code in this application turns layers and tiles into bytes -- plus `doc-0001.journal`, a one-key- per-line sidecar holding what an `OpenDocument` has that a `core::Document` does not: the bound path, the title, revision/savedRevision, the PRD I11 edit labels, the residency mode, and an integrity record. That split is the `core::Document` / `app::OpenDocument` line, not a convenience: a `.npaint` deliberately does not record which file it is or that it has three unsaved changes, so no second serialiser for the *model* exists. **What is not journalled, and why that is the format's problem rather than this module's:** *anything `saveNpaint()` cannot write, the journal cannot recover*, which is the correct consequence of PRD O7 forbidding a second writer. io/NpaintFile.hpp's deferral list is therefore this module's, unchanged -- masks, dabs, selections, paths, latents. **The op stack is the one item on PRD O5's list that exists today and is still not journalled**, because `core::OpStack` lives on `app::AppState`, is session-global rather than per-document, and `np:ops` needs a blob attribute io/NpaintFile *measured* as not surviving this OpenImageIO's EXR writer at all; encoding `core::Op` in a recovery module would be a format decision taken in the wrong place, in the second serialiser O7 forbids. So **PLAN.md's own Verify line -- "`kill -9` mid-session and confirm the recovered document matches the journal, including layer structure and op stacks" -- is half met: layer structure yes, op stacks no.** Unblocked by io/NpaintFile gaining `np:docOps` via the hex/base64 string carrier its header already names as the cheap fix, at which point the journal gets it for free. **The scratch directory.** `~/Library/Application Support/naturalPaint/recovery/session-YYYYMMDD-HHMMSS-<pid>`, `$NP_JOURNAL_DIR` overriding -- io/ExportAs' and app/DocumentLifecycle's location and override exactly, for their stated reason. Deliberately **not** `~/Library/Caches`, which is where a scratch directory would normally go on this platform: Caches holds data the system may purge and the application can regenerate, and after a crash the journal is the only copy of the work. A directory exists exactly while a session is unclean, because a clean shutdown removes it. **Liveness is an `flock()` probe, never the recorded pid, and that is the load-bearing choice in discovery.** A `flock` is kernel state released when the holder dies for any reason, power loss included, so the "stale lock from a machine that lost power" case is structurally impossible rather than merely unlikely; a pid check has exactly the wrong properties, since pid reuse after a reboot would make a crashed session look alive and hide the work forever. Every probe outcome that is not "held" -- no lock file, cannot open it, `flock` unsupported on a network home -- **lists the session anyway**, because the two errors are not symmetrical: offering a live session costs a confusing duplicate in a list and nothing else (recovery never writes and never deletes), while hiding a crashed one costs the work. `--selftest` proves a live session is not offered back to the process holding it. **Crash safety of the journal write itself.** Model to `doc-0001.tmp.npaint` (extension kept, because io/NpaintFile selects the OpenImageIO plugin from the path and `.npaint.tmp` would be handed to a lookup that has never heard of `.tmp`), read back to size and hash it, `fsync`, `rename`, then the sidecar the same way -- **sidecar last**, so the only state a crash between the two can leave is a model with a stale sidecar or none, never a sidecar promising a model that is not there. The sidecar carries the model's byte count and an FNV-1a 64 hash, checked **before** `loadNpaint()` is ever called, so a truncated journal is a named refusal quoting both byte counts rather than a half-loaded document that looks plausible; both refusals are asserted in the OFF build too, where the ordering is *provable* because the message is about truncation and not about the missing backend. Both readers also require a terminating `end` line. `fsync`, not `F_FULLFSYNC`: measured on a 1 MiB write across three runs, `fsync` 0.16-0.94 ms against `F_FULLFSYNC` 7.4-21.4 ms -- 8x to 112x, always at least an order of magnitude -- to cover the single failure mode where the machine loses whatever was in flight everywhere else too. **The timer: 60 s, derived rather than inherited from ADR-0008's suggestion.** One journal write of io/TileResidency's own 2048x2048, 256-tile fixture (32.0 MiB of half data) measures **0.080-0.085 s** across three runs, i.e. ~2.6 ms per MiB, linear in occupied tiles. The fixture is not the case the interval has to survive, so: PRD's own 4K RGB document is 510 tiles / 63.8 MiB / ~0.17 s, and a realistic eight-layer 4K painting ~1.4 s, which gives duty cycles of 9.3% at 15 s, 4.7% at 30 s, **2.3% at 60 s** and 1.2% at 120 s. 60 s is the largest interval whose bound on lost work is still a sentence a user accepts and the smallest whose big-document duty cycle stays near 2%. `--selftest` prints the write cost every run and fails above 3% of the interval -- a 22x margin over what is measured, so the assertion catches an order-of-magnitude regression rather than policing variance. **One measurement changed the design rather than confirming it:** a deep copy of that same document costs **0.002-0.003 s**, under 4% of the write, so moving the write to a background thread would leave ~3 ms on the calling thread instead of ~82 ms -- a 27x reduction that does **not** need Phase 5 step 6's copy-on-write tiles first. I had expected the snapshot to be most of the cost. Recorded as a deferral with a known payoff, held back only because a second thread writing documents needs a rule about what may mutate one mid-write, and the canvas bridge that would create such mutations does not exist. So what is delivered against PRD O10 is its explicit clause -- a due write colliding with an active stroke is deferred to the first tick *after* the stroke, not to the next interval -- and **not** the stronger "never blocks the paint loop": the write is synchronous on the caller, `main()` runs it at the very end of the frame after `latency.recordFrame()` has judged that frame, so the one hitch per minute is never attributed to the pen. **The trigger reuses step 8's dirty model rather than inventing a second one**, which needed one small addition to it: `recordEdit()` gained an `EditKind` (`Structural` by default, so every existing caller is unchanged *and* correct) and `OpenDocument` a `structuralRevision`. Structural edits are due within one frame; ordinary content edits wait for the interval -- which is exactly the line ADR-0008 draws when it rejects "journal every edit" for making every keystroke a disk write. The whole rule is a pure function, `journalWriteDue()`, precisely so the build with no session still asserts it (PLAN.md §1.5); `tick()` calls it and holds no second copy. **PRD O6, the defect ADR-0008 names, is delivered but not by the mechanism the ADR describes.** There is no `mmap` tile spill in this codebase at all -- `core::TileStore` is an `unordered_map<TileCoord, Tile>` with no dirty set and no spill file -- so "dirty tiles flush on the same timer" is delivered by the journal write *being* the flush: every dirty document is written whole, the active one included, on the timer, with no deactivation anywhere. `--selftest` changes a tile in the **active** document, ticks, and reads the new pixel back out of the recovered document. The cost is that ADR-0008's "only tiles dirtied since the last flush are written" is **not** what happens: the granularity available today is the document, via `OpenDocument::revision`, so an unchanged document is skipped entirely and a changed one is rewritten entirely. Unblocked by `TileStore` gaining a dirty set, most naturally alongside Phase 5 step 6, where the write barrier that marks a tile dirty is the barrier that copies it. **Recovery is read-only, on success and on failure alike.** Nothing in a scratch directory is opened, moved or deleted by discovery or by recovery -- PRD O8's "never opened silently, never auto-deleted" is a property of those functions having no write path, not a policy on top of one -- and a *successful* recovery deletes nothing either, so a user who recovers and then loses power before saving finds the session again. Only `discardRecoverySession()` removes anything, and only from a button that says so. A recovered document comes back **dirty**, bound to its original path with its edit labels in order, so PRD O9 holds by construction: nothing is written anywhere until the user decides to write it. **The cache-invalidation question step 8 left as a trap: the journal calls `tileCacheInvalidate()` on its own scratch path, and no stale read is reachable today.** The journal path is written only by the journal and read only by `recoverDocument()`'s `loadNpaint()`, which goes through `ImageInput` and not the `ImageCache`. The call is there because the alternative is a comment asserting that no future caller will ever open a residency on a recovery file -- and recovering a 4K document through a cached residency is exactly what step 5's module is for -- and because it is a no-op with no cache and always in the OFF build, so being right costs nothing. **Where the invariant belongs is the writer, and it is not there for a reason worth pricing.** `saveNpaint()` leaving a process-wide cache holding a file's previous bytes is unsound regardless of caller, but `io/TileResidency` includes `io/NpaintFile.hpp` (for `NpaintCarry` in `npaintLayerTileSource()`), so the reverse edge is a cycle. Two ways out: move `npaintLayerTileSource()` into io/NpaintFile, which is the one function in TileResidency needing `NpaintCarry` and would remove the edge outright (~40 lines moved, two shipped headers and their tests touched); or hoist a ~30-line `io/TileCache` holding just the cache handle and `invalidate()`, which both would depend on and neither would be renamed for. The second is the right shape and is a small refactor -- but it is a restructuring of two tested modules that this step was told not to do as a side effect, so it is recorded as debt with an unblocking condition: **the third direct caller of `saveNpaint()`.** **NP_USE_OIIO=OFF: there is no recovery journal, and that is a P0 requirement unmet in the default build.** `saveNpaint()` is OpenImageIO-only, PRD O7 forbids a second writer, and `NP_USE_OIIO` defaults **OFF**, so PRD O5-O10 -- P0 data-safety requirements -- are undelivered in a plain `cmake -S . -B build`. The three options were a subset through a second writer (O7 forbids it, and it would produce recovery files this application could not read back), refusing to start (absurd for a painting application's default build), or running without a journal and saying so once, by name. The third: `journalAvailable()` is false, `journalUnavailableReason()` forwards io/NpaintFile's own refusal *obtained by probing `saveNpaint()` rather than by copying its wording* (and the probe opens no file, which is asserted), `main()` prints it at startup, the recovery dialog shows it, and **no scratch directory is created at all**, since an empty one would be offered next launch and hold nothing. Discovery still runs in both builds deliberately: a directory left by an ON build must never be invisible to -- or silently deleted by -- an OFF build. The real fix is not in this module; it is `NP_USE_OIIO` defaulting ON, which is blocked by step 6's measured +29.4 MB and +9.4 ms dyld cost. **New `--selftest recovery journal` section** (`runRecoveryJournalTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain after `documentLifecycleOk`, and confirmed by its `[selftest] recovery journal PASS` line actually appearing in both runs -- **69 assertions in the OIIO build, 35 in the other**). Over half is build-independent by construction: availability, the scratch location, the entire timer rule, the naming and dating, the liveness probe, the sidecar format and every integrity refusal. **The crash is simulated in process**, by letting the session's destructor run -- which is the same code path an abnormal exit takes, since `finishClean()` is the only thing that removes anything -- so nothing is signalled or killed. The round trip asserts tiles bit-identical at zero tolerance, every `np:*` layer attribute (kind, name, blend, opacity, visible, locked, parent, all set to non-defaults in the fixture), the PRD I10 carry through journal->recover including a whole foreign `np:kind="Pigment"` part, the restored dirty state and edit labels in order, a fresh `DocumentId`, the user's own file untouched, and a saved document's journal being dropped. The hand-built-entry cases use a second, independently written FNV-1a in the test rather than the module's, so "this entry is intact" also proves the two agree. All scratch lives in one `selftest_journal/` directory with `$NP_JOURNAL_DIR` pointed at it, removed unconditionally and asserted gone; `~/Library/Application Support/naturalPaint/` is **still non-existent** after every run, verified by listing it. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` -- **32** sections (up from 31), **0 FAIL**, exit 0, idle RSS **91.3 MB** across three runs; `NP_USE_OIIO=OFF` -- **32** sections, **0 FAIL**, exit 0, idle RSS **63.0-63.3 MB**. Both at or below HEAD's own numbers measured in the same session (64.4-64.5 / 92.5-92.8) and inside the range step 8 recorded, which is what should happen: `JournalSession` holds a path, a file descriptor and an empty map, and `--selftest` returns before `AppState` is constructed at all, so the journal is not on the idle-RSS path by construction. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** -- 34 and 35 warnings total, every one of them from the SDL3 `_deps/` tree. **Regression-checked against HEAD's own binaries: 37 added lines and zero removed or changed in the OFF build, 75 added and zero removed or changed in the ON build**, excluding only the idle-RSS line and tile residency's `fetch cost:` line. Three runs of each new binary are identical to each other under the same exclusions plus the new `[measured]` lines, and `--diag 5` is still byte-identical run to run (PRD H7 untouched). **The running application was exercised, not just the test:** launched with `$NP_JOURNAL_DIR` pointed at scratch, it prints its session directory, a real `kill -9` leaves that directory behind, and the next launch discovers it and **auto-deletes nothing** -- the two session directories sit side by side afterwards. **The canvas gap, which this step must be most careful about, is unchanged and larger in consequence here than in any step before it.** `sim::PaintSim` still owns one dense texture with no layer awareness, a stroke still reaches no `Layer::rgbTiles`, so **the journal cannot recover what is on screen**: it recovers open documents, and a session spent painting on the canvas journals nothing because nothing in the session ever becomes dirty. Recovery is complete for the model; the model does not yet include the pixels a user is actually painting. The header says this in those words, and the trigger is keyed to `recordEdit()` precisely so the bridge -- "one more mutator that writes tiles and calls `recordEdit()`", as step 8 promised -- lands journalled with no change to this module. **What was deliberately not built.** No background writer (above, with its measured payoff and its blocker). No per-tile dirty tracking (needs a dirty set on `TileStore`). No op stack, masks, dabs, selections or paths in the journal (each is io/NpaintFile's deferral, not a second one). No `F_FULLFSYNC` mode. No journal for the OFF build. No prompt-on-quit for a dirty document -- closing one still refuses by name, as step 8 left it. **What got worse.** `--selftest` grew a section that writes a 32 MiB `.npaint` and a directory tree (+0.6 s in the ON build, +0.2 s in the OFF one). `app/AppState.hpp` now pulls in `app/Journal.hpp` on top of step 8's `io/NpaintFile.hpp` and `io/TileResidency.hpp`, so `main.cpp` and both UI files depend on the journal's header as well -- the same coupling increase step 8 recorded, one header deeper. `ui/MacPaintUI.cpp` gained ~120 more lines of modal code in a file PLAN.md says is **replaced, not extended**; deliberate, because PRD O8's offer that no user can reach is not an offer, but it is more work to throw away. And `app/DocumentLifecycle`'s `recordEdit()` grew a parameter, which is a shipped API changing one step after it shipped -- defaulted so no caller changed, but it is a change. **Phase 4 exit criteria, since this is the last step.** "Round-trip a 32-bit EXR without precision loss": met by step 4's section. "`kill -9` mid-session and confirm the recovered document matches the journal, including layer structure and op stacks": **half met** -- structure and pixels yes, op stacks no, for the format reason above. "Confirm cold start is unchanged with `NP_USE_OIIO=ON` but no file opened": **not met, and not by this step** -- step 6 measured 6.37 -> 15.72 ms, and the +9.4 ms is dyld mapping the OpenImageIO chain before `main()`, which no amount of deferred initialisation touches; only `dlopen`-ing OIIO on first use would, which is a linkage change. This step adds one directory scan to startup, measured at **0.02-0.05 ms** against PRD A2's 100 ms budget, so it does not make that number worse. |
| 2026-08-19 | **Phase 4 step 8 ("Document lifecycle -- revert, duplicate document, save a copy, save incremental, open recent"; PLAN.md cites PRD I17, but the wording is **I18**'s -- I17 is now "Export layer comps to files", phase 5): PASS, and this is the step where the open-document ownership question finally gets answered rather than deferred again.** New `src/app/DocumentLifecycle.hpp`/`.cpp` compiled in **both** configurations; `app/AppState`, `app/SelfTest`, `ui/MacPaintUI`, `main.cpp` and `src/CMakeLists.txt` extended. **The ownership decision, in three parts, and it is the thing a reviewer should argue with first.** (1) `OpenDocument` is the record: a `core::Document` plus the path it is bound to, io/NpaintFile's `NpaintCarry`, a dirty state, a residency mode, and a per-process `DocumentId`. (2) `DocumentSession` owns them as an **ordered list with an active index, not a single `std::optional<OpenDocument>`** -- because "duplicate document" produces a *second* open document and against a single-document owner one of the five required operations would simply have had nowhere to put its result; Phase 5 step 14's tab strip is this list drawn differently, not a replacement for it. It holds `unique_ptr`s so a pointer handed to a dialog cannot dangle when another document is opened, and `--selftest` asserts that across 32 further opens. (3) **`app::AppState` holds the session**, which is what makes this different from the last three steps: `main.cpp` now really does hold documents, File > New Document / Open... / Open Recent really do put them there, and Save / Save As / Save a Copy / Save Incremental / Revert / Duplicate / Close all act on the active one. Why `app/` and not `core/` or `io/`: io/NpaintFile.hpp already refused to put `NpaintCarry` on `core::Document` and io/TileResidency.hpp already refused to put residency in `core::TileStore`, both because `core/` is the domain model and must not learn a file format's shape; and a file-format module has no business owning the list of open windows. `app/` is where session-scoped application state already lives. **A second, smaller ownership rule was set at the same time, because step 7 explicitly left it open** ("app/AppState's ownership is PLAN.md Phase 4 step 8's decision to make"): *document and session state lives on AppState; transient widget state does not*. A list of open documents and the recent-documents list are things `main.cpp` owns and a future document-aware draw loop reads; a text buffer being typed into, a combo index, or which popup is open are none of those, and putting them on AppState grows it by one member per dialog. Under that rule step 7's function-local `ExportPresetStore` is the one item that would move, and it deliberately was **not** moved -- changing a shipped dialog's load lifetime for no functional gain is churn, and it is recorded here as debt rather than done silently. **The gap that is NOT closed, stated as specifically as the rows above it: the live painting canvas is still not one of these documents.** `sim::PaintSim` owns a single dense GPU texture with no layer awareness, so a stroke writes that texture and touches no `Layer::rgbTiles` anywhere; a document opened from a file and then painted on does not record the paint, and saving it writes what was opened. Nothing pretends otherwise -- `OpenDocument` has no canvas member, the File menu's old "New" is renamed **"New Canvas"** so it cannot be mistaken for "New Document", the Export As dialog now names which of the two it is looking at, and the menu bar shows the active document beside the canvas status rather than merging them. That bridge is genuinely bigger than this step: it needs a decision about which layer the solver deposits into, a texture-to-tile path with its own dirty tracking (PRD O6), and it only stops being provisional once Phase 5 gives `Document` multiple layers and a history. The record is shaped so it lands as one more mutator that writes tiles and calls `recordEdit()`, with no operation below changing. **What step 7's dangling end got, since a document now exists: the Export As dialog's Export button is live.** It exports the *active document* through `exportDocumentWithRequestToFile()` with an output-path field, validates against the document's own working space rather than `nullptr`, and is still disabled -- with the accurate reason, not step 7's now-false one -- when no document is open. That change is the concrete evidence the ownership decision is load-bearing rather than decorative. **Revert.** Reloads the bound path through `loadNpaint()` and replaces the document **and its carry**, because "the last saved state" is what the file says, foreign parts included. Unsaved work is discarded, so the discard is a **parameter** (`RevertOptions::discardUnsavedChanges`) rather than a dialog's private business -- a menu handler, a script or a future PRD P1 action cannot call it accidentally -- and the refusal it produces without that parameter is PRD I11 done properly: `OpenDocument::recordEdit()` keeps up to 32 edit labels plus an exact overflow count, so the message reads "2 unsaved changes (stroke before the copy, paint stroke on layer 1)" and names the file it would be replaced from, not "unsaved changes". **The load goes into a temporary and is only moved in once it has succeeded**, so a revert whose file has been deleted leaves the in-memory document byte-for-byte as it was rather than turning a recoverable mistake into data loss -- asserted specifically. **Duplicate.** Deep tile copy (`TileStore` is still uniquely-owned 128 KiB buffers; Phase 5 step 6's COW makes this cheap without changing the function, and the N x 128 KiB cost is stated rather than hidden), carry copied including `layerPartNames` so `np:parent` links inside a foreign part still point where they did, fresh `DocumentId` so a future history keyed by id cannot leak one document's undo stack into its copy, dirty from birth, and **the path deliberately not copied** -- with its own assertion, because an inherited path is the single most dangerous thing a naive struct copy gets wrong, and a second assertion that `saveDocument()` on the duplicate refuses by name instead of writing to the original's file. **Save a copy.** The distinction from Save As is the whole operation, so it is enforced by the **signature**: it takes a `const OpenDocument&`, so it *cannot* rebind the path, clear the dirty flag or record an edit, and a later change cannot make it do so without changing the declaration. It also takes no `RecentDocuments*` at all -- "open recent" is a list of documents that have been *open* and a copy never was (Photoshop behaves the same way), and offering it there invites reopening the copy believing it is the working file, which is the confusion the operation exists to avoid. Saving a copy onto the document's own path is refused by name, because that is a Save wearing the wrong name. **Save incremental.** `<stem>_v<NNN><ext>`, three digits minimum, zero-padded -- the versioning convention of the industry this container already belongs to (a `.npaint` *is* an EXR, PRD I8), with `_v` as an unambiguous marker where a bare trailing number is not, and padding that sorts. Four rules, each with its own test: an already-versioned name is **incremented keeping its own padding** (`_v7` -> `_v8`, never `_v008`) and padding grows rather than truncating (`_v999` -> `_v1000`); the next version is one above the **highest existing sibling**, not one above this file, so incrementing an old version still lands on top; **gaps are stepped over, never filled**, because writing `_v002` after `_v003` exists produces a newer file that sorts as older, which destroys the only thing the number is for; and the candidate is re-checked against the filesystem afterwards so a differently padded sibling or a file created since the scan is never overwritten. A trailing number with no `_v` is treated as part of the name (`render001.npaint` -> `render001_v001.npaint`), because a trailing digit is at least as likely to be a frame or a size and guessing would renumber someone's series. A relative input gives a relative output; nothing silently absolutises a name a user typed. **Open recent.** Capacity **10** (macOS's own Recent Items default, and the point past which a menu becomes a search problem), most-recent-first, re-adding moves rather than duplicates, and dedup is on the **lexically normalised absolute** form so `./x/../a.npaint` is the same entry as `a.npaint`. Stored at `~/Library/Application Support/naturalPaint/recent-documents.txt` with `$NP_RECENT_DOCUMENTS` overriding -- io/ExportAs' `defaultExportPresetsPath()` precedent exactly, same directory, same override, and for its stated reason (user data the application writes, not shipped data a developer hand-edits, so app/Keymap's `${NP_KEYMAP_DIR}` source-tree pattern is the wrong one and a read-only install must still be able to write it). **Format is one path per line, not JSON, and that is a deliberate departure** from export-presets.json: io/ExportAs.hpp records that its hand-rolled JSON reader is the second in this codebase and that "a third consumer is when it should be hoisted", this would have been the third, and hoisting means editing app/Keymap and io/ExportAs -- two tested subsystems -- which is a refactor this step should not carry. A list of strings does not need an object syntax. The cost is paid rather than hidden: a path containing a control character cannot be represented, so it is **refused by name** at `add()` instead of corrupting the file, and an unusable line is skipped and reported with its line number rather than failing the load, so a corrupt recent list can never stop the application starting. **A missing entry is never silently dropped**: `recentDocumentMissing()` gives a sentence naming the path and distinguishing "gone" from "could not be checked" (a permissions or unmounted-volume error, where telling the user it was deleted sends them looking in the wrong place); the menu shows the entry greyed with that sentence as its tooltip; `openRecentDocument()` refuses with it *plus* the fact that the entry has been kept and how to drop it deliberately; and `--selftest` asserts the entry really is still in the list afterwards. **One real bug found and fixed that nobody could have predicted without step 5's module in hand.** io/TileResidency documented that OpenImageIO's `ImageCache` serves a changed file's old tiles, and defended against it by stamping size+mtime at open. That defence does not cover this case: a residency opened *after* a rewrite stamps the **new** size and mtime, so its own staleness check passes while the cache underneath still holds the old tiles. Measured on this build by temporarily removing the fix: a cached read of a just-overwritten `.npaint` returned **0.5000** -- the previous pixel -- and reported success; with `tileCacheInvalidate()` called by every write path here it returns **0.2500**, the value actually in the file. Both numbers are printed by `--selftest`. So revert, save, save as, save a copy and save incremental all invalidate the file's tiles at the moment the file changes, which is the only point at which that is cheap and unconditional (and a no-op in the OFF build). **Residency, which is the other question step 5 handed here: the record gets a `residencyMode`, NOT a vector of `LayerResidency` objects, and that will look like under-delivery unless the reason is read.** io/TileResidency's own measured warning is that a `Document` beside a cached residency has an **empty** `rgbTiles` until tiles are promoted, so moving tile ownership out of `Layer::rgbTiles` today would break `saveNpaint()`, `flattenDocumentToLinear()`, `core/Probe`, `core/Histogram` and `ui/NaturalPaintUI` simultaneously, for no gain, since no consumer reads through a residency yet. Every open document is therefore `Eager` and `Layer::rgbTiles` stays the single source of truth; the field records the choice so that when consumers are converted the mode is already a property of the open document rather than a fifth thing to invent. **A limitation found by the test rather than by reading: a record that has only ever been *saved* cannot name its own subimages.** `npaintLayerTileSource()` is derived from `NpaintCarry::partOrder`, which only a *load* fills in, and `saveNpaint()` does not report the part order it wrote -- so `--selftest`'s residency case has to re-open the file it just saved. Recorded in the header as a deferral with its unblocking condition (`saveNpaint()` returning the layout it wrote) rather than worked around silently. **New `--selftest document lifecycle` section** (`runDocumentLifecycleTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain immediately after `exportAsOk`, and confirmed by its `[selftest] document lifecycle PASS` line actually appearing in both runs -- **93 assertions in the OIIO build, 63 in the other**). Roughly two thirds of it is build-independent *by construction* -- the record, the session, duplication, the incremental naming rule and the entire recent list touch no file format -- and the remaining third asserts that each file-backed entry point forwards **io/NpaintFile's own** named refusal in the build with no writer rather than a second vocabulary this module invented; one `kOiioBuild` constant carries the configuration and nothing is `#ifdef`'d out (PLAN.md §1.5). **PRD I10 is asserted after every operation that writes**, not once: the fixture carries three unrecognised document attributes, an unrecognised layer attribute and a whole foreign `np:kind="Pigment"` part sitting *between* the two layers, and the carry is checked intact after save-as, after save-a-copy, after a revert (the case where a naive "reload the pixels" implementation would drop it), after duplicate, and in the `_v001` file an incremental save left behind. PRD I12 needs no new proof because every save routes through `saveNpaint()` -- there is no second writer here, and none of these functions assembles a part or an attribute of its own. **All scratch state lives in one `selftest_lifecycle/` directory**, removed unconditionally and asserted gone -- a directory rather than loose `selftest_*` files because `nextIncrementalPath()` *lists its containing directory*, and run against the working directory its answers would depend on whatever else was there. `$NP_RECENT_DOCUMENTS` is overridden for the storage-location assertions, and the run leaves `~/Library/Application Support/naturalPaint/` **non-existent** -- verified by listing it afterwards. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` -- **31** sections (up from 30), **0 FAIL**, exit 0, idle RSS **91.2-91.5 MB** across three runs against HEAD's **91.2-91.5**; `NP_USE_OIIO=OFF` -- **31** sections, **0 FAIL**, exit 0, idle RSS **63.0-63.2 MB** against HEAD's **63.1-63.2**. Unchanged, as expected: `DocumentSession` is an empty vector until something opens a document, and the recent list is not read until the File menu is first opened. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** -- the only warnings anywhere remain `third_party/stb_image_write.h`'s `sprintf`, the SDL3 `_deps/` noise and the `ld: ignoring duplicate libraries` note. **Regression-checked against HEAD's own binaries: 64 added lines and zero removed or changed in the OFF build, 95 added and zero removed or changed in the ON build**, excluding only the idle-RSS line and tile residency's `fetch cost:` line, which is nondeterministic at HEAD too. Three runs of each new binary are byte-identical to each other under the same exclusions, and `--diag 5` is still identical run to run (PRD H7 untouched). `--selftest` wall time moved by under 0.3 s, inside the suite's own run-to-run noise. **What was deliberately not built.** No native file picker -- Open, Save As and Save a Copy take a **typed path** in a small modal, because a file panel is a platform-integration job (NSOpenPanel behind an interface) and not part of a lifecycle step; swapping the text field for a panel later changes one function. No close-with-discard from the menu (closing a dirty document refuses by name and says what to do instead; an "are you sure" flow belongs with the same confirmation machinery Phase 5's tabs will need). No move of `AppState::guides` or `AppState::opStack` onto the `Document` they conceptually belong to -- both of their own comments say they live on AppState only because no Document existed, and now one does, but guides need a per-document home once tabs exist and the op stack needs Phase 5 step 3's per-layer ownership decision, so a rename now would be relocating them twice. No undo/history (Phase 5 step 7 -- `DocumentId` is the identity it will key on, and nothing else about it exists). No PRD I13 read-back verification of a save. **What got worse.** `--selftest` grew a directory-creating, directory-listing section (~0.2 s in the OFF build), `app/AppState.hpp` now pulls in `io/NpaintFile.hpp` and `io/TileResidency.hpp` transitively through `app/DocumentLifecycle.hpp`, which makes every translation unit that includes AppState -- `main.cpp` and both UI files -- depend on the native format's headers where before they did not; that is a real coupling increase and the honest alternative (a pimpl or a forward-declared session) would have bought nothing this phase. And `ui/MacPaintUI.cpp` gained ~230 lines of File-menu and modal code, in a file PLAN.md's UI section says is **replaced, not extended** -- deliberate, because the replacement chrome does not exist and a lifecycle nobody can reach from the running application would have been the worse of the two failures, but it is more work to throw away when that chrome lands. |
| 2026-08-19 | **Phase 4 step 7 ("Export As -- format, space, depth *and resize*, with saveable presets (PRD I15). Downscale prefilters; see the phase 6 warning"; PRD I5, I11, B6): PASS for the pure core; the dialog is real but its Export button is deliberately disabled, and that is the honest half of this row.** New `src/ops/Resample.hpp`/`.cpp` (the prefiltered downscale) and `src/io/ExportAs.hpp`/`.cpp` (request model, size resolution, offerable sets, validation and warnings, the preset store, and the composed operation); `io/Export`, `ui/MacPaintUI`, `app/SelfTest`, `main.cpp` and `src/CMakeLists.txt` extended. **The load-bearing decision: `exportRefusalReason()` was extracted *out of* `encodeLinearImage()` rather than the dialog being given its own messages** -- the encoder now calls it and returns its result verbatim, both of its data arguments (working space, pixels) are optional so a preset with no document open can be checked for exactly the subset that is answerable, and `--selftest` asserts **string equality** between what the dialog would show, what the shared helper returns and what a real `encodeLinearImage()` failure carries. A UI copy of "PNG can't do that" is a copy that drifts the first time either side changes; there is now no second copy to drift. The same structure makes "the dialog cannot offer a combination `io/Export` will refuse" a property of the code: the format combo is `offerableExportFormats()` and the depth combo `offerableExportDepths(format)`, both read off `io/Capabilities`' runtime probe, so an OFF build has no EXR row and an ON build offers EXR at half and float but **not** at 8-bit -- the substitution OpenImageIO would have made silently. **Area average, not Lanczos or Mitchell, and the reason is not simplicity.** It is phase 6's own prescription ("area average or descend the mip pyramid"), it is parameter-free so no B/C choice becomes a preset-compatibility commitment, and above all **its weights are non-negative**: a windowed-sinc lobe next to a specular highlight produces negative linear radiance, and this build now writes float EXR where the [0,1] clamp deliberately does not apply, so a filter that can manufacture negative light is the wrong default for an export. The cost is stated rather than buried and is measured below -- a box kernel's sinc response leaves a real residual on patterns whose period does not divide the scale factor. **Upscale is out of scope and refused by name**, in both `resolveExportSize()` (percent above 100) and `resampleAreaAverage()` (any axis growing), because an enlargement is decided entirely by a reconstruction filter nobody has chosen; `FitWithin` clamps its scale at 1 instead of refusing, which is the deliberately different answer for the one enlargement request that has an obvious right answer. **The resize sits between `flattenDocumentToLinear()` and `encodeLinearImage()`, so it is in linear light structurally rather than by convention** -- and the proof is the number in the file, not the call order: a 2x2 black/white checker halved exports as 8-bit code **188.0** = round(255 x srgbEncode(0.5)) (linear 0.5029), while averaging the encoded values instead lands at encoded 0.5000 -> linear **0.2140**, 0.286 too dark. **Measured, all of it.** 1-px checkerboard 512->64: prefiltered RMS error against the true mean 0.5 is **0.000e+00**, naive point sampling **5.000e-01** -- and the naive answer is not noisy, it is a uniformly *black* 64x64 image with no trace of a 50%-grey pattern left, which is what "no reconstruction filter fixes aliasing after the fact" means concretely. Period-3 stripes 512->64 (period does not divide the factor): prefiltered **0.0587**, naive **0.4751**, 8.1x -- the box kernel's own honest limit, reported. A transparent-green texel beside an opaque red one contributes **exactly 0.0000** green under premultiplied filtering against **0.5000** under a straight average. Full path, 64x64 1-px stripes exported at 25%: every texel of the 16x16 file within **8.821e-06** of the true linear mean. **One bug found by deriving instead of guessing:** normalised *float* weights sum to 1 only to ~n x 6e-8, so an 8x reduction left a fully opaque image at alpha ~0.999996 -- and `io/Export` refuses a JPEG or HDR export of anything below alpha 1.0 by name, so a resize would have silently made an opaque document un-exportable. The plan's weights are `double` for exactly that reason, and `--selftest` now asserts a constant 500x30 image reduces to 61x7 **bit-identically at zero tolerance**. **Presets: a preset this build cannot honour is kept, marked, and explained -- never dropped and never substituted.** The file is shared between the two configurations, so an OFF build that dropped EXR presets on load would delete the user's presets the first time it saved anything (PRD I10's principle applied to settings), and a silent fallback to a format that *does* work is the exact failure `io/Export`'s whole "explicitly, never silently" discipline exists to prevent. So an `EXR half` preset round-trips byte for byte in **both** builds, `exportRequestAvailability()` returns empty in one and `io/Export`'s NP_USE_OIIO-naming refusal in the other, and `--selftest` asserts both. **Presets live in `~/Library/Application Support/naturalPaint/export-presets.json`, deliberately NOT following `app/Keymap`'s `${NP_KEYMAP_DIR}` source-tree pattern** -- a keymap is shipped data a developer hand-edits, presets are user data the application writes, and a read-only install would not be able to save one; `$NP_EXPORT_PRESETS` overrides, which is what lets `--selftest` check the resolution without touching the developer's own file. PRD I11's warnings are the legal-but-lossy half and each carries a number rather than an adjective: 75.0% of the pixels discarded by a named resize, 256 levels at 8 bits against a half-float working space, **12.9x** coarser shadow steps for 8-bit linear (derived from `color/Space`'s own curve at run time, not quoted), the specific highlight value an integer depth will clip and where it is, and JPEG's lossiness -- with a control asserting a clean 16-bit request warns about nothing at all. **Both builds: 30 sections (HEAD had 29), 0 FAIL, exit 0.** Idle RSS unchanged -- OFF 63.0-63.2 MB against HEAD's 63.0-63.1, ON 91.0-91.2 MB against HEAD's 90.9-91.1 (five runs each; nothing loads a preset file until the dialog is opened). Regression-checked against HEAD's own binaries: **111 added lines and zero removed or changed** in the OFF build with only the idle-RSS line excluded, and the same in the ON build once the tile-residency `fetch cost` line is excluded too -- that line is nondeterministic at HEAD as well (75.10 vs 74.44 us/tile across two HEAD runs), so it is timing noise, not a regression. Zero compiler warnings from any path under `src/` in either build. **What was deliberately not built, and what got worse.** No upscale, no filter choice in the UI (phase 6 owns both), no per-format quality knob, no output path or filename template (PRD I17's naming rule is phase 5), and **no verbatim preservation of a preset whose *token* this build does not recognise** -- an unknown format string skips that one preset, reports it in `problems()`, and is lost on re-save; preserving it means `io/NpaintFile`'s carry mechanism rebuilt for a settings file, and the cost is recorded here rather than discovered later. `ops/Resample.cpp` is now the **third** copy of the `a <= 0 -> {0,0,0,0}` un-premultiply guard, and `io/Export.cpp`'s own note says a third caller is when it should be promoted -- not done here, because hoisting it means editing `core/Probe` for three lines and Phase 5 step 6 ("COW tiles") reworks that premultiplied-storage boundary anyway; it is debt, and it is now due. `io/ExportAs.cpp` also carries a second small hand-rolled JSON reader beside `app/Keymap.cpp`'s, for the same stated reason (different schema, private to its TU) and with the same trigger for hoisting. **And the gap this step did not close, stated as plainly as the rows above it: there is still no bridge from the live painting canvas to a `core::Document`.** `main.cpp` holds no Document; `sim::PaintSim` owns one dense texture with no layer awareness. So File > Export As... is real code -- combos populated from the live capability query, validation through the shared refusal strings, presets saving to a real file, the resize preview computed against the live canvas size -- and its Export button is **disabled with an on-screen sentence saying why**, because there is nothing to export from. `exportDocumentWithRequest(const Document&, const ExportRequest&)` is the whole operation and `--selftest` already runs it end to end on documents it builds itself; the ownership question is Phase 4 step 8's to answer, and inventing an answer here would have pre-empted it. |
| 2026-08-19 | **Phase 4 step 5 ("Wire OIIO's `ImageCache` as the residency layer for unmodified source tiles. This is the main reason the dependency earns its cost"; ADR-0001; docs/document-format.md §1): PASS, with the dependency's justification **partly** upheld and the boundary measured rather than asserted.** New `src/io/TileResidency.hpp`/`.cpp` compiled in **both** configurations, plus six `oiioTileCache*` functions on `io/OiioBackend` -- which stays the only translation unit allowed an OpenImageIO header, so TileResidency.cpp guards its one `#include` with `#if defined(NP_USE_OIIO)` exactly as `io/NpaintFile.cpp` does, and the OFF binary still has **zero** OIIO symbols under `nm -u` and no OpenImageIO/OpenEXR/Imath dylib under `otool -L`. `app/SelfTest`, `main.cpp`, `src/CMakeLists.txt` and `docs/document-format.md` extended. **The seam is `LayerResidency` in `io/`, not a backend inside `core::TileStore`, and that is the decision most worth arguing with.** `core/` is the domain model and knows nothing about files -- io/NpaintFile already made this exact call by keeping `NpaintCarry` off `core::Document` -- and, more concretely, eviction inside TileStore would silently turn `find()`'s `const Tile*` into a pointer that can go stale, a lifetime change to a type `ops/`, `ui/`, `core/Probe` and `core/Histogram` all hold; Phase 5 step 6 ("COW tiles") reworks tile ownership anyway, so building a second ownership mechanism now would mean building the same decision twice. Cost of that choice, stated not hidden: clean pixels are only reachable through `LayerResidency`, and **today there is no consumer** -- see the deferrals. **Eager and Cached satisfy one interface and the OFF build is not a degraded mode**: unlike step 4, opening a PNG and painting on it is identical in both builds (PRD I1, I3), so `TileResidencyMode::Eager` is today's behaviour restated as one implementation, most of `--selftest`'s new `tile residency` section runs in *both* configurations asserting the **same** answers rather than merely each build's correct one, and the OFF refusal names a *complete* alternative rather than a missing feature. Copy-on-first-write: the owned `TileStore`'s own key set **is** the dirty set (no parallel bookkeeping, no dirty flag on `core::Tile`, which stays the 128 KiB-exact buffer its static_assert pins); `readTile()` never promotes and hands back a pointer into a single staging tile valid only to the next call; `tileForWrite()` promotes once and the cache is never consulted for that coordinate again; and **a promotion whose clean read fails returns `nullptr` rather than starting from zeros**, because zero-filling would mean one stroke silently erased the image underneath it. The cache is never written to. **Measured, all of it, on a 2048×2048 (256-tile, 32.00 MiB) `.npaint`:** our own resident cost **32.00 MiB eager → 0.12 MiB cached** (one staging tile, zero owned); all **256 tiles bit-identical** to the eager path by `memcmp` of the full 128 KiB at **zero tolerance** (HALF in, HALF out, no conversion stage -- the same claim and the same reasoning step 4 applied to the `.npaint` round trip, and it holds); **73 µs cold / 5.6 µs warm** per tile; OIIO's own `stat:cache_memory_used` 32.00 MiB of a 64.00 MiB budget. **The honest part: at this document size the cache does not pay for itself.** It converts an O(document) cost into an O(budget) one, so at 32.00 MiB of content against a 64 MiB budget it is a wash -- 32.00 MiB moved out of our allocator and into OpenImageIO's. The win only begins above the budget, and it is the *shape* of the cost curve, not this measurement, that justifies the dependency. Two further numbers say where the walls are. **(1) Cold fetches do not fit an interactive frame.** Phase 1.1's measured p50 is 12.1 ms and ADR-0001's amendment sizes a 2560×1440 viewport at 240 tiles, giving a 50.4 µs/tile budget; warm fetches (5.6 µs, 1.34 ms/viewport) clear it ~9×, but cold ones (73 µs, **17.6 ms/viewport**) exceed a whole p50 frame. First paint of a cold region will drop frames; prefetch is unbuilt. **(2) The budget is only honoured above ~16 MiB.** Sweeping 256 distinct tiles at eight nominal budgets: 1/2/4/8 MiB all settle at **9.88 MiB (79 tiles)** -- a hard floor this build's cache never went below -- while 16/24/32 MiB land within 1 %. A budget picked for tightness would simply not have been obeyed. Landed **64 MiB**: above ADR-0001's ~60 MiB two-visible-document working set and inside the honoured range. Idle RSS is untouched by construction -- the cache is created on first cached open and costs **0.11 MB**, and idle RSS is sampled before any document exists; both builds' ceilings are unchanged (ON 91.5 → 91.5 MB, OFF 63.8 → 63.8 MB). **docs/document-format.md §1's claim that the `ImageCache` becomes the residency layer for our own documents holds, and holds *better* for our own documents than for imports** -- corrected in that file, with the wording's two problems: "and mip-mapped" does no work (nothing writes a pyramid; the residency runs at miplevel 0 throughout), and "not only for imported files" reads as if imports were the easy case when **they are the case that does not work**. Every format `io/ImageDecode` handles is scanline-stored, and OIIO gives those no partial residency: `autotile` off pulls the **whole 16.00 MiB image** into cache for one 128² request of a 2048² PNG (15.9 ms), and `autotile=128` bounds the memory but costs **1549 µs per scattered cold tile** against 49 µs tiled, because the decoder restarts to reach a passed scanline. So `openCachedLayerResidency()` **refuses untiled sources by name**, with that measurement in the message, and leaves them on the eager path -- meaning PLAN.md's own phrase "unmodified *source* tiles" is backwards: the strategy serves our own tiled container and refuses the imports the step was worded around. **Failure paths, each measured before being coded against.** OIIO's `get_pixels` leaves the destination *untouched* on error (does not zero it), so missing/truncated files surface as `Failed` with OIIO's own message and never as pixels. A file **changed on disk is served stale indefinitely** -- verified: rewriting a file with different pixels at the same dimensions under an open cache kept serving the old ones -- so the residency stamps size+mtime at open and re-checks on every cached fetch, at a measured **1.256 µs** per `stat()` (0.30 ms per 240-tile viewport, 2.5 % of a p50 frame; hoisting it to one per frame needs a draw loop that does not exist). A read **outside the data window returns success with zeros** -- the nastiest of the four, since it is undetectable after the fact -- so the residency bounds-checks itself and answers `Absent`. And one thing found by the test failing rather than by reading the docs: **shrinking the budget evicts nothing**; OIIO frees tiles only when adding one, so the eviction proof invalidates first, then sweeps cold, then shows a re-read of an already-swept tile incrementing `stat:tiles_created` -- evidence that *that* tile was dropped, where `current < created` alone is only a hint. **Deliberately not built**, each with an unblocking condition: no document-level residency manager and no `openResidentDocument()` (that record is Phase 4 step 8's central decision, and `main.cpp` holds no `Document` at all); **no consumer reads through this module yet** -- `core/Probe`, `core/Histogram`, `io/Export`'s flattener and `ui/NaturalPaintUI` all still read `Layer::rgbTiles` directly, so a cached document would flatten to transparent black today, and nothing in this build opens one that way except `--selftest`; no mip levels (`automip` off, `miplevel` always 0, unblocked by io/NpaintFile writing a pyramid, which its own deferral list says becomes worthwhile precisely now); no per-frame hoisting of the staleness `stat()`; and no adaptation of untiled sources, which is a measurement rather than an omission. **What got worse:** one more `stat()` per cached fetch, and `--selftest` grew ~1.3 s of wall time writing and sweeping a 32 MiB fixture -- the price of measuring on a document big enough for the answer to mean anything. |
| 2026-08-19 | **Phase 4 step 4 ("Native `.npaint` save and load -- multi-part tiled EXR via OIIO"; docs/document-format.md; PRD I4, I5b, I6, I7, I8, I10, I11, I12): PASS, with three measured corrections to the format spec and one substitution in the attribute set, both flagged rather than absorbed.** New `src/io/NpaintFile.hpp`/`.cpp` (the format: part assembly, attribute policy, every refusal, the carry-through bag) compiled in **both** configurations, plus `oiioWriteMultiPartExr()`/`oiioReadMultiPartExr()` added to `io/OiioBackend` -- which stays the only translation unit allowed an OpenImageIO header, so NpaintFile.cpp guards its one `#include` with `#if defined(NP_USE_OIIO)` exactly as `io/Export.cpp` and `io/Capabilities.cpp` already do, and the OFF binary still has **zero** OIIO symbols under `nm -u` and no OpenImageIO/OpenEXR/Imath dylib under `otool -L`. `core/Layer`, `app/SelfTest`, `main.cpp`, `src/CMakeLists.txt` and `docs/document-format.md` extended. **`core::Layer` gained the six metadata members the format persists** -- `name`, `blend`, `opacity`, `visible`, `locked`, `parent` -- because without them "round-trip fidelity" is vacuous: the struct was `{kind, optional<TileStore>}` and there was nothing to lose. All defaulted, so every existing construction site compiles untouched. **`blend` is a `std::string`, not an enum, and this is the decision most likely to be argued with.** Phase 5 step 2 (`core/Blend`) owns the enumeration *and* two decisions this step cannot make honestly -- which display-referred modes exist, and how PRD B7's "labelled as such" is expressed -- and a guess baked into a file format is expensive to withdraw, since `np:blend` is a string on disk either way. The string also makes PRD I10 true at the *value* level for free: a newer build's `np:blend = "linear-burn"` survives a load/save through this build because nothing parses it into a closed set and back, where an enum would need a side channel whose only job is to undo the enum. When `core/Blend` lands, this member becomes that enum plus one name<->enum mapping, the shape `layerKindName()`/`layerKindFromName()` already has. **The seven per-layer attributes written are `np:kind`, `np:name`, `np:blend`, `np:opacity`, `np:visible`, `np:locked`, `np:parent` -- `np:ops` is NOT among them, and `np:kind` takes its place in the count.** Stated plainly because the task asked for seven and this is a substitution, not a full delivery: `core::Layer` has no op stack (`core::OpStack` lives on `app::AppState`), so `np:ops` has no source, and hanging an OpStack off Layer purely to have something to serialise would pre-empt Phase 5 step 3's ownership decision *and* require inventing a blob encoding for `core::Op`. `np:kind` is not a filler: it is in the spec's own part listing, it is a real Layer member, and it is what the reader keys the "can I hold this part?" test off. **The single most load-bearing design decision is the reader's recognition rule, and it is deliberately strict in the reader's own disfavour**: a part becomes a `core::Layer` only if it is named `L####`, carries `np:kind="RGB"`, has channels *exactly* R/G/B/A in HALF, and has a tile-aligned data window. Anything else -- a Pigment part with `pig.*` channels, a group part, a saved selection, an RGB part a newer build gave a fifth channel -- is carried whole into `NpaintCarry::rawParts` with a warning naming why. Turning a part this build half-understands into a Layer would drop the half it does not, which is exactly what I10 forbids; and it is that rule, not the deferral comments, that makes the deferrals *safe*. **The carry bag is `NpaintCarry`, returned beside the Document rather than living on it** -- `core/` is the domain model and must not grow a list of OpenEXR header attributes; `loadNpaint()` returns one, `saveNpaint()` takes one, and step 8's document lifecycle is where the pair gets an owner. It carries unrecognised `np:*` attributes (document-level and per-layer), whole foreign parts as **raw bytes in their own sample type** (not converted to float, so a UINT8 or UINT32 part is genuinely unchanged rather than approximately so), the file's `np:basis`, **and the part order** -- because "part order is layer order" means appending a carried part at the end would move a newer build's Pigment layer to the top of the stack, which is data loss in the ordering rather than in the bytes. It also carries each layer's original `L####` id, so an `np:parent` link inside a foreign part still points where it did after a round trip. Carry scope is `np:*` only, per the spec's own wording: everything else in an EXR header (`compression`, `chromaticities`, the windows, `oiio:*`) is the container's own and is regenerated, and writing those back verbatim would fight the writer rather than preserve the document. **Part 0 is regenerated unconditionally through `io/Export`'s own `flattenDocumentToLinear()`, called rather than reimplemented**; its signature needed no extension. Two flatteners would make PRD I5b a claim about two implementations staying in sync instead of about one being correct. It returns straight alpha, so the re-association to EXR's associated convention happens in NpaintFile, in linear light, before the half conversion -- and the composite is the *only* place `docs/document-format.md` §6's "clamp on write" (NaN -> 0, |v| -> 65504) is applied, because the layer parts are the tiles' own half words and clamping those would be an edit. **Data windows are the tile-aligned bounding box of each layer's occupied tiles** (the format's own "layers allocate only where content exists"), and the reader **drops all-zero tiles** rather than allocating them -- not a shortcut but the only behaviour consistent with `core/TileStore`'s contract, since a rectangular EXR data window cannot encode a hole and keeping the zeros would grow `occupiedTileCount()` on every save/reopen of a sparse document. An empty layer is written as one all-zero tile (EXR has no zero-area part) and comes back with zero tiles. **Request validation runs *before* the NP_USE_OIIO gate**, deliberately: a malformed request is then refused identically in both builds, so a caller fixes it once, and --selftest proves every PRD I11 refusal in the build that has no OpenImageIO at all -- PLAN.md §1.5's lesson applied to the refusals rather than to the option. **PRD I7 is enforced by an allowlist plus a named-lossy predicate, not by a lossless-only enum**, because an enum containing only lossless values would make I7 true by construction and therefore untestable, and because the EXR `compression` attribute genuinely is a string (including the `name:level` form). `dwaa`, `dwab`, `b44`, `b44a` and `pxr24` are refused by name with the reason and an alternative; an *unrecognised* name is refused too, since a name this build does not know cannot be assumed lossless. `pxr24` is a judgement call and is flagged as one in the code: it is lossless for half and integer data, i.e. for every part this build writes, and is refused anyway because a `.npaint` file can carry FLOAT parts forward from a newer writer and a compressor whose losslessness depends on what a future version put in the file is not one a P0 rule can allow. **Three things were measured against this OpenImageIO and all three contradict or constrain the spec; `docs/document-format.md` was corrected in place.** (1) **`UINT8[n]` blob attributes do not survive.** Written as `TypeDesc(UINT8, 5)`, an attribute is simply *absent* when the file is read back -- no error, no warning; same for `INT32[n]` and every array type tried, while `string`, `int` and `float` all survive. The spec's "Use only OIIO-representable attribute types: string, int, float, and `UINT8[n]` for blobs" is therefore wrong for this build, and **every blob it names -- `np:ops`, `np:dabs`, `np:comps`, `np:paths`, `np:docOps`, `np:simParams` -- has no working carrier today**. `saveNpaint()` refuses a Blob attribute by name with the base64/hex-string workaround named, rather than writing a file that quietly lacks it. (2) **Empty string attributes are dropped too.** Harmless here only because the two attributes this build can legitimately write empty (`np:name`, `np:parent`) have the empty string as the reader's default, so absent and empty reconstruct identically -- luck, not design, which is why a *carried* empty-string attribute produces a save-time warning naming it (`NpaintSaveResult` gained a `warnings` field for this). (3) **OpenEXR multi-part output cannot mix tiled and scanline parts** -- it opens the file and then throws `Can't build a TiledOutputFile from a type-mismatched part` partway through -- so a carried scanline part is refused up front by name at save and warned about at load, rather than surfacing later wearing OpenEXR's wording. Two smaller measurements: `.npaint` is not in OpenImageIO's extension table, so `ImageOutput::create(path)` returns null and the writer is selected by **format name** (`create("openexr")`) with the real path passed to `open()` -- which is also what makes PRD I8 true by construction rather than by promise, since the same plugin with the same settings writes both extensions; and this build's OpenEXR writer reports `supports("appendsubimage") == 0` while the multi-part idiom (declare N specs up front, then `AppendSubimage`) works perfectly, so gating on that capability would refuse a file format that demonstrably works -- noted in the code so nobody adds the gate. **New `--selftest npaint format` section** (`runNpaintFormatTest`, declared in `SelfTest.hpp`, defined in `SelfTest.cpp`, wired into `main.cpp`'s `ok` chain immediately after `formatSupportOk`, and confirmed by its `[selftest] npaint format PASS` line actually appearing in both runs -- 61 assertions in the OIIO build, 18 in the other). It is the only section that writes real files, because a document format is a file format; every path is `selftest_npaint_*` in the working directory and all six are removed unconditionally at the end, verified by listing the directory afterwards. **Fidelity is asserted at zero tolerance and measured as zero: 4 tiles, 262,144 half words compared, 0 differ.** That is the right claim rather than an optimistic one because the chain has no rounding stage in it -- tile `uint16_t` half words, memcpy into the part buffer, `TypeDesc::HALF` out, ZIP (lossless), `TypeDesc::HALF` back, memcpy into a tile; no float appears anywhere along it -- which is precisely `docs/document-format.md`'s own justification for EXR. The sparse tile set survives too, including a hole inside layer 0's data window and a layer with no tiles at all. **The one honest tolerance is the composite's, and getting it to bound anything took a second attempt worth recording.** Derived: the composite passes tile-half -> exact readPixel -> flatten's sum -> un-premultiply (/a) -> re-associate (*a) -> float-to-half -> half-to-float -> un-premultiply again, so relative error <= 2^-11 + 3*2^-24 = 4.8848e-4, absolute the same for the fixture's <=1.0 values; landed **7.0e-4, 1.43x the derived bound** (not 1.43x the measurement), matching `runLutBakeTest`'s `kResidualTol` / step 1's `kRoundTripTol16` / `runFormatSupportTest`'s `kHalfEncodedTol` discipline. The first fixture measured **exactly 0.000e+00** -- because dividing and re-multiplying by the *same* alpha cancels, so the tolerance bounded nothing at all. Fixed by adding two layers contributing to the same pixel with alphas 0.5 and 0.25, whose premultiplied float sum is genuinely not a half: measured **1.628e-04**, comfortably under the 4.8848e-4 bound as expected since a small fixture cannot be relied on to hit the worst-case rounding phase. The composite is *additionally* asserted **exactly** at every alpha-1 and alpha-0 pixel, where both un-premultiplies and the re-association are by exactly 1.0 and nothing rounds -- measured 0.000e+00. **PRD I12 is proven by mutation, not by presence**: the composite is compared against `flattenDocumentToLinear()`, then a layer pixel is changed, the document saved again over the same file, and the reloaded composite is checked to have *changed* **and** to match the *new* flatten -- a test that only checked part 0 existed would pass against a stale one. The layer tiles are re-checked bit-exact after that second save, so regenerating the composite is proven not to disturb them. **PRD I10 is proven across two generations with data this build's code has no knowledge of**: three unrecognised document attributes (string/int/float), one unrecognised layer attribute, and a whole foreign `np:kind="Pigment"` part named `L0002` with `pig.c0`/`pig.m` FLOAT channels -- all injected through a hand-built carry, which is exactly how a future writer's part is represented. Each survives byte for byte, the foreign part stays *between* the two layers rather than being appended, the layers keep their `L0001` / `L0003` ids, `np:basis` comes back as `"future-basis-v9"` rather than being overwritten with this build's, and a **second** save/load generation is still identical -- which is the claim that actually matters, since one round trip only shows the reader kept what the writer had in hand. (Fixture note: the foreign part's channel names are already in sorted order, because OpenEXR stores channels in a sorted `ChannelList` and an unsorted fixture would come back reordered for a reason unrelated to preservation.) Also covered: the OpenEXR magic **and the multi-part bit in the file's own version field**, read out of the bytes rather than trusted from the writer; `np:opacity` checked at 0.0 and `np:visible` at false specifically so an absent attribute reading back as its default would fail rather than pass; PRD I6's `chromaticities` round trip; PRD I8's `.exr` rename; and the NP_USE_OIIO=OFF refusal, which names `.npaint`, the build option, the cmake line, *and* `exportDocumentToFile()` as the thing that does work in that build. **Both configurations built from scratch (`rm -rf` + reconfigure) and both pass**: `NP_USE_OIIO=ON` (`-DCMAKE_PREFIX_PATH=$HOME/.local/openimageio`) -- **28** sections (up from 27), **0 FAIL**, exit 0, idle RSS **91.3-91.4 MB** across repeated runs; `NP_USE_OIIO=OFF` -- **28** sections, **0 FAIL**, exit 0, idle RSS **63.0-63.3 MB**. Both unchanged from before this step, as expected: everything new here is pure CPU with no persistent allocation. **Zero `warning:` lines naming a `naturalPaint/src/` path in either full build log** (the only project-adjacent warning remains `third_party/stb_image_write.h`'s `sprintf`, plus the SDL3 `_deps/` noise and the `ld: ignoring duplicate libraries` note). **No regression in the export surface, verified rather than asserted**: the entire `--selftest export` section (58 lines, all assertions and both measured-residual lines) and the entire `format support` section are **byte-identical** to the pre-change binaries in *both* configurations, and the whole OFF transcript differs only by the new section and the idle-RSS line. **Not built, deliberately, each with the reason and the unblocking condition recorded in `io/NpaintFile.hpp` rather than silently omitted**: pigment/residual latents as `pig.*`/`res.*` channels and a `mask` channel (Layer has no latent or mask storage -- Phase 5 steps 3 and 4); a strokes part and `np:dabs` (no Dab type or stroke record exists in `core/`); `np:ops` and `np:docOps` (no per-layer or per-document op stack exists to serialise, *and* no blob carrier exists to write one into); `np:comps` and `np:paths` (Phase 5 step 12 and the vector work); `np:medium`/`np:simParams` (sim parameters live on PaintSim/AppState); saved selection parts (Document holds no selection); and **the mip pyramid** -- the only pyramid in this codebase is `ui/NaturalPaintUI`'s display-side GPU one, rebuilt from tiles and not part of the document model, so there is nothing on `Document` to persist; it becomes worth writing when step 5 wires `ImageCache` and a coarse level is something the reader would *fetch* rather than recompute. Also not built: any UI (no File > Save menu item, and this codebase still has no live-painting-canvas-to-`core::Document` bridge -- the same gap every prior UI-facing step's row records), OIIO's `ImageCache` (step 5), the recovery journal (step 9, which will aim this same writer at a scratch file), and PSD export (phase 15). **What got worse:** `docs/document-format.md`'s blob-attribute mechanism is now known not to work, which is a real hole in the format rather than in this step -- nothing that needs a blob can land until someone picks the string encoding or bypasses OpenImageIO for the header, and this row is the first place that cost is visible. |
| 2026-08-19 | **Phase 4 steps 2 and 3 (`io/OiioBackend` behind `NP_USE_OIIO` -- EXR, TIFF, HDR, DPX, flattened PSD, camera raw; and the runtime capability query, PRD I3/I1/B6): PASS, with camera raw deliberately absent and one exit-criterion regression recorded rather than hidden.** Landed together because step 3's query has to report on the backend step 2 adds. New `src/io/Capabilities.hpp`/`.cpp` (the query) and `src/io/OiioBackend.hpp`/`.cpp` (the backend); `io/Export`, `io/ImageDecode`, `app/SelfTest`, `main.cpp` and both `CMakeLists.txt` extended. **The single load-bearing decision: the query asks the linked OpenImageIO at run time, and there is no hardcoded format table anywhere.** PRD I3 reads "Format support is a runtime capability query, not a build-time hard requirement", and taking that literally is not pedantry here -- this project's OpenImageIO is a deliberately stripped-down local build, so any `if NP_USE_OIIO then {EXR, TIFF, HDR, DPX, PSD, camera raw}` table would have been factually wrong for this very build. Format presence comes from OpenImageIO's own `format_list` / `input_format_list` / `output_format_list` attributes; **alpha support and the per-depth write matrix come from actually opening a 1x1 in-memory writer for that format at that sample type and comparing `ImageOutput::spec().format` against what was asked for.** That second probe is not decoration, it is a PRD B6 defence: measured against this build, OpenImageIO accepts a request for an 8-bit EXR and writes `half`, accepts a `half` TIFF or DPX and writes `float`, accepts a 16-bit TGA and writes 8-bit -- silently, reporting success, which is exactly the truncation B6 forbids. `io/Export` refuses every substituted combination by name instead. **The query caught two of my own wrong hand-written expectations while landing**, which is the concrete argument for its existence: the new `--selftest` section's expectation table originally had TIFF accepting `half` and DPX refusing 32-bit float, both from memory of what those formats "usually" do; the probe disagreed and the probe was right (OpenImageIO's TIFF writer substitutes float for half; its DPX writer genuinely writes the DPX spec's R32 float element). A hand-maintained table would have shipped both mistakes. **Camera raw is NOT available, deliberately, and the query says so in the OIIO build too.** The OpenImageIO this project links was built without LibRaw, to keep LibRaw and its transitive dependency weight out; this build's own `format_list` reads `openexr,tiff,jpeg,bmp,cineon,dds,dpx,fits,gif,hdr,ico,iff,null,png,pnm,psd,rla,sgi,softimage,targa,term,zfile` with no `raw` entry. So PLAN.md step 2's list is delivered *except* camera raw, PRD I2 (P1) stays open, and `--selftest` asserts the unsupported answer specifically -- that assertion is the one a build-time table would fail while passing everything else, so it is the assertion that actually proves I3. The refusal string names LibRaw, names the plugin it looked for, and says the answer came from asking the library at run time. **`NP_USE_OIIO` defaults to OFF, and the reasoning is the opposite of `NP_USE_MIXBOX`'s.** A default that made `cmake -S . -B build` fail on a machine without OpenImageIO would contradict I3 at the first step; PRD I1's PNG/JPEG/TGA/BMP are stb-backed regardless, so the default build is a complete implementation of I1 rather than a crippled one; and `find_package(OpenImageIO)` cannot locate a `~/.local` install unaided, so an ON default could only ever succeed by accident. **PRD I1 protected structurally, not by promise: PNG/JPEG/TGA/BMP are deliberately NOT routed through OpenImageIO even when it is available**, on either the read or the write side (`decodeImageLinear()` tries stb first always and falls back to OpenImageIO only when stb declines; `encodeLinearImage()` branches to a wholly separate OIIO path). Verified rather than asserted: the entire `--selftest export` section's output -- all 55 assertions plus both measured-residual lines -- is **byte-identical** in the `NP_USE_OIIO=ON` build, the `=OFF` build, and the pre-change binary at `bfcf96c` (`diff` of the extracted section, three ways). The whole selftest transcript is byte-identical too apart from the new section and the idle-RSS line. **`ExportBitDepth` renamed from `{Eight, Sixteen}` to `{UInt8, UInt16, Half, Float32}`** -- "sixteen" cannot keep meaning one thing once EXR is in scope, where it means IEEE binary16 half float: same bit count, different representable values, different maximum, and only one of the two carries values above full scale. `ExportFormat` became `ImageFormat` and moved to `io/Capabilities.hpp` with the depth enum, because once "which formats exist" is a runtime question the type naming them belongs with the query that answers it, and because the list now has to name read-only members (PSD, camera raw) that an "export format" enum should not. **The [0,1] export clamp is now keyed to the depth, not the format**: integer depths clamp (an integer file has nowhere to put a value above full scale), float depths do not (clamping them would discard the scene-referred highlights EXR exists to carry) -- proven in `--selftest` by the same 4.0/12.5/1000.0 pixel surviving EXR exactly and clipping to full scale in 16-bit PNG. **Alpha association**: EXR is written associated (premultiplied) per the OpenEXR spec and un-associated on read, everything else stays straight -- verified first that OpenImageIO does *not* do this for us in either direction for EXR/TIFF/DPX (round-tripped a straight `(0.8, 0.4, 0.2, a=0.5)` unchanged), so the choice was genuinely ours; made this way because `docs/document-format.md`'s step-4 native container stores the working space's already-premultiplied `rgba16float` tiles as EXR HALF channels "byte-identical, no conversion", which is only true if EXR is the associated side of this boundary. Association happens in *linear light before* the transfer function, since it is a linear-light operation. **Linearity on decode follows the file's sample type, not its extension** (float/half source = linear, integer source = sRGB-assumed, reusing `io/ImageDecode.cpp`'s already-documented assumption verbatim); measured caveat recorded in `io/OiioBackend.hpp` rather than glossed: `oiio:ColorSpace` is *absent* on every EXR/TIFF/DPX/PSD this build produced or read, so there is nothing more authoritative to consult, and a genuinely log-encoded DPX therefore decodes as if sRGB. **New `--selftest format support` section** (`runFormatSupportTest`, wired into `main.cpp`'s `ok` chain immediately after `exportOk`; 79 assertions in the OIIO build, 47 in the other), and it is **deliberately not `#ifdef`'d out of the OFF build** -- PLAN.md §1.5's "an unexercised build option is not a seam" applies one level down, so a single `kOiioBuild` constant carries the configuration and every assertion states the correct answer for it. **Tolerances, derived and printed, not guessed.** EXR half and EXR 32-bit float linear round trips are asserted **exactly zero**, not within a tolerance, and both measure `0.000e+00`: the chain tile(half) -> readPixel -> single-contribution sum -> un-premultiply by exactly 1.0 -> re-associate by exactly 1.0 -> no transfer function -> no clamp (float depth) -> float-to-half of a value that already *is* a half has no rounding stage in it, which is the same claim `docs/document-format.md` makes for the native container; a tolerance there would let a real regression through. Half in the *encoded* domain: binary16's 10 stored mantissa bits bound the error at v*2^-11 <= 4.883e-4, landed 7.0e-4 (1.43x), measured **2.155e-04**. 16-bit integer through TIFF and DPX: step 1's own derivation re-derived for a different encoder -- 0.5/65535 amplified by the sRGB decode curve's steepest slope 2.4/1.055*((1+0.055)/1.055)^1.4 = 2.2749, giving 1.736e-5, landed 2.5e-5 (1.44x), measured **1.371e-05** for both. Radiance RGBE given its own separate, much looser constant rather than folded in, because it is ~16x coarser: three 8-bit mantissas sharing one 8-bit exponent, truncating rather than rounding, so error <= 2m/256 = 7.8125e-3 for a fixture peaking at m = 1.0; landed 1.1e-2 (1.41x), measured **7.324e-03**. 32-bit float proven genuinely deeper than half with 0.1f (not representable in half): the float file returns it bit-exact, the half file returns 0.099975586 -- both printed. Flattened PSD read proven with a **hand-built 52-byte PSD** constructed byte by byte from the published file layout (the same "the fixture cannot share the decoder's assumptions" discipline `encodePng16()` started life under), decoded through the production `decodeImageLinear()` entry point to its exact hand-computed `srgbDecode()` values. **Both configurations built from scratch and both pass:** `NP_USE_OIIO=OFF` -- 27 sections, 0 FAIL, exit 0, idle RSS 63.2 MB; `NP_USE_OIIO=ON` (`-DCMAKE_PREFIX_PATH=$HOME/.local/openimageio`) -- 27 sections, 0 FAIL, exit 0, idle RSS 91.4 MB. Zero warnings naming a `naturalPaint/src/` path in either (the only project-adjacent warning remains `third_party/stb_image_write.h`'s `sprintf`). `otool -L` confirms the OFF binary references no OpenImageIO dylib and `nm -u` finds no OIIO symbol in it -- I3's "the core builds and runs without OIIO" enforced by the link step, since `io/OiioBackend.cpp` is the only translation unit that may include an OpenImageIO header and CMake only adds it to the target when the option is on. Both `OpenImageIO::OpenImageIO` and `OpenImageIO::OpenImageIO_Util` are named explicitly in `target_link_libraries` -- the config package does chain the Util library, verified, but `ustring`'s symbols live only there and an install whose imported target did not chain it would fail with an error pointing nowhere near the cause. **The `ld: warning: building for macOS-15.0 ... built for newer version 15.6` warning does not occur in this build**, checked rather than assumed: CMake defaults `CMAKE_OSX_DEPLOYMENT_TARGET` to the host OS (15.6), matching the dylibs, so the compile line already carries `-mmacosx-version-min=15.6`; it appears only when the clang driver is invoked with no CMake and falls back to its own 15.0 default. Nothing was pinned or suppressed to achieve that. **The one thing that got worse, stated plainly: the `NP_USE_OIIO=ON` configuration's idle RSS does not fit under phase 1's 80 MB ceiling -- 91.4 MB against 63.2 MB for the core build.** The 28-29 MB difference was isolated to the dynamic loader, not to anything this code does, with a standalone program that reads RSS as its first statement: linking nothing costs 1.03 MB resident, linking `libOpenImageIO` + `libOpenImageIO_Util` and their transitive OpenEXR/Imath/OpenColorIO/libtiff/libpng/libjpeg-turbo/giflib dependencies costs **30.56 MB**, i.e. **29.5 MB paid before `main()` runs a line**. The same program's first OpenImageIO call then moves RSS by 0.02 MB, which independently confirms both that the lazy cached capability probe is free and that **PLAN.md step 6 ("lazy OIIO init on first file open") will not recover this** -- OpenImageIO's own init is already lazy and already free; only `dlopen`-ing the library on first use would defer the 29.5 MB, and that is a different linkage architecture, not a tuning change. `runIdleMemoryTest()` therefore keeps the 80 MB ceiling unchanged for the dependency-free build and adds a **separate, additive, separately-printed 32 MB allowance** for the OIIO configuration -- printed as a sum (`ceiling 80 MB core + 32 MB OpenImageIO dylib allowance`), never as one number, specifically so it can never read as if the core budget had quietly grown. Recorded here as a real cost of the dependency rather than absorbed silently; if the OIIO build ever needs more than 32 MB of allowance, that is a regression and should fail. **Not built, deliberately:** any UI (PLAN.md step 7's "Export As" owns the format/space/depth/resize dialog, and this codebase still has no live-painting-canvas-to-`core::Document` bridge for a menu item to export from -- the same gap every prior UI-facing step's row records); OIIO's `ImageCache` (step 5); lazy init (step 6); the native `.npaint` multi-part tiled EXR (step 4), whose foundation this is. PSD *export* stays phase 15 and is refused by name here. |
| 2026-08-19 | **Phase 4 step 1 ("Export path -- encode from working space to a chosen target space and bit depth, explicitly, never silently"; PRD B6, I5, I1): PASS. First step of Phase 4, and the first write path anywhere in `io/`.** New `src/io/Export.hpp`/`.cpp` -- `flattenDocumentToLinear()` (Document -> straight, linear float RGBA), `encodeLinearImage()` (straight linear float + its source `WorkingSpace` -> encoded file bytes: primaries check, depth/format check, transfer function, quantization, byte writer), `exportDocument()` (the two composed, the Document-level operation the step asks for), `exportDocumentToFile()` (the above plus one `fwrite`), and `encodePng16()`. Layered entry points rather than one do-everything function, deliberately mirroring `io/ImageIO.hpp`'s own stated "entry points, deliberately kept separate" philosophy and its documentation density; `flattenDocumentToLinear()` returns `io/ImageDecode.hpp`'s existing `DecodedImage` rather than a parallel struct, because that type's documented contract ("linear-light float RGBA, straight (non-premultiplied) alpha, row-major top-to-bottom, no row padding") is *exactly*, to the letter, what the flatten stage produces -- so import and export meet in the middle on one shared representation and the round-trip test below is a direct comparison rather than a conversion exercise. **I5 is enforced in the type system, not the docs**: `ExportTargetSpace` and `ExportBitDepth` are non-defaulted parameters on every entry point -- no overload, no default argument, no fallback -- so omitting either is a compile error, which is the only enforcement that actually holds for "explicitly, never silently." The enum values name *both* halves of what a colour space is (`Rec709Linear`, `Rec709Srgb`, `Rec709Bt709`), so "which transfer function got applied" is never inferable-by-absence at a call site, and `ExportBitDepth` is an enum rather than an `int` so a caller cannot request 10 or 12 bits and receive a silent nearest fit. **The primaries scope decision, made deliberately and recorded here so it isn't second-guessed later: option (b) -- transfer-function conversion only, with a *differing-primaries target explicitly refused*, not implemented-now and not silently ignored.** `color/Space.hpp` says outright that `Primaries` is "*not* an RGB<->XYZ matrix: deriving one is a job for whichever later step actually needs to adapt primaries", and no such matrix exists anywhere in this codebase; building one here would mean landing three separate design decisions the step does not ask for (Bradford-vs-CAT02 chromatic adaptation, an XYZ intermediate, and a gamut-mapping-vs-clipping policy for out-of-gamut colour), none of which the two transfer functions that actually exist need. So `exportTargetPrimaries()` states each target's primaries and `encodeLinearImage()` compares them against the document's `workingSpace.primaries`; a mismatch is a hard failure whose error quotes all sixteen chromaticity coordinates, names the target space, and says *why* ("this build converts transfer functions only -- no RGB<->XYZ primaries-conversion matrix exists yet"). Refusing a question this build cannot answer correctly is the honest option; writing the pixels out unconverted under the target's name is exactly the silent misreporting the step's own "never silently" wording exists to prevent, and it is the one outcome that is definitely wrong. The comparison uses a 1e-6 epsilon rather than exact float equality -- authored constants are bit-identical today, but a working space read back from an EXR `chromaticities` attribute or round-tripped through a UI field would not be, and Rec.709's and ACEScg's red x differ by 0.073, six orders of magnitude above the epsilon, so it cannot mask a real mismatch. When a later step needs real gamut conversion, that one check is the single place that turns from "reject" into "convert". **B6's loud-failure half** is a `FormatCaps` table (`maxBits`, `hasAlpha`) consulted by every refusal, so no two code paths can disagree about what a format carries: PNG 16/alpha, JPEG 8/no-alpha, TGA 8/alpha, BMP 8/alpha (verified by reading `stb_image_write.h`'s own `stbi_write_bmp_core` -- `comp == 4` emits a `BITMAPV4HEADER` `BI_BITFIELDS` 32bpp file with an `0xff000000` alpha mask -- and `stbi_write_jpg_core`, whose `ofsG`/`ofsB` prove it reads only offsets 0/1/2 and discards the fourth channel outright). A 16-bit request into JPEG/TGA/BMP returns `ok = false` with zero bytes and an error naming the format, the refused depth, the format's real limit, and the format that *can* carry the request. **A second refusal, added on the same "never silently" principle and flagged here as a judgement call rather than buried**: JPEG has no alpha channel, so a flattened document containing any pixel with alpha < 1 is refused by name with the first offending pixel's coordinates, rather than having its transparency silently dropped -- PRD I11's "a save that would lose data names exactly what, rather than degrading silently" applied to the same principle the step already demands for depth. Fully opaque documents export to JPEG normally; compositing a translucent document onto a chosen matte is a real UI decision (what colour?) that belongs to step 7's "Export As", not here. **The `[0,1]` clamp is called out as an export-policy decision rather than a B6 violation**: `color/Space.hpp` deliberately leaves its transfer functions unclamped because "whether to clamp is a display/export policy decision", and this module is where that decision gets made -- an integer format has no representation above full scale, so >1.0 highlights clip; that is a property of asking for an integer format, not a depth truncation, and lossless >1.0 export needs EXR (step 2's OIIO territory). **Flattening follows `core/Probe.cpp`'s precedent identically rather than inventing compositing**: a plain sum over every RGB-kind layer with populated tile storage, documented in the same terms and for the same reason (no blend-mode implementation exists anywhere in this codebase -- that is Phase 5 step 2's `core/Blend` -- and the sum is provably correct for today's invariant of at most one layer with painted content at a point). It iterates `TileStore`'s occupied tiles rather than a grid across the canvas, clips tile content to the canvas rectangle (export writes the canvas, not the content's bounding box), and un-premultiplies once at the end rather than per texel, for the exact reason `core/Probe.cpp` gives at length. **One honestly-flagged duplication**: the `a <= 0 -> {0,0,0,0}` un-premultiply guard is re-implemented in `Export.cpp` rather than shared, with a comment naming `core/Probe.cpp`'s `unpremultiply()` as its source and stating that promoting it would mean editing `core/Probe`, outside this step's scope -- the logic is identical, not "subtly different", and if a third caller appears the promotion is the right move. **The 16-bit PNG writer was promoted out of `app/SelfTest.cpp`**, where it had been a test-only helper (`buildMinimal16BitPng`) producing a 16-bit *decode* fixture: B6 makes 16-bit export P0 production work and PRD I1 forbids a second image library, so the hand-rolled writer (stb's public PNG API is 8-bit-only) is now `io/Export.hpp`'s `encodePng16()`, its original reasoning comments preserved and extended, and `runImageDecodeTest()`'s fixture calls the promoted function -- there is exactly one 16-bit PNG writer in the binary, not a production copy and a test copy that can drift apart. `STB_IMAGE_WRITE_IMPLEMENTATION` moved with it: `io/Export.cpp` is now the one translation unit defining it (production code must not depend on a test file for symbol bodies) and `app/SelfTest.cpp` includes the header for declarations only, exactly the arrangement `paint/Palette.cpp`/`io/ImageDecode.cpp` already have for `stb_image.h`. One visible consequence, stated rather than hidden: `stb_image_write.h`'s pre-existing `sprintf`-deprecation warning is now emitted from `io/Export.cpp`'s compile instead of `app/SelfTest.cpp`'s -- it is the same known third-party warning, relocated, not a new one. **New `runExportTest()`**, declared in `SelfTest.hpp`, defined in `SelfTest.cpp` and **wired into `main.cpp`'s `ok` chain** (confirmed by its `[selftest] export PASS` line actually appearing in the run, not merely by the code being written). Fixtures are built by writing *straight* linear RGBA through the same `rgb *= a` premultiply `io/ImageIO.cpp` performs, so the documents under test hold what a real opened document holds. **Tolerance derivation, measured rather than guessed**, following `runLutBakeTest`'s `kResidualTol` / `runApplyPassTest`'s `kApplyResidualTol` discipline: every precision claim is checked against the *tile's own stored value* read back through `Tile::readPixel` (post-half-rounding), not against the float literal written in -- half-precision storage is `io/ImageIO`'s boundary, already covered by `runImageIOTest()`, and its ~4.9e-4 relative error would otherwise swamp the term under test by a factor of ~35 -- and every fixture pixel used for a precision claim is fully opaque (alpha = 1.0, exact in half), so un-premultiply is a division by exactly 1.0 and contributes nothing. That leaves exactly one lossy stage: quantization of the *encoded* value, re-expanded through the sRGB decode curve, whose steepest slope is d(linear)/d(encoded) at encoded = 1 = 2.4/1.055 * ((1 + 0.055)/1.055)^1.4 = 2.2749. Half a quantization step times that slope bounds the residual at **1.74e-5 for 16-bit** and **4.46e-3 for 8-bit**; the fixture's *measured* worst cases, printed by the test at run time so the derivation is checkable rather than asserted, are **1.371e-5** and **3.542e-3** -- both of the same order as and below their bounds, as expected, since 16 sampled pixels cannot be relied on to hit the worst-case rounding phase at the worst-case slope. Landed `kRoundTripTol16 = 2.5e-5` and `kRoundTripTol8 = 6.5e-3`, i.e. ~1.44x and ~1.46x above the *derived bound* (not above the measurement -- a tolerance fitted to one fixture's measured number would be fragile against any other fixture), matching the 1.37x headroom ratio `runLutBakeTest`'s own `kResidualTol = 2e-3` used over its measured 1.46e-3. **B6 is proven in both directions, not assumed.** Precision: two pixels whose sRGB-encoded values are 0.5010 and 0.5030 -- both inside 8-bit code 128's bucket, `[127.5/255, 128.5/255) = [0.50000, 0.50392)`, with margin on each side -- come back as the *bytewise identical* sample after an 8-bit export and stay distinct (codes 32833 and 32964, 131 apart) after a 16-bit one, with each 16-bit value additionally checked to return at its own individually correct level rather than merely "different from the other", and their surviving difference checked to exceed the 16-bit quantization step by orders of magnitude. Loud failure: the JPEG/TGA/BMP 16-bit refusals have their **error strings actually inspected** for the format name, "16-bit", "8 bits per channel" and the suggested alternative -- not merely "returns false" -- with a PNG control proving the refusal is about the format's limit and not a blanket "16-bit never works". The file's own IHDR bit-depth byte (offset 24 in every PNG) is read directly for both depths, so "16-bit" is a property of the bytes rather than of this module's bookkeeping. **I5 is proven**: the same Document exported to all three target spaces yields three pairwise-different byte vectors, and each file's *literal* 0..1 sample is recovered (as `srgbEncode()` of the decoded value, since `decodeImageLinear()` always sRGB-decodes) and checked against `color/Space`'s own `srgbEncode(0.5)` / `rec709Encode(0.5)` -- including the explicit assertion that `Rec709Linear` writes 0.5 verbatim and is emphatically **not** srgbEncode(0.5) ~ 0.735, i.e. the no-encode option is verifiably not silently sRGB-encoding, plus a check that sRGB and BT.709 land on genuinely different samples so the two curves are not being conflated because their primaries match. **Premultiply correctness** is checked against the tile's own raw premultiplied storage divided by its own stored alpha (`runProbeTest()`'s discipline -- not "a plausible-looking number"), with the exported red additionally checked to differ from the raw premultiplied value (so passthrough could not pass), the `a <= 0` guard exercised on a never-painted texel, and an alpha=1 control. Also covered: all four I1 formats encoding and decoding back (the three lossless containers at the derived 8-bit tolerance, JPEG at a separately-labelled looser one, since calling it lossy and then holding it to a lossless tolerance would be dishonest); `flattenDocumentToLinear()`'s canvas clipping and multi-layer sum; a `createBlank()`'d document with zero painted tiles exporting successfully as fully transparent (a blank canvas is a legitimate thing to export, not an error) while a zero-sized one fails with a specific message; and `exportDocumentToFile()` producing a file byte-identical to `exportDocument()`'s in-memory bytes (one encode path, not two) and, on a refused request, forwarding the encode's own error while leaving **no file behind** -- nothing is opened until the encode has fully succeeded, so a refusal can never leave a truncated file. The test removes its own scratch files. **Full `--selftest` independently re-verified from a `rm -rf build`-and-reconfigure clean rebuild** (`cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j`): zero build errors; **zero warnings from any file under `src/`** (checked by grepping the whole build log for `warning:` lines naming a `naturalPaint/src/` path -- count 0), the only warnings anywhere being the pre-existing `stb_image_write.h` `sprintf` deprecation (relocated, as noted above), the SDL3 `_deps/` macOS deprecation noise, and the pre-existing `ld: ignoring duplicate libraries` note. All **26** `--selftest` sections pass (up from step 8's 25 -- the new export section), **0 FAIL, exit 0**, idle RSS **63.2 MB** (ceiling 80 MB) -- unaffected, as expected, since every new symbol here is pure CPU with no persistent allocation. `src/CMakeLists.txt` gained exactly one new line, `io/Export.cpp`, registered the same way `io/ImageIO.cpp` already was -- confirmed by grepping the existing pattern first rather than assumed. **Deliberately not done, and not a gap in this step**: no File > Export menu item. PLAN.md step 7 ("Export As -- format, space, depth **and resize**, with saveable presets") is where that UI and its presets belong, and this codebase still has no live-painting-canvas-to-`core::Document` bridge for such a menu item to export *from* -- the same gap steps 8/10/13's and Phase 3 step 6's own Findings rows already record. This is the Document-level operation underneath that step, exactly as `placeImageAsLayer()` was for step 13. No OpenImageIO reference of any kind (step 2's territory, and unavailable here) and no new third-party dependency: everything runs against the already-vendored `third_party/stb_image_write.h`, per PRD I1's "no optional dependency". Nothing outside this step's scope was touched -- `color/Space`, `core/Document`, the grading pipeline and the interactive UI are all unmodified. Unblocks step 2's OIIO backend, which slots in behind the same `ExportFormat`/`ExportTargetSpace`/`ExportBitDepth` vocabulary, and step 7's "Export As", which is this operation plus resize plus presets plus a dialog. |
| 2026-08-18 | **Phase 3 step 8 ("Op-stack UI -- reorder, toggle, delete, and a curve widget operating in the shaper domain"): PASS. Last step of Phase 3.** New `src/app/CurveEdit.hpp`/`.cpp` -- pure screen<->curve-space geometry (`curveToPlot()`/`plotToCurve()`, with the y-flip curve-space-up-vs-screen-space-down needs) and list mutation (`hitTestPoint()`, `insertPoint()`, `movePoint()`, `removePoint()`) over `ops/PointOps.hpp`'s `Curve`/`CurvePoint`, zero ImGui/GPU includes -- mirroring `app/Snapping.hpp`'s own split from Phase 2 step 12 exactly, including its "pure math, no ImGui/GPU dependency -- this is the piece... `--selftest` can actually exercise headlessly" framing verbatim in the new header's doc comment. `ui/MacPaintUI.cpp` gained a new "GRADE" section in the existing `##controls` panel (same `TextUnformatted`/`Separator`/`Dummy(0,8)`/tooltip idiom as `BRUSH`/`PIGMENT`/`OIL`/`INK`/`WATER`/`GRID`/`SOLVER` above it -- no new floating window, no new layout math) implementing all of PLAN.md's literal step-8 wording: a `Checkbox("Preview Graded Output", &st.view.grade)` at the top; an add control (a six-way `Combo` over `PointOpKind` plus a `+ Add` button, `OpClass::PointA`, every other field left at its struct default -- already the identity op, no extra reset logic needed); one row per op in stack order with an enable `Checkbox` (routed through `OpStack::setEnabled()`, never `Op::enabled = ...` directly -- see the version-bump note below), a kind-name label, `Up`/`Down` buttons (`OpStack::reorder(i, i-1)`/`reorder(i, i+1)`, `BeginDisabled`-greyed at the list's own ends) and a `Delete` button (`OpStack::remove(i)`); and a per-kind inline editor. **Every mutation goes through `OpStack`'s own mutators, never a direct field write**: each row copies `Op op = st.opStack.at(i)` (a *value*, not a reference) at the top of its iteration specifically because `reorder()`/`remove()` erase-and-insert into `OpStack`'s internal `std::vector<Op>`, which can invalidate references to later elements -- holding a reference across one of those calls and reading it again later in the same iteration would be exactly that bug. A `structureChanged` flag stops the loop the same frame a reorder/remove fires (standard immediate-mode practice: the list reflects the new state from the next frame on) rather than continuing to render rows against indices that no longer mean what they did a moment ago. Per-kind editors mutate the local `op` copy and write back via `OpStack::setOp(i, op)` only when something actually changed -- confirmed load-bearing, not cosmetic: `OpStack` doesn't expose a non-const reference to mutate in place even if a caller wanted to, and `updateGradePreview()`'s rebake gate depends entirely on `version()`, which only the five mutators bump. **Per-kind scope, deliberately narrow, per PLAN.md's own wording** ("a curve widget", singular, naming only Curves): Exposure gets one `SliderFloat("Stops", -5..5)`; Saturation one `SliderFloat("Scale", 0..2)`, `lumaWeights` left at `kRec709LumaWeights`; Levels gets ONE shared 5-slider `LevelsParams` editor (black/white in, gamma, black/white out) applied identically to `levels[0..2]` on any change, per step 2's own "a composite levels adjustment is just the caller passing the same LevelsParams for all three channels, not a separate code path" -- per-channel authoring stays out of scope; Grayscale and Channel Mixer get add/toggle/reorder/delete only, no weight/matrix editor (both fully functional at their struct defaults -- `kRec709LumaWeights`, the identity 3x4 -- bake and grade correctly, just without a bespoke editor), documented inline as a deliberate cut rather than a silent gap, not a missing feature. **Curves -- the one PLAN.md names explicitly -- got the real interactive widget**: three `SmallButton` channel tabs (R/G/B, selection persisted per-op-row via `ImGui::GetStateStorage()` keyed off the row's own `PushID(i)` scope -- UI-only state, no new `AppState` field, exactly the same non-persistent-session-state treatment `guides`/`view.zoom` etc. already get) above a 200px square `ImDrawList` plot: the spline is drawn by sampling the *real* `evalCurve()` (the same function the grading pipeline runs, not a separate approximation) at 64 steps, control points as filled circles, click-empty-area-to-add / drag-to-move / right-click-to-delete. **Double-click vs. right-click, decided in favour of right-click**: PLAN.md's own wording offered either ("double-click (or right-click, your call)"); right-click was chosen because Dear ImGui's own documented behaviour -- "a double-click will also report `IsMouseClicked() == true`" -- means a double-click's second click fires as an ordinary left click one event earlier, which would insert-then-immediately-delete a point when double-clicking empty plot area under the click-to-add handler, a real interaction bug rather than a hypothetical one (worked through by hand before deciding against it); right-click has no such overlap with the left-click add/drag gestures and needed no special-casing, and this codebase already has an established right-click-to-delete precedent to match (`ui/MacPaintUI.cpp`'s existing right-click-near-a-guide deletion in the canvas block, `hovered && IsMouseClicked(ImGuiMouseButton_Right)`, matched verbatim rather than inventing a second idiom). **Insertion capped at `kMaxCurvePointsPerChannel` (16, `color/LutBake.hpp`)**, a scope decision not explicitly asked for but found necessary while reading `LutBake.cpp`: its GPU kernel silently truncates any longer curve to its first 16 points when baking (with a stderr warning), so an uncapped widget would let the plotted spline (drawn via the full, uncapped `evalCurve()`) silently diverge from what grading actually bakes past 16 points -- capping insertion in the widget keeps the preview and the baked result in agreement by construction; existing points beyond the cap can't arise (insertion is the only way to grow a curve) so no separate handling was needed for that case. A `"%d / %d points"` counter under the plot makes the cap visible rather than a silent ceiling. **New ops start disabled** (a documented, deliberate choice, not the only defensible one): mirrors `CanvasView::grade` itself never auto-enabling just because the stack is non-empty -- adding an op should never itself change what's on screen, only enabling it should. **Step 6 debug scaffolding removed, not left alongside the real UI**: `main.cpp`'s `AppState` construction no longer seeds the two hardcoded `Saturation`/`Exposure` debug ops (`st.opStack` now starts empty -- the new GRADE section is how a user populates it); `ui/MacPaintUI.cpp`'s View-menu "Test Grade (debug)" item (which hardcoded indices 0/1, an assumption no longer safe once ops can be freely added/removed/reordered) is deleted outright, not kept alongside the new checkbox -- exactly one way to toggle `view.grade` now (the "Preview Graded Output" checkbox at the GRADE section's top), per this step's own instruction that there be no ambiguity between an old and a new toggle path; `AppState.hpp`'s `CanvasView::grade` doc comment rewritten to describe this real role instead of the retired debug toggle. Confirmed no other code anywhere still assumes the old fixed indices (grepped `opStack` across `src/` after the change). **New `--selftest curve edit` section** (`runCurveEditTest`, 32 assertions, wired into `main.cpp`'s `ok` chain immediately after `applyPassOk` -- checked it actually appears in the output before reporting this done, per this task's own explicit warning that a written-but-unwired test is a real mistake this session already made once for `runPointOpsTest`): `curveToPlot()`/`plotToCurve()`'s y-flip and round trip including the `plotSize<=0` divide-by-zero guard; `hitTestPoint()`'s exact/inside/outside-radius cases, an always-missing empty curve, and a hand-built equidistant-tie case pinning the documented earlier-index-wins rule; `insertPoint()` fed five points in scrambled (not ascending, not descending) order with ascending-`.x` order checked after *every* insertion, not just at the end, plus an out-of-`[0,1]` clamp case; `movePoint()`'s neighbour-crossing re-sort (checked both that the crossed neighbour ends up on the opposite relative side and that the moved point lands at its exact new `(x,y)`), a non-crossing move's order-preservation, an out-of-`[0,1]` clamp case, and an out-of-range index throwing `std::out_of_range` (matching `core::OpStack`'s own bounds-checked-mutator convention, called out explicitly since `CurveEdit.hpp`'s functions follow the identical `std::vector::at()` discipline); `removePoint()`'s index-shift and its own out-of-range throw; and the 0/1-point degenerate cases `evalCurve()` documents as identity, checked two ways -- by `Curve::size()` (this module's own bookkeeping) and by feeding the `CurveEdit`-built curve straight into the real `evalCurve()` as an oracle, proving the two modules agree rather than merely that `CurveEdit`'s sizes look plausible. **Full `--selftest` independently re-verified from a `rm -rf build`-and-reconfigure clean rebuild** (`cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j`): zero build errors, zero warnings from any file touched this step (the only warning anywhere in the log is `stb_image_write.h`'s pre-existing `sprintf`-deprecation notice, unrelated, present before this step); all 25 `--selftest` sections pass (up from step 6's 24 -- the new curve-edit section), 0 FAIL, exit 0, idle RSS 64.0 MB (ceiling 80 MB) -- unaffected, as expected, since every new symbol here is either pure CPU (`CurveEdit`) or ImGui immediate-mode drawing with no new persistent GPU/heap allocation. `src/CMakeLists.txt` gained exactly one new line, `app/CurveEdit.cpp`, registered the same way `app/Snapping.cpp` already was -- confirmed by grepping the existing pattern first rather than assumed. **Interactive verification, partial, honestly reported, the same limitation every UI-facing step this session has hit**: launched the freshly built interactive binary directly (not through `--selftest`); it started cleanly with empty stderr/stdout and ran a steady, continuing render loop (~11-22% CPU across repeated checks, RSS ~86 MB, both consistent with a live GPU/window/render-loop process rather than a crash or a hang) for several seconds, then was terminated cleanly via `kill -TERM` with no orphaned process left behind. Could not go further: this sandboxed shell's `screencapture` only captures the desktop wallpaper (not the app's actual window) and `osascript`/System Events input simulation is denied (`-25211`, no assistive-access permission -- a system/security setting this task correctly does not attempt to grant itself), so the new GRADE section's click/drag/right-click behaviour, the curve plot's visual output, and the op-row layout could not be visually confirmed end-to-end in the running app beyond "it starts and renders without error." This is the identical, already-documented limitation step 6's and step 11's own Findings rows record for the same reason; no click-and-see result was fabricated to paper over it -- the `--selftest` coverage above is what stands in for it, the same tradeoff `app/Snapping.hpp` made for Phase 2 step 12. Depends on the already-landed `core/OpStack` (step 5), `ops/PointOps` (steps 2+3), `color/Shaper` (step 1) and the Apply pass (step 6); this closes out Phase 3 -- no further steps remain in this phase. |
| 2026-08-18 | **Phase 3 step 6 ("Apply pass -- shaper -> 3-D LUT fetch -> un-shape"): PASS.** New `shaders/grade_blit.wgsl`; `sim::PaintSim` gained `updateGradePreview()`/`gradedView()`/`readbackGraded()` plus `graded_`/`gradedView_`/`gradePipeline_`/`lut_`/`hasBakedLut_`/`lastBakedOpStackVersion_`, mirroring the existing grayscale-preview subsystem's exact shape throughout (same texture-creation site in `allocFields()`, same pipeline-build/hot-reload site in `buildPipelines()`, same release path in `releaseFields()`/`shutdown()`). **Deliberate target: sim::PaintSim's live canvas_, not core::Document/TiledDocumentView** -- this fork was presented explicitly and decided in favour of the live canvas, a conscious scope narrowing recorded here so it isn't second-guessed later: it grades exactly what the painter currently sees, not a linear scene-referred `core::Document`, because no live-canvas-to-`core::Document` bridge exists in this codebase yet (the same gap steps 8/10/13's own Findings rows already hit, restated once more in `PaintSim.hpp`'s and `AppState.hpp`'s new comments rather than silently assumed). **Domain-contract decision, the load-bearing one**: `color::Shaper`/`color::LutBake`'s design assumes scene-linear HDR input (ADR-0004: "linear -> shaper -> 3-D LUT -> linear"), but `composite.wgsl`'s actual output is not that -- verified by reading all three of its return paths, every one ending `vec4<f32>(clamp(rgb, vec3<f32>(0.0), vec3<f32>(1.0)), 1.0)`, an already-clamped-to-[0,1] stylized paint-appearance value. `grade_blit.wgsl` still follows step 6's literal "shaper -> 3-D LUT fetch -> un-shape" wording as a numerical contract regardless, treating that clamped RGB as shaperEncode's "linear" input. Documented consequence, not a bug: since canvas RGB never exceeds 1.0, `shaperEncode(1.0) ~= 0.5547945` caps the shaper-domain coordinate this pass ever samples at -- only the lower ~55% of the LUT's `[0,1]^3` domain is ever reached by this pass, a direct, accepted result of grading the SDR live canvas rather than a linear document. **Single-run narrow scope**: `updateGradePreview()` bakes and blits only `opStack.detectRuns().front()` (identity/empty ops when `detectRuns()` returns none), documented explicitly as narrow rather than silently assumed -- correct today because nothing in this codebase constructs a real non-`PointA` op yet (per `core/OpStack.cpp`'s own comment, `SpatialB`/`StrokeC`/`BakedD` are selftest-only placeholders), so there is at most one run in practice; a future step revisits this if that changes. The ordered `std::vector<Op>` fed to `bakeLut()` is built by looping the run's raw `[startIndex, endIndex)` range and calling `OpStack::at(i)`, per `LutBake.hpp`'s own documented calling convention -- not `run.ops`, which is already-composed closures `bakeLut()` has no way to run on the GPU. Rebake gate keyed off `OpStack::version()`, exactly as `LutBake.hpp`'s header comment anticipated for "a future caller" (this one); a `bakeLut()` failure (null `Lut3D.texture`) leaves `hasBakedLut_` false and returns without blitting, rather than binding a null texture. The sampler is the existing `linear_` member (built in `init()` with `addressModeW` already set, i.e. built with 3-D use anticipated) -- reused as-is, no second sampler created. **Debug test-grade toggle, explicitly a temporary placeholder for step 8, not the real op-authoring UI**: `main.cpp` seeds exactly two fixed, initially-disabled ops (`Saturation` scale 0.3, `Exposure` +0.5 stops) into the new `AppState::opStack` member immediately after constructing `AppState`; `ui/MacPaintUI.cpp`'s View menu gained a "Test Grade (debug)" item (a menu item, not a checkbox -- the existing "Grayscale Preview" item it sits beside is itself a `MenuItem` bound to a `bool*`, not an `ImGui::Checkbox`, despite the brief's wording; matched the real existing pattern rather than the brief's literal description) whose click handler calls `st.opStack.setEnabled(0, st.view.grade); st.opStack.setEnabled(1, st.view.grade);`, so toggling it on/off enables/disables both fixed ops together for one clearly visible combined effect (desaturate 70% + brighten half a stop). Without this, nothing in the interactive app could construct a non-empty `OpStack` before step 8 ships its real reorder/toggle/delete/curve-widget UI, and the Apply pass would stay invisible in the running app exactly like steps 1-5 and 7 currently are. View-selection precedence in `MacPaintUI.cpp`'s canvas block is now grade > grayscale > plain canvas, mutually exclusive by design (composing both isn't a normal use case and isn't required by this phase's Verify criterion). **`AppState.hpp`**: `CanvasView::grade` (mirrors `grayscale`'s own comment shape) and a new top-level `AppState::opStack` (`core::OpStack`, comment mirrors `guides`' own "lives here because there's no live-canvas-to-Document bridge yet" reasoning verbatim, since it's the identical gap) -- not inside `CanvasView`, since ops are document-adjacent content, not view state, per that struct's own header comment. **New `--selftest apply pass` section** (`runApplyPassTest`, wired into `main.cpp`'s `ok` chain immediately after `lutBakeOk`): bakes a one-op `Saturation(scale=0.3)` LUT and blits it over a known, deterministic unpainted canvas (blank paper substrate only -- `sim::PaintSim`'s procedurally generated `paper_` field is fixed for the sim's lifetime, so this is a real, reproducible value to check against, not one the test gets to dictate), then checks six hand-picked canvas pixels' graded readback against an independent CPU reference. **Methodology deviation from the brief, reasoned and deliberate**: the brief's suggested technique -- choosing canvas pixels whose `shaperEncode()` lands very close to an exact LUT grid coordinate, so a nearest-grid-cell CPU reference approximates the true trilinear sample -- turned out to not be achievable to the needed precision here: unlike `runLutBakeTest()` (which bakes a LUT directly and can pick *any* grid cell to check), `sim::PaintSim`'s `canvas_` is only reachable through the real solver/composite pipeline, so there is no way to force an arbitrary, precisely grid-aligned byte value onto it -- `clearCanvas()` gives exactly one fixed value (0,0,0), and hand-checking composite.wgsl's substrate formula showed the closest the untouched paper substrate ever gets to a grid-cell centre is roughly 1.5e-3 in shaper-domain distance, not negligible against the target tolerance. Built a full CPU-side trilinear interpolation reference instead (`texelAt()` fetches each of the 8 corner LUT texels via the same half-precision ping-pong simulation `runLutBakeTest()`'s own `simulateBakeCpu()` uses; `trilinearSample()` blends them with the identical `ClampToEdge`, single-mip-level formula `textureSampleLevel()` itself evaluates) -- exact for *any* input coordinate rather than an approximation that only holds near a grid line, which still "eliminates the variable you're not testing" (interpolation-weight uncertainty is computed exactly, not left to leak into the residual as unmodelled error), just by a different, equally rigorous route. **Tolerance, measured empirically, not guessed**: temporary instrumentation printed the actual GPU-vs-CPU-reference residual at all six hand-picked points (18 channel comparisons) before landing -- max was `4.37e-3` (pixel (204,921), R channel), with most other points/channels in the 1-2e-3 band matching `runLutBakeTest`'s own dominant half-quantization term; the outlier is explained by this pass's added `RGBA8Unorm` quantization on both `canvas_` (source) and `graded_` (destination), a source `runLutBakeTest`'s own comparisons don't have (that test's LUT-to-LUT comparisons never cross an 8-bit-unorm boundary). Landed `kApplyResidualTol = 6e-3`, the same ~1.37x headroom ratio `runLutBakeTest`'s own `kResidualTol = 2e-3` used above its measured 1.46e-3 worst case. Also extended `runViewTransformTest()`'s existing "toggling grayscale never mutates canvas_" block (c) to cover `view.grade`/`updateGradePreview()` the same way, with an empty (identity-bake) `OpStack` -- that block's job is only "never touches canvas_", not grading correctness, which `runApplyPassTest()` already covers. **Full `--selftest` independently re-verified from a from-scratch clean reconfigure and rebuild** (`build_clean`, deleted after verifying): all 24 sections pass, 0 FAIL, idle RSS 64.1 MB (ceiling 80 MB) -- unaffected as expected, since `graded_`/`gradedView_` are the same order of size as `grayscale_`'s own texture and the 32³ rgba16float LUT is ~256 KiB, trivial against the ~64 MB baseline. **No `CMakeLists.txt` changes were needed, confirmed rather than assumed**: no new `.cpp` files (only existing files modified) and `shaders/grade_blit.wgsl` needed no registration, since `NP_SHADER_DIR` already points at the source `shaders/` tree read at runtime, matching every prior WGSL file this session -- the build succeeded without touching `CMakeLists.txt` at all, which is the actual proof, not an assumption. **Interactive verification, partial, honestly reported**: launched the freshly built interactive binary; it starts cleanly (empty stderr/stdout) and runs a continuous render loop (~11% CPU, matching the profile of a second, separately pre-existing instance of the same app already running on this machine) -- consistent with correct GPU/window/pipeline initialisation, including the new `gradePipeline_` build. Could not go further: this sandboxed shell's `screencapture` does not surface the app's actual window (only the desktop wallpaper, for either instance), and `osascript`/System Events input simulation is denied (`-25211`, no assistive-access permission, which this task correctly does not grant itself since that would be a system/security-setting change). This is the same "practical limit of dynamic verification available without a tool to drive this native window's mouse/keyboard from outside it" step 11's own Findings row already hit; did not fabricate a click-and-see result to paper over it. Depends on the already-landed `color/LutBake` (step 4), `core/OpStack` (step 5), and `ops/PointOps`/the shaper (steps 1-3); unblocks step 8's real op-authoring UI, which can replace the debug toggle wholesale once it exists. |
| 2026-08-18 | **Phase 3 step 4 (`color/LutBake`, "bake a maximal run of adjacent point ops onto a 32³ grid via one compute dispatch"): PASS.** New `src/color/LutBake.hpp`/`.cpp` plus eight new WGSL files (`shaders/include/shaper.wgsl`, `shaders/lut_seed.wgsl`, `shaders/lut_op_{levels,curves,exposure,saturation,grayscale,channel_mixer}.wgsl`) — the first 3-D texture anywhere in this codebase (`WGPUTextureDimension_3D`, `{32,32,32}`, `rgba16float`, `TextureBinding\|StorageBinding\|CopySrc`). **Namespace correction to the brief**: the brief called for `color::Shaper`/`color::LutBake` as a nested C++ namespace; `color/Shaper.hpp` (already landed, step 1) is actually flat `namespace np` like every other module in this codebase — `color/`, `core/`, `ops/` are directory groupings only, never C++ namespaces. `LutBake` follows the same flat `np::` convention (`np::bakeLut`, `np::Lut3D`, `np::kLutSize`), not a new `np::color::` namespace. **`OpRun`-vs-`std::vector<Op>` API: resolved as option (b), `OpStack` itself untouched.** `core::OpStack::at(size_t)` already returns the original `Op` (kind + params) by index, read-only — no new accessor was needed. `bakeLut(GpuContext&, const std::vector<Op>&)` takes the ordered op list directly; a future Apply-pass caller (step 6, not built here) slices `OpStack` by an `OpRun`'s `[startIndex, endIndex)` with a trivial loop over `at(i)`. `bakeLut()` itself skips any entry with `!enabled` or `opClass != PointA` (contributing no GPU pass, matching `OpRun::ops`'s own "a disabled entry contributes nothing" rule), so a caller may hand it either an already-filtered list or a raw index-range slice with disabled/non-PointA entries still present — it doesn't have to replicate `OpRun`'s filtering logic first. This keeps `core/OpStack.hpp`/`.cpp` completely unmodified (confirmed unchanged by diff; its own `--selftest op stack` section still passes unmodified, see below). **Why N sequential small dispatches, not one big interpreter dispatch, despite the plan's literal "via one compute dispatch" wording**: a single generic shader branching on a per-texel op-kind tag read from a heterogeneous storage buffer would be a brand-new pattern (no storage-buffer-of-structs exists anywhere else in this codebase), harder to verify (one shader doing six things vs. six shaders each directly comparable 1:1 against their `ops/PointOps.cpp` counterpart), and doesn't help where the phase's actual Verify criterion lives: "twelve stacked grade ops cost the same as one at draw time" is about the **Apply pass's** per-frame draw-time cost (an O(1) LUT sample, step 6, not built here), not the occasional bake-time cost that only happens on parameter change and can reasonably scale with run length. Implemented as one seed compute pass (writes texel `(x,y,z)` as shaper-domain coordinate `((x+0.5)/32,...)` directly — no shaperDecode/Encode needed for the seed itself) followed by one dispatch per op, ping-ponging between two 32³ textures exactly the way `sim/PaintSim.cpp`'s Jacobi solve already ping-pongs two 2-D textures within one compute pass encoder (same "sequential dispatches, no explicit barrier needed between them" pattern, confirmed safe by that solver's own 40-iterations-per-frame precedent) — one dimension higher, same idiom. `@workgroup_size(4,4,4)` dispatched as `(8,8,8)`: `32/4 == 8` exactly, no remainder handling needed anywhere. **The LUT holds a shaper-domain value at every ping-pong stage, not linear** — this is what makes the per-op-kernel shape uniform: five of six kernels (Levels/Exposure/Saturation/Grayscale/ChannelMixer) wrap their own linear-domain math in an outer `shaperDecode -> op -> shaperEncode -> clamp01`, converting to linear for the op and back for storage. **Curves is the one exception, by design, not oversight**: ADR-0004 authors curve control points *in* the shaper domain, and the texel is already a shaper-domain value, so `lut_op_curves.wgsl`'s kernel runs `evalCurveGpu()` directly on the texel with no encode/decode wrapper at all — wrapping it the way `ops::applyCurves()` does (which has its own internal shaperEncode/evalCurve/shaperDecode, built for a linear-in/linear-out contract) would decode-then-immediately-re-encode for no reason. Verified this is a precision-only distinction, not a different mathematical result, before relying on it (see tolerance derivation below). **Uniform-buffer packing**: every op kernel's params buffer is built from 16-byte (`vec4<f32>`) chunks specifically to sidestep WGSL's uniform-address-space scalar-packing subtleties — Levels: 4×`vec4` (`chR`/`chG`/`chB` = `{blackIn,whiteIn,gamma,blackOut}`, `whiteOut` = `{Rw,Gw,Bw,unused}`, 64 bytes); Exposure/Saturation/Grayscale: a single `vec4<f32>` each (16 bytes, mostly-unused lanes accepted for simplicity over a tighter but riskier scalar layout); ChannelMixer: 3×`vec4` rows (48 bytes). Every C++-side struct carries a `static_assert(sizeof(...) == N)` pinning it against its WGSL counterpart. **Curves' storage buffer is the one narrow exception to "small uniform buffer per op," and deliberately not a precedent for a general op-interpreter**: WGSL's uniform address space forces array elements to a 16-byte stride (the classic std140 quirk), which would waste half of every packed `vec2<f32>` curve point or force explicit `vec4` padding; a **read-only storage buffer** has no such requirement (`array<vec2<f32>>` packs at its natural 8-byte stride), so `lut_op_curves.wgsl` binds one instead — 4×`u32` counts (`countR/countG/countB/pad`) plus a flat `array<vec2<f32>,48>` (R then G then B, each padded to a new `kMaxCurvePointsPerChannel = 16` slots), 400 bytes total, `static_assert`-pinned including `offsetof(points) == 16`. This is a single, self-contained, easy-to-justify use (one buffer, one binding, read-only, no dynamic op-kind tagging), not a generalization of the storage-buffer idiom to every op — the other five kernels still use plain uniforms. `evalCurveGpu()` in WGSL is a line-by-line port of `ops/PointOps.cpp`'s `evalCurve()`/`tangentAt()`/`secantSlope()` — same degenerate 0/1-point identity, same flat extrapolation, same Catmull-Rom-adapted Hermite tangents, same linear segment-scan loop condition. **Tolerance derivation for the GPU-vs-CPU `--selftest` comparisons, measured empirically, not guessed**: the real bake round-trips every shaper-domain value through `rgba16float` storage once per ping-pong write (once for the seed, once per op pass) — half's own ~2⁻¹¹ relative quantization step is the dominant, *expected* source of GPU-vs-CPU disagreement, so comparing against raw float math (this codebase's usual `1e-4`) would compare against a value the real bake could never produce. The test's `simulateBakeCpu()` mirrors the pipeline stage by stage, applying `core::floatToHalf`/`halfToFloat` at each write, exactly matching the number of half round-trips the value actually undergoes. Measured directly before landing (temporary instrumentation, not left in the shipped file): max delta at any sampled channel across every single-op case was `4.88e-4` — exactly one half ULP at unity magnitude (`2^-11`), confirming the dominant term really is quantization; the composed 3-op run (the deepest chain tested, seed + 3 writes) topped out at `1.46e-3`. A second check — comparing against a reference that does *not* simulate the half round-trips — produced deltas of the same order (up to `1.08e-3`), not dramatically larger, because the hand-picked sample points don't hit a steep-tangent region where quantization would compound; the half-simulated reference is still used since it's the theoretically correct comparison (bit-for-bit what the GPU actually computes at each write). Landed `kResidualTol = 2e-3`, comfortable but not loose headroom above the observed `1.46e-3` worst case. New `--selftest lut bake` section (73 checks): each of the six op kinds baked alone (6 grid cells each) against `shaperEncode(clamp01(op(shaperDecode(gridCoord))))` computed with the real `ops::PointOps` functions and the same params fed to the GPU; Curves specifically at three shapes (2-point straight line, 3-point interior, exactly `kMaxCurvePointsPerChannel=16` points); a composed 3-op run (Exposure → Saturation → Channel mixer) baked in one `bakeLut()` call and checked against running the same sequence through `ops::PointOps` directly in order on the CPU — the GPU analogue of `runOpStackTest()`'s own composition proof, and the one case that actually exercises ping-pong sequencing rather than any single kernel in isolation; and an empty-ops run confirming the seed pass alone is already a valid identity LUT. New `readbackRGBA16F3D()` helper in `app/SelfTest.cpp`, a 3-D generalization of the existing `readbackRGBA16F()` — copies the whole 32×32×32 volume in one `wgpuCommandEncoderCopyTextureToBuffer` call (`copySize.depthOrArrayLayers=32`, `layout.rowsPerImage=32`) rather than 32 separate per-layer 2-D copies; `32 texels × 8 bytes/texel (rgba16float) = 256 bytes/row` exactly, satisfying the alignment requirement with zero padding, the same convenient coincidence the existing 2-D helper's callers already lean on. Every baked `Lut3D` is released via `releaseLut3D()` immediately after each test case (matching `runTiledViewportTest()`/`runMipPyramidTest()`'s own cleanup-before-idle-RSS-check convention) — the two 256 KiB ping-pong textures never accumulate. Full `--selftest` independently re-verified from a from-scratch `rm -rf`-equivalent clean reconfigure and rebuild (`build_clean`, deleted after verifying): all 23 sections pass, 0 FAIL, idle RSS 63.7 MB (ceiling 80 MB) — unaffected, as expected (two 256 KiB LUT textures, released before the idle-RSS check runs, are trivial against the ~63 MB baseline). Depends on the already-landed `ops/PointOps` (steps 2+3), `color/Shaper` (step 1) and `core/OpStack` (step 5); unblocks the still-unbuilt Apply pass (step 6, "shaper → 3-D LUT fetch → un-shape") and the curve-widget UI (step 8), neither built here. |
| 2026-08-18 | **Phase 3 step 5 (`core/OpStack`, "ordered ops, dirty tracking, run detection for the collapse"): PASS.** New `core/OpStack.hpp`/`.cpp`. `Op` is a kind tag plus fields only meaningful for that kind — deliberately `core/Layer.hpp`'s own established idiom, not a `std::variant` (this codebase has no `std::variant` use anywhere yet, and one here for six point-op kinds carrying per-channel arrays would be a bigger style deviation than reusing the fat-struct pattern `Layer` already set): `OpClass opClass` (default `PointA`), `bool enabled` (default `true`), `PointOpKind pointKind`, then one field per point-op kind (`std::array<LevelsParams,3> levels`, `std::array<Curve,3> curves`, `ExposureParams exposure`, `SaturationParams saturation`, `GrayscaleParams grayscale`, `ChannelMixerParams channelMixer`) — only the field matching `pointKind` is meaningful, and only when `opClass == PointA`, the exact "populated only for the relevant kind" convention `Layer::rgbTiles` documents for itself. `OpClass` carries `docs/operations.md` §1's own four-letter taxonomy (`PointA`/`SpatialB`/`StrokeC`/`BakedD`) as a real, general enum even though only `PointA` has working ops anywhere in this codebase today — `SpatialB`/`StrokeC`/`BakedD` exist purely so `detectRuns()`'s run-boundary logic is written against "is this class A" rather than hard-coded to "everything is always class A" in a way a later phase would have to rewrite once a real class-B/C/D op exists; nothing constructs one with meaningful data anywhere in this codebase, only as a `--selftest` fixture to exercise run-splitting. A private free function (`toPointOp()`, anonymous-namespace, `ops/PointOps.cpp`'s own precedent for small internal helpers) binds one `PointA`-classed `Op`'s params into a callable `ops::PointOp` — the bridge `detectRuns()` uses to build each run's composed list; documented precondition, never called on a non-`PointA` entry. `OpRun{startIndex, endIndex (exclusive), std::vector<PointOp> ops}` — `ops` holds only the *enabled* `PointA` entries in the run, in order, already bound and ready for `ops::applyPointOpsPremultiplied()` (or the still-unbuilt `color/LutBake`) with no further lookup. `OpStack` holds `std::vector<Op>` plus a `uint64_t version_`; `size()`/`at()` (bounds-checked via `std::vector::at`, throws on misuse) are read-only and never touch `version()`; every mutator (`add` returns the new index, `remove`, `reorder(from,to)` — "move an element to a new index" semantics, `setEnabled`, `setOp`) increments `version()` unconditionally, including a call that changes nothing observable — documented as a deliberate asymmetry, not an oversight: the only consumer is a future `color/LutBake` comparing versions across frames to decide whether to rebake, for which an occasional spurious rebake is cheap and harmless, while under-incrementing and missing a real change would silently leave a stale grade on screen, the far worse failure. **`detectRuns()`'s one genuinely subtle rule, called out explicitly because it is easy to get backwards**: a *disabled* `PointA` entry does NOT split a run — op *class*, not *enabled state*, defines a run boundary, because a disabled op is the identity function on whatever runs through it, exactly equivalent to "not present" for LUT-baking purposes; splitting on it would force an unnecessary run break (and rebake) every time a user temporarily toggles an op off, defeating the whole point of a maximal-run collapse. The disabled entry still occupies its slot in `[startIndex, endIndex)`; it is simply excluded from `.ops`. New `--selftest op stack` case (35 assertions): an empty stack (`detectRuns()` empty, `version() == 0`); every mutator bumps `version()` by exactly one including a same-value `setEnabled()` no-op, while `at()`/`size()` never do; an all-`PointA` stack (Exposure then Saturation) collapsing into one run spanning `{0,2}` whose hand-run composed output matches calling `applyExposure()` then `applySaturation()` directly; a disabled `PointA` entry mid-stack leaving one undivided run over `{0,3}` whose `.ops` has only the two enabled entries, hand-verified to match skipping the disabled op entirely; a real class boundary (`PointA`, a `SpatialB` test-only placeholder, `PointA`) splitting into exactly two runs, `{0,1}` and `{2,3}`, each containing only its own side's op; `reorder()` — first proving Exposure (linear scalar multiply) and Levels with `gamma=2.0` (nonlinear `pow`) genuinely disagree by order (unlike two purely linear ops, e.g. Exposure+Saturation, which always commute with a uniform multiply — a trap the all-`PointA` case above deliberately avoided), then reordering and confirming the composed run flips from exposure-then-levels to levels-then-exposure; and `remove()` shifting a later run's indices down by one and dropping the removed op's effect from the composed output. Full `--selftest` independently re-verified after a full rebuild (`cmake --build build -j`): all 22 sections pass, 0 FAIL, idle RSS 63.7 MB (ceiling 80 MB, unaffected — pure CPU bookkeeping, no new persistent allocation). Depends on the already-landed `ops/PointOps` (steps 2+3) and `docs/operations.md`'s taxonomy; unblocks the still-unbuilt `color/LutBake` (step 4) and the apply pass (step 6), neither built here. |
| 2026-08-18 | **Phase 3 steps 2+3 (`ops/PointOps` + the premultiply wrapper, `docs/operations.md` §1.1, PRD B4): PASS.** New `src/ops/PointOps.hpp`/`.cpp` (new `ops/` directory) — the six committed P0 point ops as plain `std::array<float,3> -> std::array<float,3>` functions, deliberately never touching alpha and never clamping their output (matching `color/Shaper`'s own precedent), per ADR-0004's requirement that a later, unbuilt `color/LutBake` step can chain a maximal run of them per 3-D LUT grid cell. Exact formulas/constants, independently re-checkable: **Levels** — `t=clamp((in-blackIn)/max(whiteIn-blackIn,1e-6),0,1)`, `t=pow(t,1/gamma)`, `out=t*(whiteOut-blackOut)+blackOut`, applied per-channel (`applyLevelsChannel`) and composed three times for "composite" (`applyLevels`). **Curves** — authored in `color/Shaper`'s log domain (`shaperEncode -> evalCurve -> shaperDecode`); `evalCurve` is a cubic Hermite spline with Catmull-Rom tangents adapted for non-uniform x-spacing (interior tangent = average of adjacent secant slopes, endpoint tangent = the one adjacent secant slope), flat extrapolation outside the authored x-range, and 0/1 control points is identity with the shaper round-trip skipped entirely (not merely approximated). **Exposure** — `out=in*2^stops`, deliberately linear-light, not shaper-domain. **Saturation** and **grayscale** — `luma=0.2126*r+0.7152*g+0.0722*b`, the exact three literals `shaders/grayscale_blit.wgsl` already hardcodes, reproduced verbatim as `kRec709LumaWeights` and shared via one `computeLuma()` helper so the CPU grading path and the GPU display-preview pass can never numerically disagree; saturation is `luma+(in[c]-luma)*scale`, grayscale replicates `luma` to all three channels (confirmed against `core/Layer.hpp` that only `LayerKind::RGB` has populated tile storage today, so there is no single-channel target to write instead). **Channel mixer** — `out[i]=matrix[i][0]*r+matrix[i][1]*g+matrix[i][2]*b+matrix[i][3]`, a 3×4 row-per-output-channel matrix including the offset column. The premultiply wrapper (`applyPointOpsPremultiplied`) mirrors `core/Probe.cpp`'s `unpremultiply()` guard exactly (`a<=0 -> {0,0,0,0}`), runs a caller-supplied ordered `std::vector<PointOp>` (`std::function<rgb(rgb)>`) against the un-premultiplied straight colour with no ordering opinion of its own (that's `core::OpStack`'s job, unbuilt), then re-premultiplies with alpha left untouched — confirmed against `docs/operations.md` §1 first that no P0 op ever modifies alpha (channel mixer is 3 rows/RGB-only, grayscale replicates luma across RGB never alpha) before relying on that as the wrapper's invariant. **Caught a real test-design bug before it could stand as a false pass, not a code bug**: the first-drafted "neutral Levels params is a true no-op" `--selftest` case used an HDR input (1.7, above default `whiteIn=1.0`) and correctly FAILed, because the internal `t`-clamp's job is genuinely two-sided — it saturates *any* out-of-range input to `blackOut`/`whiteOut`, not only guarding `pow()`'s negative-base case as the task's own inline comment emphasizes. That saturation is correct, ordinary Levels black/white-point behaviour (every real levels tool clips outside its own range) and not the module's general "don't clamp output" policy being silently violated — fixed by re-scoping that assertion to in-range values and adding two new explicit assertions that pin the saturation behaviour itself as intended, plus a clarifying paragraph added to `PointOps.hpp`'s Levels doc comment so a future reader doesn't hit the same false assumption. New `--selftest point ops` case (34 assertions): each op's neutral/identity case; Levels' hand-computed non-trivial case and the below-blackIn-doesn't-NaN guard; Curves' 0/1-point identity, the 2-point-reduces-to-a-line property checked at five interior x values (not just endpoints, with the algebraic reduction — `h00-h10-h11 == 1-t`, `h10+h01+h11 == t` — verified by hand first), a hand-computed 3-point interior case, flat extrapolation on both sides, and the `(0,0)-(1,1)` shaper-domain-identity line round-tripping five linear values end to end through `shaperEncode`/`shaperDecode`; Exposure's ±1-stop double/halve; Saturation's scale=0/1/2 cases against Rec.709 weights computed independently in the test, not via `computeLuma()`; Grayscale's hand-computable pure-red case and a general case; Channel mixer's identity/swap/offset cases; and the premultiply wrapper's alpha≤0 short-circuit, the exact hand-worked partially-transparent example from the brief (premultiplied `(0.5,0,0,0.5) -> (1,0,0) -> +1 stop -> (2,0,0) -> (1,0,0,0.5)`), an alpha-never-altered check, and a two-op composition (exposure then saturation) matching manual hand computation. Full `--selftest` independently re-verified from a `rm -rf build` clean reconfigure and rebuild: all 22 sections pass, 0 FAIL, idle RSS 63.0 MB (ceiling 80 MB) — unaffected, as expected for pure CPU math with no new GPU/persistent allocation. Landed alongside the concurrently in-flight `core/Histogram` (step 7) work — touched only the usual shared append-only files (`CMakeLists.txt`, `SelfTest.{hpp,cpp}`, `main.cpp`) and merged without conflict, `ops/PointOps.{hpp,cpp}` itself entirely new and untouched by the other track. |
| 2026-08-18 | **Phase 3 step 7 (`core/Histogram`, "Histogram over the visible region"): PASS, narrow scope.** New `core/Histogram.hpp`/`.cpp` — `computeHistogram(Document, HistogramParams)` -> `HistogramResult{r,g,b,luma,sampleCount}`, a pure read-only query mirroring `core::Probe`'s shape exactly (plain params in, plain result out, one free function, no classes). `HistogramParams` reuses `ProbeParams`'s `sampleAllLayers`/`activeLayerIndex` layer-selection convention verbatim, plus a document-pixel `regionMin`/`regionMax` (half-open) and a `binCount` (default 256), with a `HistogramParams::wholeDocument(doc)` static factory for `{0,0}..{width,height}` — the "visible region" itself (a future viewport-bounds caller via `app/ViewTransform.hpp`'s `toCanvas()`) is deliberately left unwired, the same "narrow, Document-level, defer the UI" pattern `core::Probe` (step 10) already used. **Domain decision, made explicit rather than silently picked**: bins in display-encoded (sRGB) values, not the working space's scene-linear light — a histogram is read visually to judge exposure/levels, and binning in linear would crowd everything into the first few percent of the range exactly as ADR-0004 describes for un-shaped curves. This is a *different* and *reversible* choice from `color::Shaper`'s own domain: unlike Shaper's saved curve control points (a format-level commitment per ADR-0004), a histogram is recomputed fresh from the tiles every draw, so nothing is saved in this domain and re-choosing it later costs nothing. **Binning rule, simpler than `core::Probe`'s box-average on purpose**: only tiles the `TileStore` actually has allocated contribute (walked via the store's own `begin()`/`end()` — "iterate only the tiles that exist" — each tile's document-space box intersected against the requested region and skipped on no overlap, never a per-pixel `find()` over the whole region); within an allocated tile, a texel with alpha `<= 0` is skipped entirely, contributing to no bin — unlike Probe's premultiplied-sum-then-single-unpremultiply (which exists specifically to dilute an averaged box's alpha without darkening its colour), a histogram bins each pixel independently, so there's no averaging to protect and "no content" simply isn't a data point. Qualifying texels un-premultiply, `srgbEncode()` each of R/G/B, and bin R/G/B independently plus a Luma channel (`0.2126/0.7152/0.0722` — the exact three literals `shaders/grayscale_blit.wgsl` already hardcodes, re-used verbatim rather than re-derived, so the histogram's Luma and the grayscale preview agree); values outside `[0,1]` (reachable since the working space is unclamped) clamp into the first/last bin. New `--selftest histogram` case (18 checks): an all-transparent `createBlank()`'d Document reports `sampleCount == 0` and every bin zero; a hand-built one-tile fixture (pure red/green/blue opaque texels plus a fourth alpha-0 texel mixed in) lands in exactly the hand-computed R/G/B bins (255 and 0) and Luma bins (54/183/18, hand-derived from the Rec.709 weights at `binCount=256`) with the alpha-0 texel contributing to nothing and `sampleCount == 3`; a region narrower than one allocated tile excludes a same-tile pixel outside it; `wholeDocument()` is checked both structurally (`{0,0}..{width,height}`) and functionally (a pixel at the document's exact far corner, `(width-1,height-1)`, is reached — proving `regionMax` is genuinely exclusive-at-`{width,height}`, not off-by-one); and a translucent premultiplied `(0.25,0,0,0.5)` texel is checked to bin at `srgbEncode()` of its un-premultiplied straight colour `(0.5,0,0)`, with an explicit sanity check that this differs from where `srgbEncode(0.25)` (the un-premultiply-skipped/buggy answer) would have landed, and that the wrong bin holds zero. Full `--selftest` independently re-verified: all 21 sections pass, 0 FAIL, idle RSS unaffected (62.7 MB, ceiling 80 MB). Built and landed independently of the concurrently in-flight `ops/PointOps`/`color/LutBake`/`core/OpStack` steps — `core/Histogram` depends on none of them (only `core/Document`, `core/Tile`, `core/TileStore`, `color/Space`), and nothing in those steps depends on this one either. |
| 2026-08-18 | **Phase 3 step 1 (`color/Shaper`): PASS.** New `color/Shaper.hpp`/`.cpp` implementing ACEScct (published spec S-2016-005) — a real industry-standard log-domain grading transfer function, chosen (over inventing a bespoke log2 curve) specifically because ADR-0004 flags this as the single hardest-to-reverse decision in the whole colour pipeline: saved curve control points are permanent coordinates in whatever domain this function defines. Piecewise: a linear segment at/below lin=2⁻⁷ (avoiding the log2(0)/log2(negative) singularity), a log2 segment above it, spec constants (`kSlopeA=10.5402377416545`, `kOffsetB=0.0729055341958355`, `kLogA=9.72`, `kLogB=17.52`) reproduced verbatim, not re-derived. Deliberately narrow and pure, matching `color/Space.hpp`'s `srgbEncode`/`srgbDecode` precedent: no clamping (deferred to `color/LutBake`/the apply pass, both later steps), never applied to alpha (matching the established display-transfer-function policy). **I independently verified the constants myself before dispatch** (not just recalled from memory): confirmed via direct Python computation that the two branches agree in both value (0.1552511415525113, diff ~1.7e-16) and derivative (10.5402377416545 vs 10.540237741654527, diff ~2.7e-14) at the breakpoint, and that round-trip holds across negative/zero/near-breakpoint/mid-range/well-above-1.0 values — then handed the agent the exact verified formula rather than asking it to source or re-derive one. New `--selftest shaper` case (independently re-verified against my own build): re-types the spec constants independently of `Shaper.cpp`'s own copy (so the test can't pass by construction), checks breakpoint value/derivative continuity, round-trips both directions across 11+9 sample points including HDR headroom up to 16x, a hand-computable known-value check (`shaperEncode(1.0) = 9.72/17.52 ≈ 0.5547945`), and strict monotonicity. Full `--selftest` independently re-verified: all 20 sections pass, 0 FAIL, idle RSS unaffected (63.0 MB). Dispatched and landed alone, deliberately — every remaining Phase 3 step (`ops/PointOps`, the premultiply wrapper, `color/LutBake`, `core/OpStack`, the apply pass, the curve UI) depends on this domain being settled first. |
| 2026-08-18 | **1.4 lazy allocation: PASS, with one target unmet.** `main.cpp` now holds `std::unique_ptr<PaintSim>`, null until the first paint-tool stroke actually deposits (`MacPaintUI.cpp`'s stroke-start block); `--selftest`/`--diag`/`--modes` still construct it eagerly since exercising the solver is their whole point. New `src/app/Memory.{hpp,cpp}` (`task_info`/`MACH_TASK_BASIC_INFO`, current RSS not peak). **Found and fixed a real, previously-invisible bug while verifying my own pre-dispatch claim that ADR-0001's per-mode split was already done**: it was half-done — `PaintSim::setMode()` allocated the *incoming* medium's fields (already correct) but never freed the *outgoing* medium's, so a session that visited Ink then Oil held both forever, contradicting ADR-0001's own residency table. Invisible before because nothing had ever exercised switch-away-and-back in a way that surfaced it. Fixed, plus the `bindCache_` invalidation that fix newly requires (freed-then-reallocated fields reset ping-pong parity to 0, which can collide with a stale cached bind group at the same key). New `--selftest field allocation` case (7 assertions, cycles Watercolour→Ink→Oil→Watercolour) — all pass. `--diag 20` still byte-identical; `--modes` unchanged across all three media. **The `< 40 MB` idle-RSS exit criterion does not hold on real hardware**: measured idle RSS (SDL3 + window + WebGPU/Metal device, before any `PaintSim` exists) is **62.2 MB**, not the plan's untested 40 MB. This is baseline SDL/Metal/dyld overhead, unrelated to anything this project controls directly. The relative win is real and unaffected: `PaintSim` (~193 MB+) is genuinely absent until first paint, confirmed by the new field-allocation test and by launching the interactive binary (idle ~76-78 MB, ImGui context adds ~14 MB over the selftest-path baseline, `PaintSim` never constructed). Investigated further before deciding what to do about the 40 MB number (see below) rather than accepting 62 MB as an unexamined floor. |
| 2026-08-18 | **Idle-RSS breakdown: SDL's video subsystem, not WebGPU/Metal, is the cost.** Instrumented `main.cpp` with RSS checkpoints (temporary, reverted after measuring): before `SDL_Init` 3.5 MB → after `SDL_Init(SDL_INIT_VIDEO)` **46.5 MB (+43.0 MB)** → after `SDL_CreateWindow` 55.3 MB (+8.8 MB) → after `GpuContext::init` (adapter+device+surface) 59.9 MB (**+4.6 MB only**) → after `MixboxLut::load` 63.0 MB (+3.1 MB). `SDL_Init` already requests only `SDL_INIT_VIDEO`, not `SDL_INIT_EVERYTHING` — audio/haptics/gamepad were never in the picture, so that hypothesis is ruled out too. The dominant cost is SDL3's own video-subsystem init on macOS (Cocoa/AppKit, display enumeration, etc.), which any windowed SDL3 app pays before a line of this project's code runs — WebGPU/Metal, the thing that looked like the obvious suspect, is actually cheap. Confirmed SDL3 itself isn't accidentally built as a Debug config (`CMAKE_BUILD_TYPE=RelWithDebInfo` throughout). No actionable reduction found — this is the floor of the windowing framework, not something 1.4 or a follow-up can close. Recommend revising the 40 MB target rather than continuing to investigate; awaiting a decision. |
| 2026-08-18 | **1.3 arc-length dab emission: PASS.** New `src/brush/StrokePath.*` (centripetal Catmull-Rom + arc-length walk); watercolour/ink deposition moved out of `PaintSim::frame()`'s substep loop into a new cheap, self-contained `PaintSim::depositDab()` called once per emitted dab; oil kept its existing continuous contact→transfer pipeline inside `frame()` (a genuine rate, not a per-event quantity — no `P.dt` term to strip there) but now reads a dab-quantized `(lastDab → newestDab)` segment instead of the raw per-frame cursor position, which is what makes deleting `oil_transfer.wgsl`/`oil_brush.wgsl`'s `velocityCutoff` safe. `--selftest`'s new stroke-speed case: identical path at 6 vs. 240 samples, deposited mass differs by 0.0% (tolerance was 5%) — strong confirmation of ADR-0003. `--diag 20` still deterministic; final deposited mass roughly doubled (225674.0 vs. 112821.7) — traced to the old `kSplat` reading `P.dt` *after* `PaintSim::frame()` had already divided it by `activeSubsteps`, so the old code was accidentally depositing at half strength; removing the `* P.dt` term removes that accidental halving too. README's Known Bug #2 marked closed. Two pre-existing README tables (working time, board tilt) measure water/pigment magnitude that shifted with this change and are flagged as unverified-stale rather than silently left wrong; not re-measured. |
| 2026-08-18 | **1.2 fixed timestep: PASS.** `app/FixedStep.hpp`'s `consumeFixedSteps()` accumulator, wired into `main.cpp`'s render loop; `PaintSim::frame()`'s internals deliberately untouched. `--diag 20` run twice: byte-identical. Its default (no-tilt) final deposited mass (112821.7) exactly matches the value already on record at README.md:344 from before this change — confirming, as expected from reading `runDiagnostic()`'s code, that `--diag`'s numbers are **unchanged** by 1.2, since that harness calls `PaintSim::frame()` directly in its own frame-count loop and never touches `main.cpp`'s accumulator. The plan's shifted-numbers warning doesn't apply here; it's more likely aimed at 1.3, which does touch deposition math directly. New `--selftest accumulator` case (5 assertions) is what actually exercises the new code, since `--diag` structurally can't. |
| 2026-08-18 | **1.5 Mixbox seam: does not exist, not just unverified.** `NP_USE_MIXBOX` is defined by CMake but never read by any `#if`/`#ifdef` in `src/` or `shaders/` — there is no `km2` two-constant Kubelka-Munk fallback implemented. Flagged to the user; deferred out of phase 1 as its own scoped task rather than rushed as a plumbing step. See the correction in ADR-0006. |
| 2026-08-18 | **Phase 2 step 2 (`core/TileStore`) and the decode half of step 6 (`io/ImageDecode`): both PASS, built in parallel.** `TileStore`: sparse `unordered_map<TileCoord, Tile>` (allocate-on-write via `getOrCreate`, non-allocating `find`, exact iteration), `Tile` itself a flat 128×128×4 half-float buffer (`static_assert(sizeof(Tile) == 128*1024)`, matching DESIGN-imaging.md §2's rgba16float/premultiplied policy). Needed a half↔float codec that didn't exist as shared code — factored `halfToFloat` out of `sim/PaintSim.cpp`'s anonymous namespace into new `core/Half.{hpp,cpp}`, added the missing `floatToHalf` beside it, deleted the private copy. Both directions checked bit-exact against hardware `_Float16` across their full input domains (2^32 floats, 2^16 halfs) before landing; `--selftest`'s new `tile store` case carries a much smaller permanent regression sample of that sweep plus TileStore's own allocate/query/iterate/negative-coordinate assertions (17 checks). `io/ImageDecode`: deliberately *not* the full `io/ImageIO`/`io/StbBackend` step — decode-only (file bytes → linear float RGBA in memory), since packing into tiles depends on TileStore's API, which was still being decided concurrently. Assumes sRGB transfer function on decode (documented, not silent — PNG/JPEG/TGA/BMP carry no colour-profile info `stb_image` surfaces). Required widening `paint/Palette.cpp`'s `STBI_ONLY_PNG` to also cover JPEG/BMP/TGA at the one `STB_IMAGE_IMPLEMENTATION` site in the binary — verified against `stb_image.h`'s actual macro logic, not just assumed. New `--selftest image decode` case (27 checks: 8-bit PNG incl. no-alpha, 16-bit PNG via a hand-built fixture since `stb_image_write` has no 16-bit PNG writer, BMP, TGA, JPEG with lossy-appropriate tolerance). Both tracks shared only the usual append-only files (`CMakeLists.txt`, `SelfTest.{hpp,cpp}`, `main.cpp`) and merged without conflict — the `ImageDecode` agent even caught and fixed a missing `core/Half.cpp` CMakeLists.txt registration from the concurrent `TileStore` track before either was reviewed. Full `--selftest` independently re-verified after both landed: all 9 sections pass, 0 FAIL, idle RSS unchanged at 63.8 MB. |
| 2026-08-18 | **Phase 2 step 4 (`core/Document` + `core/Layer`): PASS.** New `core/Layer.hpp` (`LayerKind` — the 7 CONTEXT.md kinds — plus `Layer{ kind, std::optional<TileStore> rgbTiles }`, populated only for `RGB`) and `core/Document.hpp` (`width`/`height`/`WorkingSpace`/`std::vector<Layer> layers`), both header-only. Deliberately did not build `Document::createBlank()` (step 5's job — it has open policy questions this step leaves alone) or the base-layer-has-alpha guarantee (step 14 — falls out for free from `Layer` having no special-cased Background subtype, nothing explicit needed). **Consolidated a real duplication before it could calcify**: the `app/Keymap` agent (previous batch) had already sketched its own `LayerKind` enum directly in `app/Keymap.hpp`, anticipating this step but getting the dependency direction backwards (app defining a core domain concept). Relocated `LayerKind` and its `layerKindName`/`layerKindFromName` helpers into `core/Layer.hpp`, updated `Keymap.hpp`/`.cpp` to include and reuse it, deleted the duplicate — same move as the concurrent `core/Half` consolidation of `halfToFloat` two entries up. `Layer::rgbTiles` is `std::optional<core::TileStore>` rather than a mandatory member specifically so Phase 5's Pigment/Media layers (DESIGN-imaging.md §2: 7-channel latent+mass tiles, a different shape entirely from RGB's 4-channel rgba16float) can add their own differently-typed storage later without reshaping this field. New `--selftest document` case (11 checks: all 7 kinds round-trip through the relocated name helpers, a default layer is Pigment-kinded with no RGB storage, a hand-built one-entry RGB layer's pixel round-trips through the already-tested `TileStore` API). Full `--selftest` independently re-verified after landing: all 10 sections pass, 0 FAIL — critically including `keymap`, unchanged despite the enum relocation touching its owning file. Idle RSS unaffected (63.2 MB). |
| 2026-08-18 | **Phase 2 step 5 (`Document::createBlank`): PASS.** Static factory on `core/Document.hpp`, header-only, per PRD C7. Two policy calls it had to make, both flagged in-comment rather than silently decided: (1) the blank layer is `RGB`-kind, not CONTEXT.md's nominal `Pigment` default — Pigment has no working storage yet, and a document you can't paint on would defeat the phase's purpose; documented as a provisional "ship 1" compromise pending Phase 5. (2) the RGB layer's `TileStore` is populated but starts at zero occupied tiles regardless of canvas size, per PRD C2 and `TileStore`'s allocate-on-write design — no pre-fill loop. New `--selftest create blank` case, checked at both a small (64x48) and a large (4096x4096) canvas — the large-canvas check is the one that actually catches a wrong "pre-allocate a grid" implementation, since a small canvas could pass by accident. Full `--selftest` independently re-verified: all 11 sections pass, 0 FAIL, idle RSS unaffected (64.0 MB). |
| 2026-08-18 | **Phase 2 step 6, completed (`io/ImageIO`): PASS.** New `io/ImageIO.hpp`/`.cpp`, built directly on `Document::createBlank()` rather than re-deciding "what does a fresh Document look like" a second time — the dependency this step was deliberately sequenced after `createBlank` to respect. Two entry points: `writeDecodedImageIntoLayer(DecodedImage, Layer&)` (premultiply + half-float pack into an *existing* layer's tiles — factored out now because PLAN.md step 13, "place an image as a layer," will need exactly this against a layer in an already-open document, not a speculative split) and `openImageAsDocument(bytes) -> optional<Document>` (decode → `createBlank` → `writeDecodedImageIntoLayer`). Premultiplication (`rgb *= a`, alpha unchanged) closes the gap `io/ImageDecode` deliberately left open — DESIGN-imaging.md §2's storage policy is premultiplied, decode intentionally stayed straight so this step would own that conversion. New `--selftest image io` case (16 checks) specifically proves premultiply ran rather than merely passing alpha through (checks the premultiplied value differs from the un-multiplied one, not just that it's *some* plausible number), and separately proves memory tracks content, not canvas size (PRD C2, this phase's own stated verify criterion): a 140×140 fixture occupies exactly the 4 tiles its footprint spans. Three failure modes (garbage bytes, truncated real PNG, null input) all fail cleanly to `std::nullopt` rather than returning a bogus Document. Full `--selftest` independently re-verified: all 12 sections pass, 0 FAIL, idle RSS unaffected (63.8 MB). Phase 2 step 6 is now fully closed (decode half from the prior batch + this tile-write half). |
| 2026-08-18 | **Phase 2 step 8 (`ui/NaturalPaintUI`, "Tiled viewport draw"): PASS, narrow scope.** New, self-contained `src/ui/NaturalPaintUI.hpp`/`.cpp` — read-only proof of `core::Document` → one `RGBA16Float` GPU texture per occupied `core::Tile` → screen, via `TiledDocumentView::setDocument()`/`draw()`. Deliberately does not touch the interactive painting path: `MacPaintUI.{hpp,cpp}`, `Theme.*`, and `AppState.hpp`'s existing fields are untouched (confirmed by file mtime — all last modified 1-2 hours before this dispatch began), the module never references `PaintSim`, and the PaintSim→`TileStore` "bake" bridge (DESIGN-imaging.md, cadence still undecided) stays explicitly out of scope. Pan/zoom (`zoomOnWheel`/`applyPan`) is `MacPaintUI.cpp`'s existing cursor-anchored wheel-zoom and drag-pan math, ported to take an explicit `CanvasView&` parameter instead of reaching into `AppState` — reuses `AppState.hpp`'s `CanvasView` type rather than inventing a parallel one, per the brief. `tileScreenRect()` is pure geometry (`canvasOrigin + tileOrigin(coord)*zoom + pan`, size `kTileSize*zoom`), independently callable from `--selftest` with no GPU/ImGui context. New `--selftest tiled viewport` case (15 checks) goes beyond "it compiled": a dedicated offscreen WebGPU render pipeline (embedded test-only WGSL blit shader, nearest-texel sampling) places an uploaded tile at `tileScreenRect()`'s own computed rect and reads it back via the same copy-to-buffer/`wgpuBufferMapAsync` technique as `PaintSim::readbackField`, confirming a known 2×2 fixture's four corner colours land at the exact expected screen pixels, an untouched tile interior reads transparent black, and the area outside the tile's quad stays at the clear colour — genuinely exercises the upload and position math end to end, not just a CPU-side geometry check. Full `--selftest` independently re-verified: all 13 sections pass, 0 FAIL, idle RSS unaffected (63.9-64.0 MB). |
| 2026-08-18 | **Phase 2 step 14 (base layer has alpha, PRD C16): PASS — already true by construction, now proven.** `core/Layer.hpp` has no background/locked concept at all: every `Layer`, first or otherwise, is the same struct with the same `std::optional<TileStore>`. New `--selftest base layer alpha` case (4 checks) proves the two things a locked/special Background layer *would* do differently and this one doesn't: refuse a non-opaque alpha (writes alpha=0 through the ordinary `TileStore` API and reads it back unclamped) and refuse an arbitrary partial value (writes/reads alpha=0.75). No implementation change — this step is a verification-only closure. Full `--selftest` independently re-verified: all 14 sections pass, 0 FAIL, idle RSS unaffected (63.9 MB). |
| 2026-08-18 | **Phase 2 step 13 (`placeImageAsLayer`): PASS, narrow scope.** Two new overloads in `io/ImageIO.hpp`/`.cpp` — `placeImageAsLayer(Document&, const DecodedImage&)` and a file-bytes convenience overload — build a fresh `RGB`-kind layer using `Document::createBlank()`'s own policy (not a second, competing way to build one), fill it via the existing `writeDecodedImageIntoLayer()` (no duplicated premultiply/pack loop), and `push_back` it onto `doc.layers`, landing on top per `Document.hpp`'s own "ordered, bottom-to-top" contract. Placed at document origin (0,0) — no drag-to-position parameter, that's real feature work for later. Deliberately does not wire into any interactive menu or drag-drop: PLAN.md's own step-13 wording ("place an image as a layer into the *open* document") presupposes the interactive canvas already has a `Document`/layer stack it's rendering, which it doesn't — `MacPaintUI.cpp` still only knows `PaintSim`'s single dense texture. That's the same still-undecided PaintSim↔Document bake bridge step 8 deferred; this step hits the identical wall for the identical reason, so it stays deferred rather than guessed at. `MacPaintUI.*`/`AppState.hpp`/`Theme.*` confirmed untouched by file mtime. New `--selftest place image as layer` case (16 checks): a `createBlank()`'d 1-layer document ends up with exactly 2 layers after placing (base layer at index 0 byte-for-byte unchanged, new layer appended at index 1, correctly premultiplied — proven via the same "differs from the un-multiplied value" discipline `runImageIOTest` established, not just "a plausible number"); a 0-layer document ends up with exactly 1 layer after placing (the function only ever appends, never assumes index 0 pre-exists); the `DecodedImage`-taking overload exercised directly, not only reached through the file-bytes path; and two clean-failure cases (garbage bytes, an invalid `DecodedImage`) both leave `doc.layers` completely untouched. Full `--selftest` independently re-verified: all 15 sections pass, 0 FAIL, idle RSS unaffected (63.0 MB). |
| 2026-08-18 | **Phase 2 step 10 (`core/Probe`): PASS, narrow scope.** New `core/Probe.hpp`/`.cpp` — `probePixel(Document, PixelCoord, ProbeParams)` -> `ProbeSample{linear, display}`, a pure read-only query (`TileStore::find()`, never `getOrCreate()` — probing unpainted area allocates nothing). `ProbeParams` carries `sampleSize` (NxN box average, 1 = point sample, Photoshop's point/3×3/5×5 shape) and `sampleAllLayers`/`activeLayerIndex` as real parameters, per step 10's own "not separate tools" wording. Correctness detail worth recording: box averages sum *premultiplied* texels first and un-premultiply once at the end, not per-texel-then-average — this is what makes a box straddling painted/unpainted texels dilute alpha correctly (4-of-9 painted → alpha lands at exactly 4/9) without darkening the reported colour toward black; un-premultiplying every texel first would have to invent an RGB value for the unpainted ones and wrongly drag the average down. `display` is `linear`'s RGB run through the already-built `color::srgbEncode()`; alpha passes through unencoded in both forms, matching `io/ImageDecode`'s established "alpha is opacity, not light" policy. Deliberately does not wire into `AppState`'s brush/foreground-colour state or any UI — the "eyedropper writes to foreground colour" half of PRD Q10 needs docs/ui.md §3.3's still-undesigned RGB/PIGMENT colour-picker mode toggle, since today's `BrushState` only holds a palette index, not an arbitrary RGB value; deferred for the same reason step 8 deferred the PaintSim↔Document bridge. `MacPaintUI.*`/`AppState.hpp`/`Theme.*` confirmed untouched by file mtime. One documented, currently-unobservable simplification: `sampleAllLayers`'s "sum every populated RGB layer" is a plain sum, not Porter-Duff compositing, because no blend-mode implementation exists anywhere in this codebase yet — today's Documents never have more than one layer with *actually painted* content at a given point (even after step 13's `placeImageAsLayer`, a second layer only ever coexists with an empty, zero-occupied-tile base layer), so the plain sum is provably correct for every Document this codebase can currently produce; the comment correctly flags this as exactly where real per-layer alpha-under compositing must replace the sum once multi-layer painting lands, rather than hiding the gap. New `--selftest pixel probe` case (14 checks): point sample exact match (both linear and a from-the-source-byte round-trip check that display isn't accidentally the unencoded linear value); 3×3 box average against a hand-computed per-channel mean over a fully-painted fixture, shown to genuinely differ from the centre pixel alone; the corner-straddling box's alpha-dilutes-without-darkening property (hand-verified against the exact fixture bytes); a translucent-pixel case proving un-premultiplication actually ran (checked against the tile's own raw premultiplied storage, not a plausible number); `sampleAllLayers` vs. active-layer agreement (the one thing testable today, since the two modes can't yet observably differ); and five misuse/edge cases (far-away coordinate, no-layer Document, zero-tile `createBlank()`'d Document, out-of-range layer index, `sampleSize <= 0`) all reading back safely rather than crashing. Full `--selftest` independently re-verified: all 16 sections pass, 0 FAIL, idle RSS unaffected (63.1 MB). |
| 2026-08-18 | **Phase 2 step 11 (view controls, one view matrix, PRD Q1-Q4): PASS.** New `app/ViewTransform.hpp`/`.cpp`: `Mat2` (2x2 linear map with an analytic adjugate/determinant `inverse()`) and `ViewTransform`, which builds `M = zoom * rotate(theta) * mirror(mx,my)` once from a `CanvasView` and exposes `toScreen()`/`toCanvas()` — both reading the same `M` (`toCanvas()` via `M.inverse()`), so the two are provably exact inverses rather than independently hand-derived formulas, per docs/shortcuts.md §3's own mandate. Hand-verified the derivation myself independently of the agent's own comments (mirror-then-rotate-then-scale algebra, and the `M = zoom*I` reduction at identity that makes this exactly reproduce the pre-step-11 `origin + p*zoom` formula) and independently re-derived the test's hand-worked case (zoom=2, mirrorX, 90°, off-centre pivot) by hand from the matrix — matches the test's own expected value and the code's actual output. `AppState.hpp`'s `CanvasView` gained `mirrorX`/`mirrorY`/`rotation`/`grayscale`; `zoom`/`panX`/`panY` kept their exact prior meaning, so `NaturalPaintUI.cpp` (step 8) needed zero changes. `MacPaintUI.cpp`'s canvas block now draws via `AddImageQuad` (not `AddImage`, which can't express a flipped/rotated quad) with corners from `ViewTransform::toScreen()`, and pointer input (`tx`/`ty`) comes from `ViewTransform::toCanvas()` — replacing the old direct `(mouse-origin)/zoom` formula with the transform's actual inverse, which is the concrete meaning of "pen input maps back through its inverse." Added fit-to-window/100%/keyboard zoom-in-out (a request-then-consume flag pattern, since they need the canvas window's on-screen size, only known inside its own `Begin()`/`End()` block) and an `R`-held-plus-left-drag rotate gesture (angular delta via `(v x d)/|v|^2` around the pivot, a standard small-angle approximation, guarded against a near-zero radius) — deliberately read as live key state rather than routed through `app/Keymap`'s `resolve()`, since that dispatcher fires once per discrete keydown and has no way to express "held"; `⇧R` reset *is* a discrete keymap action for exactly that reason. New `keymaps/default.json` rows and `main.cpp` dispatch arms for `⌘+`/`⌘−`/`⌘0`/`⌘1`/`F`/`⇧F`/`⇧R`/`⌘Y`, matching docs/shortcuts.md §3 exactly. Grayscale preview (`sim/PaintSim`'s `updateGrayscalePreview()` + new `shaders/grayscale_blit.wgsl`, Rec.709 luma) is a dedicated per-pixel desaturate GPU pass into its own separate texture — not an ImGui tint multiply, which can't collapse independent R/G/B into a luminance average — called from `main.cpp` *after* `frame()`'s own submission each render iteration (not from inside `drawUI`, which runs earlier the same iteration, before that frame's physics/composite work is even submitted) so it reads the fresh composite, not the previous frame's; verified this GPU-submission-order reasoning is actually correct WebGPU semantics (same-queue submissions execute in program order). `⌘⌥0` "zoom to selection" and "scrubby zoom" both correctly omitted rather than faked — no selection concept exists before phase 7, and wheel/keyboard zoom already cover Q1's core intent. New `--selftest view transform` case (46 assertions): round-trip identity (`toCanvas(toScreen(p)) == p`) across 8 zoom/pan/mirror/rotation combinations x 5 sample points each, including mirrors and rotation together; the hand-worked known-point case above; and a view-only proof that toggling mirror/rotation/grayscale never mutates `PaintSim`'s own canvas bytes (readback before/after, byte-identical) — the explicit, flagged substitute for PLAN.md's literal "save with a mirror on, reopen — unmirrored," since no document-save path exists yet in this codebase. Beyond `--selftest` (which never exercises `MacPaintUI::drawUI()` at all — headless by construction, same limitation the pre-existing interactive canvas code already had), independently smoke-tested the actual interactive binary: launched it, confirmed GPU/adapter init and 3 seconds of real rendering through the new `ViewTransform`/`AddImageQuad` path with no crash or error output — the practical limit of dynamic verification available without a tool to drive this native window's mouse/keyboard from outside it. `Theme.*`, `core/Document.hpp`, `core/Layer.hpp`, `core/TileStore.hpp`, `io/*` confirmed untouched by file mtime. One documented simplification worth recording: keyboard-triggered zoom (fit/100%/in/out) reuses the wheel-zoom formula's pre-existing anchor, which — despite its own comment — never actually reads the mouse cursor position (it anchors to `origin - canvasPos`, the current pan offset); this quirk predates step 11 and was deliberately preserved exactly, not "fixed," since exact behavioral preservation at identity view-state was step 11's own regression bar. Full `--selftest` independently re-verified from a from-scratch `rm -rf build` reconfigure and rebuild: all 17 sections pass, 0 FAIL, idle RSS 63.1 MB (ceiling 80 MB) — confirms the eagerly-allocated ~4 MB grayscale texture didn't meaningfully move the needle. |
| 2026-08-18 | **Phase 2 step 9 (mip pyramid): PASS.** Built entirely as an ephemeral, CPU-side, upload-time detail of `ui/NaturalPaintUI.{hpp,cpp}` — `core::Tile`'s exact 128 KB size (`static_assert(sizeof(Tile) == 128*1024)`) and `core::TileStore`'s allocate-on-write contract are untouched, confirmed by file mtime, so no other Phase-2 piece's memory-cost assumptions moved. `buildMipChain(const Tile&)` decodes level 0 via `readPixel()` then recursively 2x2-box-filters each subsequent level from the one directly above it (128→64→32→16→8→4→2→1, 8 levels for `kTileSize=128=2^7`) — genuinely recursive, not repeatedly downsampling level 0, which is what makes it match a real GPU mip pyramid rather than a shallower approximation. A plain box-filter average is the mathematically correct downsample here specifically because `core::Tile` stores premultiplied RGBA (`DESIGN-imaging.md` §2) — no separate unpremultiply/premultiply step needed, one of the standard benefits of premultiplied alpha for filtering. `mipLevelForZoom(zoom) = clamp(floor(-log2(zoom)), 0, 7)` matches the plan's own literal example exactly (zoom=0.25 → level 2, the 32px level); `TiledDocumentView::draw()` binds one explicit single-level `WGPUTextureView` per tile (built once in `setDocument()`, not per-frame) rather than relying on the GPU/ImGui backend's own automatic LOD selection. Hand-verified the box-filter math myself against a hand-built 4x4 ramp fixture (both the level 0→1 step and the recursive level 1→2 step) and the `mipLevelForZoom` formula across six cases (100%, 25%, 50%, and both zoom extremes) — all match. New `--selftest mip pyramid` case: CPU-only downsample correctness against the hand-computed ramp values; the level-selection formula; and an end-to-end GPU proof (a finest-period checkerboard tile, whose exact-uniform 2x2-average property makes level 2's expected colour unambiguous) rendered through an offscreen blit and read back, confirmed to match the *downsampled* level's colour and to differ from level 0's own pure-checker colour at the same screen position — proving level selection reaches the real draw path, not just computed and discarded. One incidental, disclosed, and verified-harmless side effect: this step's own agent found the shared build broken by step 12's concurrent in-flight work (a missing `CMakeLists.txt` registration for the not-yet-complete `app/Snapping.cpp`) and added that one registration line to unblock its own verification — confirmed via mtime that it touched nothing else outside its own stated files. |
| 2026-08-18 | **Phase 2 step 12 (rulers, guides, grid, snapping, PRD Q5-Q7): PASS.** New `app/Snapping.{hpp,cpp}` (pure math, no ImGui/GPU dependency): `gridLinePositions()`/`isMajorGridLine()`, `parseGuidePosition()` (plain number or percentage), and `resolveSnap()` (nearest-candidate-wins per axis independently, with grid < canvas-edges < guides priority on exact ties — guides win as "the most deliberate placement of the three"). `AppState.hpp` gained `Guide`/`GuideOrientation` and session-local fields (`guides`, `showRulers`, `showGuides`, `showGrid`, `snappingEnabled`, `gridSpacing`, `gridSubdivisions`, `pendingGuide`) — guides are explicitly *view*/session state, not document state, since this codebase has neither a save path nor a live-canvas-to-`Document` bridge yet (the same deferred-bridge theme steps 8/10/13 already hit); true persistence waits for both. `MacPaintUI.cpp` draws rulers, the grid overlay and guide lines all through the exact same `ViewTransform` (step 11) every other document-space thing on the canvas already goes through — not a second, parallel position mapping — and reserves a ruler-thickness band via `paintOrigin`/`avail` that provably reduces to the pre-existing `canvasPos`/`avail` exactly when rulers are off (the same "identity is a no-op" regression discipline step 11 established). Drag-to-create guides claims the left-mouse-drag gesture with the same mutual-exclusion pattern already governing rotate-drag vs. pan (`!st.pendingGuide.has_value()` guards on all three). **Verified independently that snapping cannot reach freehand painting**: `resolveSnap()` is called from exactly one place (the pending-guide drag block); the stroke-deposit condition explicitly excludes an in-progress guide drag, and I confirmed by reading the full stroke block that no snap call sits anywhere on that path — a brush stroke is exactly as continuous and unsnapped as before this step. **Caught and correctly routed around a real pre-existing spec conflict before dispatch**: `docs/shortcuts.md` §3 assigns `⌘R` to rulers, but `keymaps/default.json` already bound `⌘R` to `reload_shaders` from early Phase-1 work; rulers ships menu-only (no keyboard shortcut) rather than silently colliding or unilaterally picking a different key — flagged as an unresolved documentation/product decision, not fixed here. Hand-verified `resolveSnap()`'s priority and independent-per-axis behaviour against six hand-computed cases (both-guides snap, nothing-in-range, grid-snaps-X-while-Y-stays-free, both canvas corners, and the zero-threshold global-off case) — all match by hand calculation. "Layer bounds" from PRD Q6 is exactly canvas-edge snapping today (no separate code path), since the interactive canvas has only one implicit layer — documented, not faked. New `--selftest guides/grid/snap` case covers `gridLinePositions()`'s 0-anchored lattice (a queried range not starting at 0 still lands on the same grid, not one offset to the range's own start), `parseGuidePosition()`'s number/percentage/whitespace/junk handling, and `resolveSnap()`'s full six-case suite above. Full `--selftest` independently re-verified from a combined build (both this step and step 9 landed together): all 19 sections pass, 0 FAIL, idle RSS 63.6 MB (ceiling 80 MB). Beyond `--selftest`, smoke-tested the actual interactive binary twice (once per step, since both touch `MacPaintUI::drawUI()` extensively) — clean GPU/adapter init and several seconds of real rendering with no crash or error output each time. |
| 2026-08-17 | **1.1 latency baseline: PASS.** Two interactively painted strokes (mouse, not tablet — this dev machine has no pen), measured pen-to-photon via `--latency`: stroke 1 n=108 p50=12.1ms p99=15.7ms; stroke 2 n=73 p50=12.4ms p99=16.4ms. Both clear the <20ms target (PRD F3) with margin, and are well inside the 30ms stop-and-investigate threshold. PRD risk #1 (latency unreachable on wgpu) does not materialize — proceed to 1.2. |

---

# Deviations

Where implementation diverged from the plan, and why. Keeping this honest is what
makes the plan worth re-reading.

| step | deviation | reason |
|---|---|---|
| 1.1 | Timestamped `SDL_EVENT_MOUSE_MOTION`/`MOUSE_BUTTON_DOWN` as well as `SDL_EVENT_PEN_*`, using each event's own `e.common.timestamp` rather than a fresh `SDL_GetTicksNS()` call at poll time | The plan's "SDL_PenEvent" reads as pen-only, but no dev machine here has tablet hardware, so a pen-only gate would make `--latency` unverifiable without one. `e.common.timestamp` is populated by SDL at event generation (still `SDL_GetTicksNS()`-based per SDL3's own docs), which is closer to true arrival time than a timestamp taken whenever the app happens to drain its queue. |
| 2.6 | Built only the decode half of step 6 (new `io/ImageDecode`, not `io/ImageIO`/`io/StbBackend`) — file bytes to linear float RGBA in memory, no tile packing/premultiply/write | Dispatched in parallel with `core/TileStore` (step 2); the rest of step 6 needs TileStore's finished, reviewed API to write into, which didn't exist yet. Splitting at the real dependency boundary let both proceed concurrently without one guessing at the other's interface. The pack/premultiply/tile-write remainder is still owed under the `io/ImageIO` name once `core/Document`/`core/Layer` (step 4) exist to receive it. |
| 4.8 | Went back into step 7's `File > Export As...` dialog: its Export button is now live against the active document, its output-path field is new, and its "nothing to export from" sentence was replaced | That sentence named step 8 as the step that would fix it, and step 8 has now put documents in `AppState`. Leaving an on-screen explanation that had become false would have been worse than the small scope increase, and enabling the button is the concrete evidence the ownership decision is load-bearing rather than decorative. |
| 4.8 | Renamed the File menu's `New` (which clears the solver canvas) to `New Canvas`, alongside the new `New Document` | Two commands one word apart doing entirely different things is exactly where the canvas-is-not-a-document gap would first get papered over. The rename costs nothing and makes the gap visible in the place a user meets it. |
| 4.8 | Added ~230 lines of File-menu and modal code to `ui/MacPaintUI.cpp`, which PLAN.md's UI section says is **replaced, not extended** | The replacement chrome does not exist yet, and a document lifecycle unreachable from the running application would have been the worse of the two failures — the same trade step 7's dialog already made. Recorded as throw-away work rather than as architecture. |
| 4.9 | Built as **`app/Journal`**, not PLAN.md's `core/Journal` | The module serialises an `app::OpenDocument` through `io/NpaintFile`'s `saveNpaint()` (PRD O7 makes that writer mandatory), and a `core/` module may include neither: core/Layer.hpp states "app/ depends on core/, never the reverse", and io/NpaintFile.hpp and app/DocumentLifecycle.hpp both already refused to put a file format's shape on the domain model. A `core::Document` also has no bound path and no dirty state, so it cannot decide *whether* to write. The plan's directory is wrong; its content is not. |
| 4.9 | Added `EditKind` and `structuralRevision` to step 8's `OpenDocument`, one step after it shipped | ADR-0008 writes on a timer *and* after any structural edit, and step 8's dirty model had no way to tell a structural edit from a painted stroke -- without the distinction the journal would either write on every edit (the alternative ADR-0008 explicitly rejects) or on none. Defaulted to `Structural`, which is the correct classification for every caller that already existed, so no call site changed. Preferred to a second notion of dirtiness owned by the journal. |
| 4.9 | Added ~120 lines of recovery-offer modal and a File menu item to `ui/MacPaintUI.cpp`, which PLAN.md's UI section says is **replaced, not extended** | Same trade as steps 7 and 8, and one PRD O8 forces: "unclean scratch directories are **offered** for recovery on launch" is not delivered by a function no user can reach. Recorded as throw-away work rather than as architecture. |
| 4.9 | The journal calls `tileCacheInvalidate()` on its own scratch path although no stale read is reachable through it today | The path is written only by the journal and read only through `loadNpaint()`, which does not go through the `ImageCache`. The call is there because the alternative is a comment asserting no future caller opens a residency on a recovery file, and recovering a large document through a cached residency is exactly what step 5's module is for. Enforcing it inside `saveNpaint()` instead -- where it belongs -- needs the `io/TileResidency` -> `io/NpaintFile` include cycle broken; recorded as debt in Findings, with the third direct caller of `saveNpaint()` as the unblocking condition. |
